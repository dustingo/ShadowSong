---
phase: 09-close-v1-1-milestone-tech-debt
plan: 01
type: execute
wave: 2
depends_on: ["09-02", "09-03"]
files_modified:
  - .planning/PROJECT.md
autonomous: true
requirements: [VER-03]
must_haves:
  truths:
    - "`.planning/PROJECT.md` 反映的是 Phase 9 执行完成后的最终真相：v1.1 Enterprise Access Control 已完成并可归档，而不是“cleanup 仍待处理”的中间态。"
    - "`Validated`、`Active`、`Current State`、`Current Milestone` 四个区块与 ROADMAP、STATE、milestone audit 和 Phase 8/9 已交付结果一致，不再互相打架。"
    - "PROJECT 文档不再把前端权限感知、验证、审计日志或权限收口描述成待办，也不再保留已过时的 Phase 6/Phase 8 作为最新阶段。"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "final post-cleanup project truth source for v1.1 milestone closure"
  key_links:
    - from: ".planning/PROJECT.md"
      to: ".planning/ROADMAP.md"
      via: "same milestone completion and latest-phase wording after Phase 9"
      pattern: "Phase 9|v1.1|已完成|archive"
    - from: ".planning/PROJECT.md"
      to: ".planning/STATE.md"
      via: "same current position and cleanup-complete state"
      pattern: "Phase: 9|Status:"
    - from: ".planning/PROJECT.md"
      to: ".planning/v1.1-MILESTONE-AUDIT.md"
      via: "same final audit interpretation after cleanup debt is closed"
      pattern: "23/23|5/5|tech_debt"
---

<objective>
把 `.planning/PROJECT.md` 收口成 v1.1 在 Phase 9 完成后的最终真相文档，供里程碑归档、后续 roadmap 迁移和历史回看直接使用。

Purpose: 审计指出的不是普通文案陈旧，而是项目真相文档落在里程碑完成状态之后。如果 Phase 9 结束后 PROJECT 仍写成“cleanup 待处理”，它就继续是错误的真相源。
Output: 一份面向 milestone-complete 状态的 `.planning/PROJECT.md`，准确描述 v1.1 已闭环且仅保留后续里程碑选择空间。
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/08-ship-permission-aware-ui-and-verification/08-03-SUMMARY.md
@.planning/phases/09-close-v1-1-milestone-tech-debt/09-02-PLAN.md
@.planning/phases/09-close-v1-1-milestone-tech-debt/09-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite PROJECT sections to describe the final post-cleanup v1.1-complete state</name>
  <files>.planning/PROJECT.md</files>
  <action>Rewrite `.planning/PROJECT.md` for the end-of-Phase-9 truth, not the beginning-of-Phase-9 baseline. Update `What This Is`, `Validated`, `Active`, `Current State`, and `Current Milestone` so they describe a system that has already completed Phase 5-9 and closed the milestone audit tech debt. Move shipped access-control outcomes into `Validated`; remove or rewrite stale `Active` items that still describe frontend permission awareness, verification, or audit logging as pending; and make `Current State` / `Current Milestone` clearly say v1.1 is complete and archive-ready after the cleanup work. Do not leave wording that implies Phase 9 cleanup is still open.</action>
  <verify>
    <automated>powershell -Command "$project = Get-Content '.planning/PROJECT.md' -Raw; if ($project -match '最新阶段：Phase 6 已完成') { Write-Error 'stale Phase 6 wording remains'; exit 1 }; if ($project -match 'Phase 9 cleanup' -or $project -match 'cleanup phase added' -or $project -match '仍待处理') { Write-Error 'intermediate cleanup wording remains'; exit 1 }; if ($project -notmatch 'v1.1 Enterprise Access Control' -or $project -notmatch '已完成') { Write-Error 'milestone completion not stated'; exit 1 }"</automated>
  </verify>
  <done>PROJECT describes the final v1.1-complete state rather than an in-progress cleanup state.</done>
</task>

<task type="auto">
  <name>Task 2: Prove PROJECT section alignment against roadmap, state, requirements, and the milestone audit</name>
  <files>.planning/PROJECT.md</files>
  <action>Perform the final sync pass so `.planning/PROJECT.md` aligns with the execution-finished planning state. `Validated` should reflect that all v1 requirements are complete, `Active` should no longer contradict `.planning/REQUIREMENTS.md` or the audit’s `23/23` and `5/5` results, `Current State` should no longer claim Phase 6 or Phase 8 as the latest completed stage, and `Current Milestone` should match the final milestone narrative rather than the pre-cleanup target-feature list. Refresh the footer metadata to the Phase 9 completion date actually used during execution. Keep scope to PROJECT; do not archive the milestone here.</action>
  <verify>
    <automated>powershell -Command "$project = Get-Content '.planning/PROJECT.md' -Raw; $roadmap = Get-Content '.planning/ROADMAP.md' -Raw; $state = Get-Content '.planning/STATE.md' -Raw; $req = Get-Content '.planning/REQUIREMENTS.md' -Raw; $audit = Get-Content '.planning/v1.1-MILESTONE-AUDIT.md' -Raw; if ($roadmap -notmatch 'Phase 9: Close v1.1 Milestone Tech Debt') { Write-Error 'roadmap phase missing'; exit 1 }; if ($req -notmatch '23 total' -or $req -notmatch 'Unmapped: 0') { Write-Error 'requirements completeness baseline missing'; exit 1 }; if ($audit -notmatch '23/23' -or $audit -notmatch '5/5') { Write-Error 'audit completion baseline missing'; exit 1 }; if ($project -match '让前端菜单、页面和操作按钮与后端权限校验保持一致' -or $project -match '为权限矩阵补齐验证与回归测试' -or $project -match '为关键用户与权限操作补齐审计日志') { Write-Error 'obsolete active milestone bullets remain'; exit 1 }; if ($project -notmatch '权限感知 UI' -or $project -notmatch '审计日志' -or $project -notmatch '强制改密') { Write-Error 'validated/current state incomplete'; exit 1 }; if ($project -notmatch 'Last updated: 2026-04-15') { Write-Error 'footer not refreshed to final cleanup date'; exit 1 }"</automated>
  </verify>
  <done>PROJECT is section-by-section consistent with the final roadmap/state/audit truth after Phase 9 closes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning truth -> milestone archive and future roadmap work | `.planning/PROJECT.md` is treated as the human-readable source of truth for what the project currently is |
| shipped implementation -> documentation state | The document must reflect the same completed access-control baseline proven by requirements, verification, and audit artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | T | `.planning/PROJECT.md` left in pre-cleanup or intermediate cleanup state | mitigate | Rewrite the milestone-facing sections for the final post-Phase-9 truth, not the pre-execution baseline |
| T-09-02 | R | PROJECT sections contradict roadmap/state/audit | mitigate | Validate section content against ROADMAP, STATE, REQUIREMENTS, and the milestone audit instead of only grepping a few words |
</threat_model>

<verification>
- `powershell -Command "$project = Get-Content '.planning/PROJECT.md' -Raw; if ($project -match '最新阶段：Phase 6 已完成') { exit 1 }; if ($project -match 'Phase 9 cleanup' -or $project -match '仍待处理') { exit 1 }; if ($project -notmatch 'v1.1 Enterprise Access Control' -or $project -notmatch '已完成') { exit 1 }"`
- `powershell -Command "$project = Get-Content '.planning/PROJECT.md' -Raw; $roadmap = Get-Content '.planning/ROADMAP.md' -Raw; $state = Get-Content '.planning/STATE.md' -Raw; $req = Get-Content '.planning/REQUIREMENTS.md' -Raw; $audit = Get-Content '.planning/v1.1-MILESTONE-AUDIT.md' -Raw; if ($roadmap -notmatch 'Phase 9: Close v1.1 Milestone Tech Debt') { exit 1 }; if ($req -notmatch '23 total' -or $req -notmatch 'Unmapped: 0') { exit 1 }; if ($audit -notmatch '23/23' -or $audit -notmatch '5/5') { exit 1 }; if ($project -match '让前端菜单、页面和操作按钮与后端权限校验保持一致' -or $project -match '为权限矩阵补齐验证与回归测试' -or $project -match '为关键用户与权限操作补齐审计日志') { exit 1 }; if ($project -notmatch '权限感知 UI' -or $project -notmatch '审计日志' -or $project -notmatch '强制改密') { exit 1 }; if ($project -notmatch 'Last updated: 2026-04-15') { exit 1 }"`
</verification>

<success_criteria>
- `.planning/PROJECT.md` expresses the final v1.1-complete truth after Phase 9.
- No section in PROJECT still describes Phase 8 deliverables or Phase 9 cleanup as pending work.
- PROJECT aligns with roadmap/state/requirements/audit evidence strongly enough for milestone archive prep.
</success_criteria>

<output>
After completion, create `.planning/phases/09-close-v1-1-milestone-tech-debt/09-01-SUMMARY.md`
</output>
