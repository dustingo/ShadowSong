---
phase: 08-ship-permission-aware-ui-and-verification
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/src/App.test.tsx
  - frontend/src/pages/Alerts.test.tsx
  - frontend/src/pages/DataSources.test.tsx
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/08-ship-permission-aware-ui-and-verification/08-VERIFICATION.md
autonomous: true
requirements: [VER-01, VER-02, VER-03, VER-04, FEACL-01, FEACL-02, FEACL-03]
must_haves:
  truths:
    - "项目至少有一组可重复运行的前端权限矩阵验证，而不是只靠人工截图。"
    - "Phase 7 的后端矩阵与审计证据会在 Phase 8 验证文档里被明确引用，满足跨阶段验证要求。"
    - "需求追踪、路线图和状态文件会同步反映 Phase 8 规划完成。"
  artifacts:
    - path: "frontend/src/App.test.tsx"
      provides: "shell/menu/route visibility matrix tests by role"
    - path: "frontend/src/pages/Alerts.test.tsx"
      provides: "alert action visibility tests"
    - path: "frontend/src/pages/DataSources.test.tsx"
      provides: "config read-only vs admin write-surface tests"
    - path: ".planning/phases/08-ship-permission-aware-ui-and-verification/08-VERIFICATION.md"
      provides: "phase-owned verification evidence linking backend and frontend coverage"
  key_links:
    - from: "frontend/src/App.test.tsx"
      to: "frontend/src/App.tsx"
      via: "menu and route behavior under admin/operator/viewer/forced-reset states"
      pattern: "/users|/profile|force_password_reset"
    - from: ".planning/phases/08-ship-permission-aware-ui-and-verification/08-VERIFICATION.md"
      to: ".planning/phases/07-lock-down-protected-operations/07-VERIFICATION.md"
      via: "backend matrix and audit evidence reuse"
      pattern: "PERM-01|AUDIT-01"
---

<objective>
给权限体系补齐 Phase 8 的最后一公里证据：可运行的前端角色矩阵测试、跨阶段验证文档，以及同步更新的追踪文档。

Purpose: 需求里明确要求前后端验证与文档说明，如果没有自动化前端测试和最终 verification artifact，Phase 8 就只是 UI 改动而不是可审计交付。
Output: frontend permission tests、phase verification report、requirements/roadmap/state 同步。
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a minimal frontend test runner and role-matrix permission tests</name>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/src/App.test.tsx, frontend/src/pages/Alerts.test.tsx, frontend/src/pages/DataSources.test.tsx</files>
  <read_first>frontend/package.json, frontend/vite.config.ts, frontend/src/App.tsx, frontend/src/pages/Alerts.tsx, frontend/src/pages/DataSources.tsx, frontend/src/authz/capabilities.ts, .planning/phases/08-ship-permission-aware-ui-and-verification/08-RESEARCH.md</read_first>
  <action>Add a minimal frontend test setup compatible with the existing Vite app, using concrete commands such as `vitest` and React Testing Library in `frontend/package.json`, plus any required `vite.config.ts` test block. Write `frontend/src/App.test.tsx` to cover shell/menu visibility for `admin`, `operator`, `viewer`, and forced-reset users. Write `frontend/src/pages/Alerts.test.tsx` to prove `viewer` cannot see `确认` and `静默`, while processing roles can. Write `frontend/src/pages/DataSources.test.tsx` to prove non-admin roles get read-only config UI and `admin` keeps write controls. Keep the tests deterministic by mocking store state and router state rather than depending on live backend calls.</action>
  <acceptance_criteria>`frontend/package.json` contains a frontend test script such as `test`; `frontend/vite.config.ts` contains test configuration for Vitest; `frontend/src/App.test.tsx`, `frontend/src/pages/Alerts.test.tsx`, and `frontend/src/pages/DataSources.test.tsx` exist and each assert concrete role-specific visibility outcomes; the frontend test command exits 0.</acceptance_criteria>
  <verify>
    <automated>cd frontend && pnpm test -- --run</automated>
    <automated>cd frontend && pnpm build</automated>
  </verify>
  <done>The repo has repeatable frontend permission evidence for the key Phase 8 role matrix.</done>
</task>

<task type="auto">
  <name>Task 2: Write the final Phase 8 verification and sync planning traceability</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/08-ship-permission-aware-ui-and-verification/08-VERIFICATION.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/07-lock-down-protected-operations/07-VERIFICATION.md, .planning/phases/08-ship-permission-aware-ui-and-verification/08-01-PLAN.md, .planning/phases/08-ship-permission-aware-ui-and-verification/08-02-PLAN.md</read_first>
  <action>After implementation and tests pass, update `.planning/REQUIREMENTS.md` traceability so `PERM-*`, `AUDIT-*`, `FEACL-*`, and `VER-*` reflect the completed phases accurately. Update `.planning/ROADMAP.md` and `.planning/STATE.md` so Phase 8 moves from planned to executed when appropriate. Write `.planning/phases/08-ship-permission-aware-ui-and-verification/08-VERIFICATION.md` with exact commands run, explicit evidence lines for admin/operator/viewer frontend behavior, forced-reset routing, backend audit evidence reused from Phase 7, and requirement mapping for `VER-01` through `VER-04` plus `FEACL-01` through `FEACL-03`.</action>
  <acceptance_criteria>`.planning/phases/08-ship-permission-aware-ui-and-verification/08-VERIFICATION.md` exists and contains `VER-01`, `VER-02`, `VER-03`, `VER-04`, `FEACL-01`, `FEACL-02`, and `FEACL-03`; the file records the exact frontend and backend test commands; `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` reflect Phase 8 progress consistently.</acceptance_criteria>
  <verify>
    <automated>rg -n "VER-01|VER-02|VER-03|VER-04|FEACL-01|FEACL-02|FEACL-03|pnpm test|go test" .planning/phases/08-ship-permission-aware-ui-and-verification/08-VERIFICATION.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md</automated>
  </verify>
  <done>Phase 8 ends with durable verification and synchronized project tracking artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implemented frontend gating -> regression coverage | Future UI edits could silently re-open forbidden buttons without automated tests |
| cross-phase evidence -> milestone closure | Backend and frontend verification must be tied together rather than living in separate implicit histories |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | R | missing frontend regression coverage | mitigate | Add explicit role-matrix tests for shell, alerts, and config pages |
| T-08-08 | T | docs drift from implementation | mitigate | Write Phase 8 verification from exact commands and sync traceability files |
</threat_model>

<verification>
- `cd frontend && pnpm test -- --run`
- `cd frontend && pnpm build`
- `go test ./internal/router ./internal/middleware ./internal/authz ./internal/handlers ./internal/models ./internal/database -count=1 -timeout 60s`
- `rg -n "VER-01|FEACL-01|pnpm test|go test" .planning/phases/08-ship-permission-aware-ui-and-verification/08-VERIFICATION.md`
</verification>

<success_criteria>
- Frontend permission behavior is regression-locked.
- Phase 8 explicitly ties frontend evidence to Phase 7 backend enforcement and audit evidence.
- Planning artifacts stay consistent with the executed state of the milestone.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ship-permission-aware-ui-and-verification/08-03-SUMMARY.md`
</output>
