---
milestone: v1.1
audited: 2026-04-15T00:00:00+08:00
status: tech_debt
scores:
  requirements: 23/23
  phases: 4/4
  integration: 4/4
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: milestone
    items:
      - "`.planning/PROJECT.md` 仍停留在 Phase 6 语义，未反映 v1.1 在 Phase 8 完成后的当前状态与更新时间。"
      - "前端验证虽然通过，但 `pnpm test -- --run` 仍有非阻塞 `act(...)` 与 React Router future-flag warning 噪音。"
nyquist:
  enabled: false
  overall: skipped
---

# Milestone v1.1 Audit

**Milestone:** Enterprise Access Control
**Status:** tech_debt
**Audited:** 2026-04-15

## Summary

v1.1 的 Phase 5-8 已全部完成，4 个 phase 的 `VERIFICATION.md` 均为 `passed`，`REQUIREMENTS.md` 中 23/23 个 v1 requirement 已映射并标记为完成。跨 phase 集成检查未发现阻塞性断点，`admin`、`operator`、`viewer`、禁用用户、强制改密用户这 5 条关键端到端流程均已打通。

结论：里程碑已达到归档条件，但仍有少量非阻塞技术债需要在归档时记录。

## Scores

| Area | Score | Notes |
| --- | --- | --- |
| Requirements | 23/23 | 无 orphaned requirements，无 unsatisfied items |
| Phases | 4/4 | Phase 5-8 均有 `VERIFICATION.md` 且状态为 `passed` |
| Integration | 4/4 | Phase 5 → 6 → 7 → 8 权限链路已连通 |
| Flows | 5/5 | admin/operator/viewer/disabled/forced-reset 全部验证通过 |

## Phase Verification Rollup

| Phase | Verification | Requirements | Notes |
| --- | --- | --- | --- |
| Phase 5 | passed | `AUTHZ-01` `AUTHZ-02` `AUTHZ-03` | 角色真源、JWT principal、capability matrix 基线已稳定 |
| Phase 6 | passed | `USER-01` ~ `USER-06` | 用户管理边界、禁用账号、强制改密与会话失效已闭环 |
| Phase 7 | passed | `PERM-01` ~ `PERM-04`, `AUDIT-01` ~ `AUDIT-03` | 配置写权限与告警动作后端权限收口，审计日志已落地 |
| Phase 8 | passed | `FEACL-01` ~ `FEACL-03`, `VER-01` ~ `VER-04` | 前端权限感知 UI、提示与验证链路已完成 |

## 3-Source Requirements Coverage

来源交叉检查：
- `REQUIREMENTS.md` traceability table
- Phase 5-8 `VERIFICATION.md` requirement coverage
- Phase 6-8 `SUMMARY.md` frontmatter `requirements-completed`

结果：
- 所有 23 个 milestone requirement 在 traceability table 中均存在且状态为 `Complete`
- 所有 23 个 requirement 在对应 phase `VERIFICATION.md` 中均被标记为 `satisfied`
- `SUMMARY.md` frontmatter 与 verification 结果一致，未发现 orphaned requirements

## Cross-Phase Integration

### Verified Wiring

- Phase 5 建立的角色常量、capability matrix、JWT principal 与 capability middleware 已被 Phase 6-8 实际消费，而不是停留在基线层。
- Phase 6 的 `disabled_at`、`force_password_reset`、`token_invalid_before` 已连到运行时鉴权、登录/刷新流程以及前端受限模式。
- Phase 7 的后端接口鉴权与审计日志建立在 Phase 5 capability contract 之上，配置写、告警动作和用户管理权限均已收口。
- Phase 8 的前端菜单、路由、按钮和只读提示复用了镜像 capability matrix，与后端授权边界保持一致。

### Broken Flows

无。

## End-to-End Flows

| Flow | Status | Evidence |
| --- | --- | --- |
| `admin` 管理用户与配置 | ✓ VERIFIED | 路由矩阵测试、用户处理器测试、前端 `/users` 页面联动 |
| `operator` 读配置并处理告警 | ✓ VERIFIED | 路由矩阵测试、前端告警操作按钮与只读配置页 |
| `viewer` 只读访问 | ✓ VERIFIED | 后端 `403` 边界、前端 `PermissionNotice` 与只读标记 |
| 禁用用户会话失效 | ✓ VERIFIED | 登录拒绝、旧 token 失效与中间件复检 |
| 强制改密用户受限模式 | ✓ VERIFIED | 后端限制允许路径，前端强制停留 `/profile` |

## Tech Debt

### Milestone-Level

- `.planning/PROJECT.md` 仍写着“最新阶段：Phase 6 已完成”，未同步到 v1.1 完成后的真实状态。
- 前端测试链路已通过，但仍有非阻塞 `act(...)` 与 React Router future-flag warning，影响验证噪音水平。

### Phase-Level Notes

- Phase 5 verification 仍记录一处非阻塞 brownfield warning：`internal/handlers/user.go` 有原始 `"admin"` 字符串比较分支，未完全抽象成 capability/helper。

## Nyquist

`workflow.nyquist_validation = false`，本次里程碑审计跳过 Nyquist 覆盖扫描。

## Recommendation

建议继续执行：

`/gsd-complete-milestone v1.1`

归档时将上述技术债记录到 milestone archive / PROJECT 更新中即可，无需为此重开 gap-closing phase。
