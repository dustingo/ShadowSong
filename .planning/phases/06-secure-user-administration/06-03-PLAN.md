---
phase: 06-secure-user-administration
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - frontend/src/api/auth.ts
  - frontend/src/stores/userStore.ts
  - frontend/src/types/index.ts
  - frontend/src/App.tsx
  - frontend/src/pages/index.ts
  - frontend/src/pages/Users.tsx
  - frontend/src/pages/Profile.tsx
autonomous: true
requirements: [USER-01, USER-02, USER-04, USER-05, USER-06]
must_haves:
  truths:
    - "管理员登录后有最小可用的用户管理入口，可以查看并维护用户及账号状态。"
    - "所有登录用户都有自助资料/密码入口，强制改密时会被收口到该页面。"
    - "前端只增加 Phase 6 所需的用户管理与个人资料面，不扩展到 Phase 8 的全局权限显隐。"
  artifacts:
    - path: "frontend/src/pages/Users.tsx"
      provides: "admin-only 用户列表与编辑入口"
    - path: "frontend/src/pages/Profile.tsx"
      provides: "自助资料与密码修改页面"
    - path: "frontend/src/App.tsx"
      provides: "最小路由/菜单接线与强制改密重定向"
    - path: "frontend/src/api/auth.ts"
      provides: "admin/self-service 用户 API 封装"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/stores/userStore.ts"
      via: "route/menu visibility and force-reset redirect"
      pattern: "force_password_reset|/profile|/users"
    - from: "frontend/src/pages/Users.tsx"
      to: "frontend/src/api/auth.ts"
      via: "admin user CRUD and account-state actions"
      pattern: "listUsers|createUser|updateUser|deleteUser"
---

<objective>
为 Phase 6 暴露最小前端入口：管理员用户管理页、自助资料/密码页，以及强制改密下的受限导航行为。

Purpose: 让后端 Phase 6 边界在 UI 上可用，但不提前吸收 Phase 8 的全局权限壳层工程。
Output: 用户类型扩展、admin 用户页、profile 页、最小路由/菜单接线和构建验证。
</objective>

<execution_context>
@C:/Users/doumaoxin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/doumaoxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-secure-user-administration/06-RESEARCH.md
@.planning/phases/06-secure-user-administration/06-02-PLAN.md
@frontend/src/api/auth.ts
@frontend/src/stores/userStore.ts
@frontend/src/App.tsx
@frontend/src/types/index.ts
@frontend/src/pages/index.ts
@frontend/src/pages/Login.tsx

<interfaces>
From frontend/src/types/index.ts:
```ts
export interface User {
  id: number
  username: string
  name: string
  email?: string
  role: 'admin' | 'operator' | 'viewer'
  created_at: string
  updated_at: string
}
```

From frontend/src/api/auth.ts:
```ts
export const authApi = {
  login(username: string, password: string): Promise<{ token: string; user: User }>
  getCurrentUser(): Promise<User>
  listUsers(): Promise<User[]>
  createUser(data: {...}): Promise<User>
  updateUser(id: number, data: {...}): Promise<User>
  deleteUser(id: number): Promise<void>
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend frontend auth contracts and store state for account-control flags</name>
  <files>frontend/src/api/auth.ts, frontend/src/stores/userStore.ts, frontend/src/types/index.ts</files>
  <read_first>frontend/src/api/auth.ts, frontend/src/stores/userStore.ts, frontend/src/types/index.ts, .planning/phases/06-secure-user-administration/06-02-PLAN.md</read_first>
  <action>Extend the shared `User` type with the minimal optional Phase 6 fields returned by the backend, including forced-reset and disabled/account-state markers if exposed. In `frontend/src/api/auth.ts`, preserve the existing login/logout/current-user contract while adding distinct admin and self-service calls that match the Phase 6 backend routes: admin list/create/update/delete plus self profile update and self password update. Update `useUserStore` so persisted user JSON tolerates old localStorage payloads and can drive a simple `force_password_reset` or equivalent limited-mode flag without crashing older sessions.</action>
  <acceptance_criteria>`frontend/src/types/index.ts` defines optional Phase 6 account-state fields on `User`; `frontend/src/api/auth.ts` exposes separate admin and self-service methods rather than one ambiguous update call; `frontend/src/stores/userStore.ts` can parse stored users with or without the new flags.</acceptance_criteria>
  <verify>
    <automated>cd frontend; pnpm build</automated>
  </verify>
  <done>Frontend contracts can consume the new backend response shape and route split without breaking existing login/session storage.</done>
</task>

<task type="auto">
  <name>Task 2: Build the minimum admin Users page and self-service Profile page</name>
  <files>frontend/src/pages/Users.tsx, frontend/src/pages/Profile.tsx, frontend/src/pages/index.ts</files>
  <read_first>frontend/src/pages/Login.tsx, frontend/src/pages/Channels.tsx, frontend/src/pages/OnDuty.tsx, frontend/src/pages/index.ts, frontend/src/api/auth.ts</read_first>
  <action>Create `frontend/src/pages/Users.tsx` as the smallest Ant Design admin user-management surface needed for Phase 6: list users with role and account-state columns, create-user modal/form, edit-user modal/form, and explicit controls for disable/enable and force-password-reset backed by the new admin APIs. Create `frontend/src/pages/Profile.tsx` as the self-service surface for all authenticated users with a profile form (`name`, `email`) and a separate password form. Re-export both pages via `frontend/src/pages/index.ts`. Do not add broader permission-matrix buttons for other modules; keep this page pair strictly Phase 6 scoped.</action>
  <acceptance_criteria>`frontend/src/pages/Users.tsx` exists and calls only admin user APIs; `frontend/src/pages/Profile.tsx` exists and does not expose role/account-state fields to ordinary users; `frontend/src/pages/index.ts` re-exports both pages.</acceptance_criteria>
  <verify>
    <automated>cd frontend; pnpm build</automated>
  </verify>
  <done>Phase 6 has concrete UI surfaces for both admin-managed user workflows and self-service profile/password maintenance.</done>
</task>

<task type="auto">
  <name>Task 3: Wire minimal routes, menu entrypoints, and forced-reset-only navigation</name>
  <files>frontend/src/App.tsx, frontend/src/stores/userStore.ts, frontend/src/api/auth.ts</files>
  <read_first>frontend/src/App.tsx, frontend/src/stores/userStore.ts, frontend/src/api/auth.ts, frontend/src/pages/index.ts, .planning/phases/06-secure-user-administration/06-RESEARCH.md</read_first>
  <action>Update `frontend/src/App.tsx` so Phase 6 adds only two new entrypoints: an admin-only `/users` route/menu item and a `/profile` route available to authenticated users. Keep the existing authenticated shell intact. Add minimal route guarding based on the stored user role for `/users`, but avoid broader Phase 8 menu pruning. Implement forced-reset limited mode in the shell: when the current user carries `force_password_reset`, redirect any route other than `/profile` (and optionally `/login`) to `/profile`, keep logout available, and present a concise prompt that password update is required before returning to normal use. Reuse the existing 401 interceptor behavior for disabled/stale-session logout rather than inventing a second session store.</action>
  <acceptance_criteria>`frontend/src/App.tsx` contains `/users` and `/profile` routes, shows the users entry only for `admin`, and redirects forced-reset users away from normal routes to `/profile`; no unrelated module menus or pages are reworked.</acceptance_criteria>
  <verify>
    <automated>cd frontend; pnpm build</automated>
  </verify>
  <done>The frontend exposes exactly the Phase 6 user-administration and self-service flow, including limited-mode navigation for forced-reset accounts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend user state→frontend local store | Server-provided role/reset flags drive route decisions and must not crash older persisted sessions |
| authenticated UI→admin user page | Frontend can improve UX but must not become the only enforcement boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | E | `frontend/src/App.tsx` admin route gating | mitigate | Hide and block `/users` client-side for non-admins while relying on backend capability checks as final authority |
| T-06-10 | D | `frontend/src/stores/userStore.ts` old localStorage payloads | mitigate | Treat new Phase 6 flags as optional and parse old persisted user JSON safely |
| T-06-11 | E | forced-reset navigation | mitigate | Redirect reset-required users to `/profile` until password change clears the server flag |
| T-06-12 | T | admin page action wiring | accept | Backend Phase 6 routes remain the source of truth; client-side forms only mirror allowed actions |
</threat_model>

<verification>
- `cd frontend && pnpm build`
- Grep check after implementation: `rg -n "/users|/profile|force_password_reset|listUsers|updateOwnPassword" frontend/src/App.tsx frontend/src/api/auth.ts frontend/src/pages`
- Route-gating evidence: `rg -n "role === 'admin'|/users|/profile|force_password_reset|Navigate|location.pathname" frontend/src/App.tsx frontend/src/stores/userStore.ts`
- Manual/UAT evidence to capture in `06-03-SUMMARY.md`:
  - non-admin authenticated user is blocked from `/users` and cannot see the user-management menu entry
  - admin authenticated user can see and enter `/users`
  - any authenticated user can enter `/profile`
  - forced-reset user visiting a non-`/profile` route is redirected to `/profile` and still retains logout access
</verification>

<success_criteria>
- Admins can reach a user-management entrypoint without opening Phase 8-wide shell work.
- All users can maintain profile/password via a dedicated page.
- Forced-reset users are funneled into the password-reset surface until the backend clears the flag.
</success_criteria>

<output>
After completion, create `.planning/phases/06-secure-user-administration/06-03-SUMMARY.md`
</output>
