---
phase: 07-lock-down-protected-operations
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - internal/models/models.go
  - internal/models/alert.go
  - internal/database/postgres.go
  - internal/handlers/alert.go
  - internal/handlers/config.go
  - internal/handlers/user.go
  - internal/handlers/user_test.go
autonomous: true
requirements: [AUDIT-01, AUDIT-02, AUDIT-03, PERM-04]
must_haves:
  truths:
    - "关键用户、权限、配置和告警动作在后端被记录为审计日志。"
    - "审计日志至少带有 actor、target、action、result、timestamp。"
    - "handler 可以用统一 seam 写审计，而不是到处散落手写插库。"
  artifacts:
    - path: "internal/models/models.go"
      provides: "persistent audit log model"
    - path: "internal/handlers/config.go"
      provides: "backend-authored audit entries for critical config operations"
    - path: "internal/handlers/alert.go"
      provides: "real actor-based audit entries for ack and quick-silence"
  key_links:
    - from: "internal/handlers/alert.go"
      to: "internal/middleware/auth.go"
      via: "principal-derived actor identity"
      pattern: "GetPrincipal|GetUsername|GetUserID"
    - from: "internal/handlers/config.go"
      to: "internal/models/models.go"
      via: "audit log persistence"
      pattern: "AuditLog"
---

<objective>
为 Phase 7 引入一个足够小但可持久化的后端审计 seam，并将关键账号/配置/告警动作接入它。

Purpose: 让关键安全动作既被后端强制控制，也能在事后追踪“谁对什么做了什么，结果如何”。
Output: 审计模型、统一记录 helper、关键 handler 审计接线。
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a persistent audit-log model and migration-safe recorder seam</name>
  <files>internal/models/models.go, internal/database/postgres.go</files>
  <read_first>internal/models/models.go, internal/database/postgres.go, internal/models/user.go, .planning/phases/07-lock-down-protected-operations/07-RESEARCH.md</read_first>
  <action>Add an `AuditLog` GORM model to `internal/models/models.go` with concrete persisted fields: `actor_user_id`, `actor_username`, `actor_role`, `action`, `target_type`, `target_id`, `result`, `detail`, `created_at`. Ensure `internal/database/postgres.go` includes the new model in `AutoMigrate`. Keep the phase scope pragmatic: one table and one simple helper/constructor pattern are enough; do not introduce queues, external sinks, or schema complexity beyond what Phase 7 needs.</action>
  <acceptance_criteria>`internal/models/models.go` defines `type AuditLog struct`; `internal/database/postgres.go` migrates `models.AuditLog`; field names for actor/target/action/result/timestamp are grep-visible in the model definition.</acceptance_criteria>
  <verify>
    <automated>go test ./internal/database ./internal/models -count=1 -timeout 60s</automated>
  </verify>
  <done>The codebase has a persistent backend-owned audit sink ready for handler integration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Record critical alert, config, and user-security actions through one backend audit helper</name>
  <files>internal/handlers/alert.go, internal/handlers/config.go, internal/handlers/user.go, internal/handlers/user_test.go</files>
  <read_first>internal/handlers/alert.go, internal/handlers/config.go, internal/handlers/user.go, internal/handlers/user_test.go, internal/middleware/auth.go, internal/models/models.go</read_first>
  <action>Create one reusable audit-recording seam inside the handlers layer or a close backend helper used by handlers. Use it to log successful and denied critical operations with explicit values: actor identity from middleware principal when present, target object type/id, action names such as `alert.ack`, `alert.quick_silence`, `config.datasource.create`, `config.channel.update`, `user.disable`, `user.force_password_reset`, and result values such as `allowed` or `denied`. Replace hard-coded actor fallbacks like `CreatedBy: "system"` in user-triggered quick-silence flows with the actual acting username where available. Extend `internal/handlers/user_test.go` or adjacent handler tests to verify that audit rows are persisted with the expected actor/action/result fields on representative success and denial paths.</action>
  <acceptance_criteria>`internal/handlers/alert.go` no longer hard-codes `"system"` for user-triggered quick silence actor attribution; `internal/handlers/config.go` or a shared helper writes `AuditLog` rows for representative create/update/delete actions; `internal/handlers/user.go` records critical security mutations; handler tests assert persisted audit rows with concrete `action`, `result`, and actor fields.</acceptance_criteria>
  <verify>
    <automated>go test ./internal/handlers ./internal/models ./internal/database -count=1 -timeout 60s</automated>
  </verify>
  <done>Critical backend actions now leave durable audit evidence authored by the server itself.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handler allow/deny decision -> audit persistence | Audit must reflect what the backend actually decided, not client claims |
| middleware principal -> audit actor fields | Actor identity must come from validated request context where available |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | R | missing audit trail | mitigate | Persist one backend-owned audit record for critical allow/deny events |
| T-07-05 | T | fake actor identity | mitigate | Use middleware principal fields instead of frontend-supplied actor data |
| T-07-06 | I | silent security changes | mitigate | Record role/disable/force-reset/config mutations with explicit action/result metadata |
</threat_model>

<verification>
- `go test ./internal/handlers ./internal/models ./internal/database -count=1 -timeout 60s`
- `rg -n "type AuditLog struct|alert\\.ack|config\\.|user\\.(disable|force_password_reset)" internal`
</verification>

<success_criteria>
- Critical operations are audited by the backend.
- Audit rows carry enough actor/target/result context for incident review.
- The audit seam is reusable by later phases instead of being handler-specific duplication.
</success_criteria>

<output>
After completion, create `.planning/phases/07-lock-down-protected-operations/07-02-SUMMARY.md`
</output>
