---
phase: 18-establish-delivery-ledger
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - internal/handlers/webhook.go
  - internal/handlers/webhook_test.go
autonomous: true
requirements:
  - DELV-02
  - DELV-06
must_haves:
  truths:
    - "现有 bounded 3 次发送重试仍然保留，但每次真实 attempt 都会落到账本。"
    - "成功、非重试失败和重试耗尽三类结果都会更新 delivery 终态，不再只停留在日志里。"
    - "账本写入与现有 `trace_id` / `send_attempt` / `terminal_failure` 契约保持一致，维护者可同时用日志和数据库交叉定位。"
  artifacts:
    - path: "internal/handlers/webhook.go"
      provides: "webhook 发送热路径接入 delivery service"
    - path: "internal/handlers/webhook_test.go"
      provides: "账本双写与 retry boundary 回归测试"
  key_links:
    - from: "internal/handlers/webhook.go"
      to: "internal/delivery/service.go"
      via: "StartDelivery / RecordAttempt / MarkDelivered / MarkFailed"
      pattern: "deliveryService"
    - from: "internal/handlers/webhook.go"
      to: "docs/alert-path-operations-runbook.md"
      via: "trace_id + stage contract preservation"
      pattern: "send_attempt|terminal_failure"
---

<objective>
把账本接到现有通知热路径上，但不改 bounded retry 语义、不引入新 worker，也不破坏 Phase 14-17 已验证的日志与 trace 契约。

Purpose: 落实 D-03、D-04、D-06 在发送路径上的真实行为，让 DELV-02 不再停留在 schema 层。  
Output: webhook 主链路双写账本、成功/失败终态持久化、覆盖终态场景的回归测试。
</objective>

<execution_context>
@C:/Users/doumaoxin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/doumaoxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-establish-delivery-ledger/18-CONTEXT.md
@.planning/phases/18-establish-delivery-ledger/18-RESEARCH.md
@docs/alert-path-operations-runbook.md
@internal/handlers/webhook.go
@internal/handlers/webhook_test.go
@internal/delivery/service.go

<interfaces>
From `internal/handlers/webhook.go`:

```go
const notificationMaxAttempts = 3

func (h *WebhookHandler) sendChannelNotification(
  alert *models.Alert,
  channel *models.Channel,
  title string,
  content string,
  mode string,
)
```

Current runtime guarantees from `docs/alert-path-operations-runbook.md`:

```text
- Send-stage retries are bounded to 3 attempts.
- Exhausted retries end at stage=terminal_failure.
- Failure-path logs keep trace_id, alert_id, channel_id, attempt, max_attempts.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 把 delivery service 接入 webhook 通知热路径</name>
  <files>internal/handlers/webhook.go</files>
  <read_first>internal/handlers/webhook.go, internal/delivery/service.go, docs/alert-path-operations-runbook.md, .planning/phases/18-establish-delivery-ledger/18-CONTEXT.md</read_first>
  <acceptance_criteria>
    - `rg -n "deliveryService|StartDelivery|RecordAttempt|MarkDelivered|MarkFailed" internal/handlers/webhook.go` 命中热路径接入代码。
    - `rg -n "notificationMaxAttempts = 3|stage=terminal_failure|stage=send_attempt" docs/alert-path-operations-runbook.md internal/handlers/webhook.go` 显示 bounded retry 契约仍保留。
    - `go test ./internal/handlers -run "TestWebhookHandler(.*Delivery.*|.*Retry.*|.*Terminal.*)" -count=1` 通过。
  </acceptance_criteria>
  <action>在 `internal/handlers/webhook.go` 中为 `WebhookHandler` 增加 `deliveryService` 依赖，并在 `NewWebhookHandler` 中初始化为 Plan 01 的 `delivery.Service`。把账本接入点放在 `processAlertNotifications` / `sendNotification` / `sendChannelNotification` 链路上：每个 `alert x channel` 进入发送前调用 `StartDelivery`，快照必须带上 `trace_id`、`alert_id`、`channel_id`、可用时的 `route_rule_id`、`delivery_mode`、最终 title/content；每次真实发送 attempt 后都调用 `RecordAttempt`；成功时调用 `MarkDelivered`；非重试失败时立即 `MarkFailed(retryable=false)`；重试耗尽时在现有 `terminal_failure` 分支调用 `MarkFailed(retryable=true, terminal=true)`。保留 `notificationMaxAttempts = 3`、现有 `notifier.IsRetryableSendError` 判定、`send_attempt`/`send_notification`/`terminal_failure` 日志名字与字段，不引入 MQ、worker、后台重放器，也不要让账本写入替代现有日志。</action>
  <verify>
    <automated>go test ./internal/handlers -run "TestWebhookHandler(.*Delivery.*|.*Retry.*|.*Terminal.*)" -count=1</automated>
  </verify>
  <done>发送热路径已经为每次真实投递写入 delivery + attempt 真源，且现有 bounded retry 与日志契约未被改写。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩充 webhook 回归测试覆盖成功、fallback 和终态失败账本</name>
  <files>internal/handlers/webhook_test.go</files>
  <read_first>internal/handlers/webhook_test.go, internal/handlers/webhook.go, docs/alert-path-operations-runbook.md, internal/delivery/service_test.go</read_first>
  <acceptance_criteria>
    - `rg -n "Delivery" internal/handlers/webhook_test.go` 命中新增长账本断言的测试名。
    - `go test ./internal/handlers -run "TestWebhookHandler(.*Delivery.*|.*Fallback.*|.*Terminal.*|.*Retry.*)" -count=1` 通过。
    - 新测试对 `notification_deliveries` 和 `notification_delivery_attempts` 至少断言 `attempt_count`、`delivery_status`、`delivery_mode`、`final_failure_summary`、`trace_id`、attempt 数量。
  </acceptance_criteria>
  <action>在 `internal/handlers/webhook_test.go` 新增或重构测试，用 sqlite + GORM 迁移包含 `NotificationDelivery` 与 `NotificationDeliveryAttempt`，覆盖三类最小回归：1) 首次成功发送后，账本存在 1 条 delivery、1 条 success attempt，`delivery_status=delivered`；2) datasource lookup/render 失败回落到 `default` 发送模式时，账本里的 `delivery_mode=default` 且 `rendered_payload_snapshot` 保存最终实际发送内容；3) retryable 错误耗尽 3 次后，delivery 终态是失败，attempt 明细为 3 条，`final_failure_summary` 与最后一个 `terminal_failure` 分支一致。保持现有 Phase 15/16 日志断言继续存在，不能把回归焦点从日志契约移走。</action>
  <verify>
    <automated>go test ./internal/handlers -run "TestWebhookHandler(.*Delivery.*|.*Fallback.*|.*Terminal.*|.*Retry.*)" -count=1</automated>
  </verify>
  <done>自动化测试能证明 webhook 发送链路在成功、默认模板回退、非重试失败和重试耗尽时都同步写入正确账本，而不是只输出日志。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime notification flow → PostgreSQL ledger | 每次发送 attempt 的运行时结果被持久化，若失败被吞掉则审计真源失真 |
| retry boundary logic → terminal failure persistence | 最后一次失败分支决定是否留下 DELV-02 要求的最终证据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | R | `internal/handlers/webhook.go` terminal failure path | mitigate (blocking) | 在 `attempt == notificationMaxAttempts` 的现有 `terminal_failure` 分支同步调用 `MarkFailed`，并用回归测试断言 DB 中存在失败终态。 |
| T-18-05 | D | `internal/handlers/webhook.go` send path writes | mitigate | 账本写入只做轻量同步持久化，不改重试上限和日志顺序；失败测试覆盖成功、fallback、retry exhausted 三类高频路径。 |
| T-18-06 | T | `internal/handlers/webhook.go` snapshot creation | mitigate | 快照来自发送前已确定的 title/content 与 route/channel identity，避免失败后再读取当前配置造成语义漂移。 |
</threat_model>

<verification>
整体执行后运行：

```bash
go test ./internal/handlers -run "TestWebhookHandler(.*Delivery.*|.*Fallback.*|.*Retry.*|.*Terminal.*|.*Logging.*)" -count=1
```

并确认 `send_attempt` / `terminal_failure` 现有日志测试仍为绿色。
</verification>

<success_criteria>
- 所有真实发送 attempt 都有对应账本记录。
- 超过当前即时重试上限的通知会留下失败终态记录，而不是只落日志。
- 热路径仍保留 Phase 15/16 已验证的三次重试与 canonical logging 契约。
</success_criteria>

<output>
After completion, create `.planning/phases/18-establish-delivery-ledger/18-02-SUMMARY.md`
</output>
