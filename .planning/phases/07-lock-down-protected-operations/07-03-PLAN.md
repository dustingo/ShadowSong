---
phase: 07-lock-down-protected-operations
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - internal/router/router_test.go
  - internal/middleware/authorize_test.go
  - internal/authz/capabilities_test.go
  - internal/handlers/user_test.go
  - .planning/phases/07-lock-down-protected-operations/07-VERIFICATION.md
autonomous: true
requirements: [PERM-01, PERM-02, PERM-03, PERM-04, AUDIT-01, AUDIT-02, AUDIT-03]
must_haves:
  truths:
    - "后端存在 admin/operator/viewer 对关键接口的允许/拒绝矩阵回归覆盖。"
    - "401 与 403 的边界是可验证且一致的。"
    - "Phase 7 的审计与权限收口有一份明确的验证证据。"
  artifacts:
    - path: "internal/router/router_test.go"
      provides: "protected-operation role matrix"
    - path: ".planning/phases/07-lock-down-protected-operations/07-VERIFICATION.md"
      provides: "phase-level verification evidence"
  key_links:
    - from: "internal/router/router_test.go"
      to: "internal/middleware/authorize.go"
      via: "403 insufficient permissions assertions"
      pattern: "insufficient permissions"
---

<objective>
把 Phase 7 的权限与审计结果锁进回归矩阵，并产出后续 Phase 8 可消费的验证证据。

Purpose: 防止“这轮加了 capability，下一轮又被新接口绕开”，同时给权限体系留下可审查的证明材料。
Output: 更完整的后端角色矩阵测试、拒绝语义验证、Phase 7 验证报告。
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand backend role-matrix coverage for representative protected operations</name>
  <files>internal/router/router_test.go, internal/middleware/authorize_test.go, internal/authz/capabilities_test.go</files>
  <read_first>internal/router/router_test.go, internal/middleware/authorize_test.go, internal/authz/capabilities_test.go, internal/router/router.go, internal/authz/capabilities.go, .planning/REQUIREMENTS.md</read_first>
  <action>Extend the existing backend authorization tests so representative protected operations are covered for all three roles. Include at least one alert action endpoint, one config write endpoint, one config read endpoint, and one user-management endpoint. Assert exact outcomes: unauthenticated requests return `401`, authenticated-but-disallowed roles return `403 {"error":"insufficient permissions"}`, and allowed roles reach the current handler result. Keep the capability matrix tests aligned with the router assertions so policy and route wiring cannot silently drift apart.</action>
  <acceptance_criteria>`internal/router/router_test.go` contains representative `admin` / `operator` / `viewer` matrix cases across alert actions, config reads/writes, and user-management; `internal/middleware/authorize_test.go` still asserts `403` for missing capability; `internal/authz/capabilities_test.go` reflects the intended matrix with `operator` able to process alerts but not manage config.</acceptance_criteria>
  <verify>
    <automated>go test ./internal/router ./internal/middleware ./internal/authz -count=1 -timeout 60s</automated>
  </verify>
  <done>The backend permission matrix is regression-locked across the most important protected surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Phase 7 verification evidence from executed checks and observed matrices</name>
  <files>.planning/phases/07-lock-down-protected-operations/07-VERIFICATION.md</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/07-lock-down-protected-operations/07-01-PLAN.md, .planning/phases/07-lock-down-protected-operations/07-02-PLAN.md, internal/router/router_test.go</read_first>
  <action>Create `.planning/phases/07-lock-down-protected-operations/07-VERIFICATION.md` after implementation. Summarize the phase goal, the exact automated commands executed, the protected-operation matrix that was verified, and which Phase 7 requirements were satisfied. Include explicit evidence lines for admin-only config writes, operator alert processing, viewer read-only behavior, and backend-authored audit persistence.</action>
  <acceptance_criteria>`07-VERIFICATION.md` exists; it names `PERM-01` through `PERM-04` and `AUDIT-01` through `AUDIT-03`; it records the exact `go test` command(s) run; it explicitly states admin-only config writes, operator alert actions, viewer denial, and audit persistence evidence.</acceptance_criteria>
  <verify>
    <automated>rg -n "PERM-01|PERM-02|PERM-03|PERM-04|AUDIT-01|AUDIT-02|AUDIT-03|go test" .planning/phases/07-lock-down-protected-operations/07-VERIFICATION.md</automated>
  </verify>
  <done>Phase 7 finishes with a durable verification artifact instead of relying only on commit history.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| intended policy -> future code edits | Regression tests must prevent accidental re-opening of protected endpoints |
| executed checks -> milestone evidence | Verification docs must reflect actual commands and matrices, not vague summaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | R | missing regression coverage | mitigate | Lock representative matrices into router/capability tests |
| T-07-08 | T | permission drift between matrix and routes | mitigate | Cross-check capability tests and router tests together |
</threat_model>

<verification>
- `go test ./internal/router ./internal/middleware ./internal/authz ./internal/handlers ./internal/models ./internal/database -count=1 -timeout 60s`
- `rg -n "PERM-01|AUDIT-01|go test" .planning/phases/07-lock-down-protected-operations/07-VERIFICATION.md`
</verification>

<success_criteria>
- Phase 7 ends with role-matrix regression coverage, not just implementation.
- The verification artifact makes later Phase 8 work easier instead of rediscovering evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/07-lock-down-protected-operations/07-03-SUMMARY.md`
</output>
