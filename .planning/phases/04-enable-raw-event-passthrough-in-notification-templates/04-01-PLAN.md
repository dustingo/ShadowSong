---
phase: 04-enable-raw-event-passthrough-in-notification-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/handlers/webhook.go
  - internal/handlers/webhook_test.go
autonomous: true
requirements:
  - TMPL-01
  - TMPL-03
must_haves:
  truths:
    - User-authored output templates can keep using existing standard fields such as `{{.alert_name}}` and `{{.severity}}` without edits.
    - User-authored output templates can reference raw webhook JSON through one stable top-level variable instead of guessing backend internals.
    - Notification rendering falls back predictably when raw payload decoding fails or fields are missing.
  artifacts:
    - path: internal/handlers/webhook.go
      provides: Output template context builder that exposes standard fields plus raw event data.
    - path: internal/handlers/webhook_test.go
      provides: Regression coverage for legacy templates and nested raw-field rendering.
  key_links:
    - from: internal/handlers/webhook.go
      to: internal/models/alert.go
      via: alert.Raw and alert.Labels decoding during output rendering
      pattern: "json\\.Unmarshal\\(alert\\.Raw|json\\.Unmarshal\\(alert\\.Labels"
    - from: internal/handlers/webhook.go
      to: datasource output templates
      via: template execution with expanded render context
      pattern: "template\\.New\\(\"output\"\\)"
---

<objective>
Lock the backend contract for notification template rendering so output templates can access both the normalized alert fields and the original webhook payload.

Purpose: satisfy raw-event passthrough without breaking existing templates that only use standard alert fields.
Output: a stable render-context contract in `webhook.go` plus regression tests for compatibility and nested raw-field access.
</objective>

<execution_context>
@C:/Users/doumaoxin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/doumaoxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-enable-raw-event-passthrough-in-notification-templates/04-CONTEXT.md
@internal/handlers/webhook.go
@internal/models/alert.go
@internal/models/models.go

<interfaces>
From `internal/models/alert.go`:

```go
type Alert struct {
	AlertID   string         `json:"alert_id"`
	Source    string         `json:"source"`
	AlertName string         `json:"alert_name"`
	Severity  string         `json:"severity"`
	Message   string         `json:"message"`
	Labels    datatypes.JSON `json:"labels"`
	Status    string         `json:"status"`
	Raw       datatypes.JSON `json:"raw"`
}
```

From `internal/handlers/webhook.go`:

```go
func (h *WebhookHandler) renderNotification(alert *models.Alert, tmplStr string) (string, string, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand the output-template render context contract</name>
  <read_first>
    - internal/handlers/webhook.go
    - internal/models/alert.go
    - .planning/phases/04-enable-raw-event-passthrough-in-notification-templates/04-CONTEXT.md
  </read_first>
  <files>internal/handlers/webhook.go</files>
  <behavior>
    - Legacy template case: `{"title":"[{{.severity}}] {{.alert_name}}","content":"{{.message}}"}` still renders without requiring `alert.*` rewrites.
    - Raw passthrough case: nested payload data such as `{{index .event "annotations"}}` or dot-safe access against decoded maps becomes available through one stable top-level variable per D-locked decision.
    - Decode-failure case: malformed or empty `alert.Raw` does not break standard-field rendering; raw context degrades to empty/nil rather than panicking.
  </behavior>
  <action>Refactor `renderNotification` in `internal/handlers/webhook.go` so it builds a formal render context for `output_template` per the locked decisions in `04-CONTEXT.md`: keep the current top-level standard keys (`alert_name`, `severity`, `message`, `source`, `status`, `trigger_time`, `labels`) for backward compatibility per D-compatibility/TMPL-03, and add a stable top-level raw-event object such as `event` that is decoded from `alert.Raw` for TMPL-01. Also expose a nested normalized object (for example `alert`) only if it helps readability, but do not force users to switch away from the existing top-level fields. Reuse helper funcs already familiar to templates (`toJson`, `default`) and add only narrowly scoped helpers needed for nested map safety or readability; avoid a broader DSL redesign. Update default output templates only if they can demonstrate the new contract without breaking old templates.</action>
  <verify>
    <automated>go test ./internal/handlers -run TestWebhook -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `renderNotification` accepts legacy templates unchanged.
    - `renderNotification` exposes a documented raw-event variable for arbitrary JSON payload fields.
    - Notification rendering handles missing raw data without returning malformed output for standard-field-only templates.
  </acceptance_criteria>
  <done>The backend has one explicit output-template contract that preserves old standard-field templates and adds raw event passthrough for new templates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add focused regression tests for compatibility and nested raw access</name>
  <read_first>
    - internal/handlers/webhook_test.go
    - internal/handlers/webhook.go
    - .planning/codebase/TESTING.md
  </read_first>
  <files>internal/handlers/webhook_test.go</files>
  <behavior>
    - Test 1: a standard-field-only output template still renders the expected title/content.
    - Test 2: a template can read nested raw payload data from the new stable raw-event variable and include it in rendered content.
    - Test 3: missing raw payload keys do not crash rendering and can fall back through helper/default behavior.
  </behavior>
  <action>Create or extend `internal/handlers/webhook_test.go` with table-driven tests around `renderNotification` or the extracted render-context helper. Cover TMPL-03 backward compatibility explicitly, then cover TMPL-01 nested raw JSON access using realistic webhook payloads like `annotations.runbook`, `summary`, or custom top-level fields from the phase context. Keep tests deterministic and avoid DB/network setup; this should stay a pure handler-level regression suite.</action>
  <verify>
    <automated>go test ./internal/handlers -run TestWebhook -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail if legacy top-level fields disappear from the output-template context.
    - Tests fail if nested raw payload fields are not reachable from the stable passthrough variable.
    - Tests fail if nil or missing raw payload data causes template execution errors in compatibility scenarios.
  </acceptance_criteria>
  <done>`internal/handlers/webhook_test.go` proves the new contract and guards the compatibility boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external webhook payload -> output template context | Untrusted JSON becomes template data and must not crash or corrupt rendering |
| persisted alert.Raw -> notifier payload | Stored raw event data is decoded and forwarded into outbound channel content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | `internal/handlers/webhook.go` | mitigate | Decode `alert.Raw` and `alert.Labels` defensively, preserve empty maps on failure, and keep template helper surface narrow |
| T-04-02 | D | `internal/handlers/webhook.go` | mitigate | Cover malformed/missing raw payload cases in handler tests so bad payloads cannot panic notification rendering |
| T-04-03 | I | outbound notification content | accept | Raw passthrough is an explicit user-authored template feature; exposure is controlled by template authors, not silently auto-included |
</threat_model>

<verification>
Run `go test ./internal/handlers -run TestWebhook -count=1` after the context refactor to confirm backward compatibility and raw passthrough rendering.
</verification>

<success_criteria>
- Output templates support both legacy standard fields and a stable raw-event variable.
- Nested raw JSON access is covered by automated tests.
- No change in this plan requires frontend or route updates to validate the core backend contract.
</success_criteria>

<output>
After completion, create `.planning/phases/04-enable-raw-event-passthrough-in-notification-templates/04-01-SUMMARY.md`
</output>
