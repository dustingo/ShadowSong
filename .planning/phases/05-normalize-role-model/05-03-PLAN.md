---
phase: 05-normalize-role-model
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - internal/authz/capabilities.go
  - internal/authz/capabilities_test.go
  - internal/middleware/authorize.go
  - internal/router/router.go
  - internal/router/router_test.go
autonomous: true
requirements:
  - AUTHZ-03
must_haves:
  truths:
    - The backend can answer whether a principal may view alerts, process alerts, manage configuration, or manage users based on role.
    - Route and handler code can depend on reusable capability helpers instead of ad hoc role string comparisons.
    - Later phases can add account disable, forced password reset, and capability-based guards without redesigning the Phase 5 authorization baseline.
  artifacts:
    - path: internal/authz/capabilities.go
      provides: Capability matrix mapping `admin`, `operator`, and `viewer` to backend permissions.
    - path: internal/authz/capabilities_test.go
      provides: Matrix regression coverage for allow/deny behavior across all supported roles.
    - path: internal/middleware/authorize.go
      provides: Reusable capability-based Gin middleware adapters built on the principal abstraction.
    - path: internal/router/router.go
      provides: Baseline adoption of capability middleware where Phase 5 already has admin-only endpoints.
    - path: internal/router/router_test.go
      provides: Router-level regression coverage for capability-wired authz guards.
  key_links:
    - from: internal/middleware/authorize.go
      to: internal/authz/capabilities.go
      via: capability predicates over the current request principal
      pattern: "Can[A-Z]"
    - from: internal/router/router.go
      to: internal/middleware/authorize.go
      via: protected route groups and endpoint-level guards
      pattern: "RequireCapability|RequireRole"
---

<objective>
Introduce the reusable backend authorization baseline for Phase 6 and Phase 7 by defining a role-to-capability matrix and wiring it into middleware-friendly adapters.

Purpose: satisfy AUTHZ-03 with reusable policy code now, without prematurely implementing account-disable or forced-password-reset behavior from later phases.
Output: capability matrix helpers, capability middleware, and tests for role-based allow/deny behavior.
</objective>

<execution_context>
@C:/Users/doumaoxin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/doumaoxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-normalize-role-model/05-RESEARCH.md
@internal/router/router.go
@internal/middleware/auth.go
@internal/handlers/user.go

<interfaces>
From `internal/middleware/auth.go`:

```go
func GetUserID(c *gin.Context) uint
func GetUsername(c *gin.Context) string
func GetUserRole(c *gin.Context) string
```

Expected downstream capability surface created in this plan:

```go
type Capability string

const (
	CapabilityViewAlerts    Capability = "view_alerts"
	CapabilityProcessAlerts Capability = "process_alerts"
	CapabilityManageConfig  Capability = "manage_config"
	CapabilityManageUsers   Capability = "manage_users"
)

func Can(role string, capability Capability) bool
func RequireCapability(capability Capability) gin.HandlerFunc
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and test the Phase 5 capability matrix</name>
  <read_first>
    - internal/authz/roles.go
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/05-normalize-role-model/05-RESEARCH.md
  </read_first>
  <files>internal/authz/capabilities.go, internal/authz/capabilities_test.go</files>
  <behavior>
    - Test 1: `admin` can view alerts, process alerts, manage config, and manage users.
    - Test 2: `operator` can view alerts and process alerts but cannot manage config or manage users.
    - Test 3: `viewer` can only view alerts/config-facing read operations and is denied processing and management capabilities.
  </behavior>
  <action>Create `internal/authz/capabilities.go` with a small explicit capability vocabulary for the actions Phase 5 must normalize now: viewing alerts/config data, processing alerts, managing config, and managing users. Implement the matrix in code, not comments, and keep it intentionally small and backend-oriented. Add extension seams for later Phase 6 and 7 work by keeping capabilities typed and centralized, but do not implement account disable, forced password reset, or audit logging here. Add table-driven tests in `internal/authz/capabilities_test.go` that lock the full allow/deny matrix for all three supported roles and fail on unsupported roles.</action>
  <verify>
    <automated>go test ./internal/authz -run "TestCapability" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The capability matrix encodes allow/deny behavior for all three supported roles.
    - Unsupported roles evaluate to deny-by-default.
    - Tests enumerate `admin`, `operator`, and `viewer` across the targeted capabilities from AUTHZ-03.
  </acceptance_criteria>
  <done>The repository has one tested role-to-capability matrix that later phases can reuse instead of reopening role semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add capability-based middleware adapters and baseline router adoption</name>
  <read_first>
    - internal/middleware/auth.go
    - internal/router/router.go
    - internal/router/router_test.go
    - internal/authz/capabilities.go
  </read_first>
  <files>internal/middleware/authorize.go, internal/router/router.go, internal/router/router_test.go</files>
  <behavior>
    - Test 1: admin-only user-management routes can be expressed through capability middleware instead of raw role strings.
    - Test 2: capability middleware returns 401 when no principal exists and 403 when the principal lacks the requested capability.
    - Test 3: route tests prove the protected surface still exposes login/public routes while retaining the current protected endpoints.
  </behavior>
  <action>Create `internal/middleware/authorize.go` with capability-based middleware adapters that consume the principal established in Plan 02 and the matrix from `internal/authz/capabilities.go`. Keep compatibility helpers where useful, but make `RequireRole(...)` a thin wrapper over the new baseline or start migrating the already-admin-only user routes in `internal/router/router.go` to `RequireCapability(authz.CapabilityManageUsers)` so policy is no longer expressed as repeated string literals. Update `internal/router/router_test.go` to cover middleware registration and at least one allow/deny path for capability enforcement. Do not lock down Phase 7 config and alert mutation endpoints yet; instead, expose the reusable middleware and apply it only where Phase 5 already has clear admin semantics, leaving the rest for the dedicated hardening phase.</action>
  <verify>
    <automated>go test ./internal/authz ./internal/middleware ./internal/router -run "TestCapability|TestRequireCapability|TestRouter" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/middleware/authorize.go` exposes a capability-based guard reusable by later phases.
    - `internal/router/router.go` contains at least one concrete capability-based route guard for current admin-only user operations.
    - Tests fail if capability middleware stops distinguishing missing principal from insufficient permissions.
  </acceptance_criteria>
  <done>Phase 5 leaves behind a reusable backend authz baseline that later phases can extend without redesigning route guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated principal -> capability check | Backend policy code decides whether a role may perform security-sensitive operations |
| router guard -> handler execution | Incorrect allow/deny behavior at middleware level can expose protected routes or break legitimate access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | E | `internal/authz/capabilities.go` | mitigate | Encode the allow/deny matrix explicitly in code and lock it with table-driven tests for all supported roles |
| T-05-08 | D | `internal/middleware/authorize.go` | mitigate | Return 401 for missing principal and 403 for denied capability, with direct middleware tests for both branches |
| T-05-09 | T | `internal/router/router.go` | mitigate | Adopt capability middleware on the already-admin-only route surface first so Phase 5 changes remain scoped while proving the baseline wiring |
</threat_model>

<verification>
Run `go test ./internal/authz ./internal/middleware ./internal/router -run "TestCapability|TestRequireCapability|TestRouter" -count=1` to prove the matrix and middleware adapters behave consistently.
</verification>

<success_criteria>
- The backend has a centralized, tested role-to-capability matrix.
- Capability middleware exists and is adopted on at least the existing admin-only user-management routes.
- Later phases can reuse the same principal and capability APIs for account status checks and broader protected-route hardening.
</success_criteria>

<output>
After completion, create `.planning/phases/05-normalize-role-model/05-03-SUMMARY.md`
</output>
