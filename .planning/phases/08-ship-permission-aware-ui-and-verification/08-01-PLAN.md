---
phase: 08-ship-permission-aware-ui-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/types/index.ts
  - frontend/src/stores/userStore.ts
  - frontend/src/App.tsx
  - frontend/src/api/client.ts
  - frontend/src/api/auth.ts
  - frontend/src/authz/capabilities.ts
  - frontend/src/components/index.ts
  - frontend/src/components/PermissionNotice.tsx
autonomous: true
requirements: [FEACL-01, FEACL-03, VER-03, VER-04]
must_haves:
  truths:
    - "前端存在与后端一致的角色能力真源，而不是在页面里到处散落 `role === 'admin'` 判断。"
    - "菜单、路由和会话错误处理能区分 401 与 403，并能给后续页面提供统一的权限提示 seam。"
    - "强制改密与 admin-only `/users` 行为在新权限壳层下继续成立。"
  artifacts:
    - path: "frontend/src/authz/capabilities.ts"
      provides: "frontend-owned role/capability contract matching backend Phase 7 semantics"
    - path: "frontend/src/App.tsx"
      provides: "permission-aware shell, menu wiring, and route guarding"
    - path: "frontend/src/components/PermissionNotice.tsx"
      provides: "shared read-only / forbidden feedback component"
  key_links:
    - from: "frontend/src/authz/capabilities.ts"
      to: "internal/authz/capabilities.go"
      via: "same admin/operator/viewer capability matrix"
      pattern: "view_alerts|process_alerts|view_config|manage_config|manage_users"
    - from: "frontend/src/api/client.ts"
      to: "internal/middleware/authorize.go"
      via: "401 redirect vs 403 forbidden normalization"
      pattern: "unauthorized|insufficient permissions"
---

<objective>
先搭一个可复用的前端权限壳层，让 App、菜单、路由和 API 错误语义都基于统一 capability contract，而不是继续靠零散角色字符串判断。

Purpose: Phase 8 后续页面显隐和测试都依赖统一权限 contract；如果不先把壳层搭好，第二波会重复改每个页面。
Output: frontend capability helper、共享权限提示组件、permission-aware shell 与 401/403 客户端语义。
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce a frontend capability matrix that mirrors Phase 7 backend semantics</name>
  <files>frontend/src/authz/capabilities.ts, frontend/src/types/index.ts, frontend/src/stores/userStore.ts</files>
  <read_first>frontend/src/types/index.ts, frontend/src/stores/userStore.ts, frontend/src/App.tsx, internal/authz/capabilities.go, .planning/phases/08-ship-permission-aware-ui-and-verification/08-RESEARCH.md</read_first>
  <action>Create `frontend/src/authz/capabilities.ts` with concrete role/capability constants that match the backend matrix exactly: `admin` can `view_alerts`, `process_alerts`, `view_config`, `manage_config`, `manage_users`; `operator` can `view_alerts`, `process_alerts`, `view_config`; `viewer` can `view_alerts`, `view_config`. Export helpers such as `can(role, capability)`, `canUser(user, capability)`, and page-facing booleans for `isAdmin`, `isReadOnlyConfigUser`, and `canProcessAlerts`. Update `frontend/src/types/index.ts` and `frontend/src/stores/userStore.ts` so persisted users keep optional account-control fields like `force_password_reset` and can drive these helpers without crashing older localStorage payloads.</action>
  <acceptance_criteria>`frontend/src/authz/capabilities.ts` defines explicit capability strings and a role matrix for `admin`, `operator`, `viewer`; the file contains `manage_config` and `process_alerts`; `frontend/src/stores/userStore.ts` still reads old localStorage payloads safely and exposes data usable by the new helper; `frontend/src/types/index.ts` keeps role as `'admin' | 'operator' | 'viewer'`.</acceptance_criteria>
  <verify>
    <automated>cd frontend && pnpm build</automated>
    <automated>rg -n "process_alerts|manage_config|manage_users|canUser|isReadOnlyConfigUser" frontend/src/authz/capabilities.ts frontend/src/stores/userStore.ts frontend/src/types/index.ts</automated>
  </verify>
  <done>Frontend now has one source of truth for permission decisions, aligned with the backend matrix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the app shell and API clients to the shared permission contract and forbidden semantics</name>
  <files>frontend/src/App.tsx, frontend/src/api/client.ts, frontend/src/api/auth.ts, frontend/src/components/PermissionNotice.tsx, frontend/src/components/index.ts</files>
  <read_first>frontend/src/App.tsx, frontend/src/api/client.ts, frontend/src/api/auth.ts, frontend/src/components/index.ts, frontend/src/pages/index.ts, frontend/src/authz/capabilities.ts</read_first>
  <action>Refactor `frontend/src/App.tsx` so menu construction and route decisions use the new capability helper instead of inline `role === 'admin'` checks. Preserve current routes, but make the shell compute access from capabilities: `/users` remains manage-users-only, `/profile` remains available to all authenticated users, forced-reset users still redirect to `/profile`, and configuration pages stay navigable for roles with `view_config`. Add a shared `PermissionNotice` component in `frontend/src/components/PermissionNotice.tsx` that can render concise read-only or forbidden guidance such as `当前角色无权执行该操作` and re-export it through `frontend/src/components/index.ts`. Update `frontend/src/api/client.ts` and `frontend/src/api/auth.ts` so `401` still logs out and redirects to `/login`, while `403` errors preserve the backend message (`insufficient permissions` or server-provided Chinese copy) for page-level display instead of forcing a redirect.</action>
  <acceptance_criteria>`frontend/src/App.tsx` imports the shared capability helper and uses it for menu or route gating; `frontend/src/components/PermissionNotice.tsx` exists and exports a reusable permission-feedback component; `frontend/src/api/client.ts` and `frontend/src/api/auth.ts` still clear session on `401` but do not redirect on `403`; `/users` remains protected while `/profile` and forced-reset behavior still exist in `frontend/src/App.tsx`.</acceptance_criteria>
  <verify>
    <automated>cd frontend && pnpm build</automated>
    <automated>rg -n "PermissionNotice|403|401|force_password_reset|/users|/profile|canUser" frontend/src/App.tsx frontend/src/api/client.ts frontend/src/api/auth.ts frontend/src/components/PermissionNotice.tsx</automated>
  </verify>
  <done>The shell and client boundary now expose stable permission semantics for the page-level work in Wave 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend capability matrix -> frontend capability helper | Client UX must reflect the same permission model Phase 7 already enforces on the server |
| API error status -> SPA behavior | `401` should end the session, while `403` should become a local permission message rather than a silent redirect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | E | scattered role checks in pages | mitigate | Introduce one frontend capability helper that mirrors backend semantics |
| T-08-02 | R | losing backend deny meaning in SPA | mitigate | Preserve `403` as page-displayable forbidden state instead of treating it like `401` |
| T-08-03 | D | stale localStorage payloads after user type expansion | mitigate | Keep account-control fields optional and parse old payloads safely |
</threat_model>

<verification>
- `cd frontend && pnpm build`
- `rg -n "process_alerts|manage_config|manage_users|PermissionNotice|force_password_reset|/users|403" frontend/src`
</verification>

<success_criteria>
- Frontend has a single reusable permission contract.
- The app shell uses capabilities rather than ad hoc role string checks.
- The client can distinguish forbidden UX from expired-session UX.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ship-permission-aware-ui-and-verification/08-01-SUMMARY.md`
</output>
