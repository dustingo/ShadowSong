---
phase: 13-harden-notification-delivery-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/handlers/webhook.go
  - internal/notifier/notifier.go
autonomous: true
requirements:
  - NTFY-01
  - NTFY-03
user_setup: []
must_haves:
  truths:
    - "Async notification processing cannot crash the process path when a notification goroutine panics"
    - "Notification send boundaries preserve alert and channel context through failures"
    - "Existing webhook ingest, routing, and send flow semantics remain intact after hardening"
  artifacts:
    - path: "internal/handlers/webhook.go"
      provides: "panic-protected async notification boundary and contextual logging path"
      contains: "processAlertNotifications"
    - path: "internal/notifier/notifier.go"
      provides: "notification send failure contract usable by handler-level trace logging"
      contains: "SendToChannel"
  key_links:
    - from: "internal/handlers/webhook.go"
      to: "internal/notifier/notifier.go"
      via: "context-rich notification sending boundary"
      pattern: "SendToChannel"
    - from: "internal/handlers/webhook.go"
      to: "HandleWebhook"
      via: "async goroutine entrypoint"
      pattern: "go h.processAlertNotifications"
---

<objective>
Harden the async notification path against panics and silent failures.

Purpose: Ensure webhook-triggered notification goroutines fail safely, preserve the current alert pipeline semantics, and emit enough context to debug failure points without redesigning the architecture.
Output: A panic-protected notification execution boundary plus contextual failure handling around template rendering and channel delivery.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-harden-notification-delivery-path/13-CONTEXT.md
@internal/handlers/webhook.go
@internal/notifier/notifier.go
@internal/models/models.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add panic recovery and explicit failure boundaries around async notification processing</name>
  <files>internal/handlers/webhook.go, internal/notifier/notifier.go</files>
  <read_first>internal/handlers/webhook.go, internal/notifier/notifier.go, internal/models/models.go, .planning/phases/13-harden-notification-delivery-path/13-CONTEXT.md</read_first>
  <action>Refactor the async notification entrypoint in `internal/handlers/webhook.go` so notification processing executes behind a panic-recover boundary that logs the failure context instead of letting the goroutine die silently. Keep the existing webhook ingest, route matching, template rendering, and send semantics, but make the send path explicit about stage boundaries such as route lookup, datasource/template resolution, render fallback, and channel delivery. If needed, introduce a minimal injection seam or helper functions so panic and send-failure paths can be tested without redesigning the handler architecture. In `internal/notifier/notifier.go`, preserve the sender API but ensure downstream errors are suitable for handler-level trace logging rather than opaque failures.</action>
  <verify>go test ./internal/handlers ./internal/notifier</verify>
  <acceptance_criteria>
    - `internal/handlers/webhook.go` contains a panic recovery boundary for async notification processing
    - notification failures can be logged with alert and channel context at the handler boundary
    - existing notification routing and default-fallback behavior remain present
  </acceptance_criteria>
  <done>The notification path fails safely and keeps enough context to diagnose delivery issues.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] async notification processing is panic-protected
- [ ] send/render failure boundaries preserve alert and channel context
- [ ] `go test ./internal/handlers ./internal/notifier` passes
</verification>

<success_criteria>

- All tasks completed
- Async notification reliability is improved without architecture migration
- Failure boundaries are explicit and diagnosable

</success_criteria>

<output>
After completion, create `.planning/phases/13-harden-notification-delivery-path/13-01-SUMMARY.md`
</output>
