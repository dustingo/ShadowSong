---
phase: 13-harden-notification-delivery-path
plan: 02
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - internal/handlers/webhook_test.go
  - internal/notifier/notifier_test.go
autonomous: true
requirements:
  - NTFY-02
  - NTFY-03
user_setup: []
must_haves:
  truths:
    - "Reliability hardening is backed by direct Go tests, not only source inspection"
    - "Key notification failure points are traceable to concrete alert or channel context"
    - "The hardened notification path still supports current webhook routing behavior"
  artifacts:
    - path: "internal/handlers/webhook_test.go"
      provides: "tests for panic recovery and contextual notification failures"
      contains: "processAlertNotifications"
    - path: "internal/notifier/notifier_test.go"
      provides: "tests for notifier error contracts where needed"
      contains: "SendToChannel"
  key_links:
    - from: "internal/handlers/webhook_test.go"
      to: "internal/handlers/webhook.go"
      via: "reliability-path verification"
      pattern: "processAlertNotifications"
    - from: "internal/notifier/notifier_test.go"
      to: "internal/notifier/notifier.go"
      via: "sender failure contract verification"
      pattern: "SendToChannel"
---

<objective>
Prove the hardened notification path is observable and resilient through direct tests.

Purpose: Lock in the panic-recovery and contextual failure guarantees so future changes cannot silently regress the notification delivery path.
Output: Handler/notifier tests that verify panic recovery, failure logging boundaries, and stable notification error contracts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-harden-notification-delivery-path/13-CONTEXT.md
@.planning/phases/13-harden-notification-delivery-path/13-01-PLAN.md
@internal/handlers/webhook.go
@internal/notifier/notifier.go
@internal/handlers/webhook_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add direct verification for panic recovery and notification failure traceability</name>
  <files>internal/handlers/webhook_test.go, internal/notifier/notifier_test.go</files>
  <read_first>internal/handlers/webhook.go, internal/notifier/notifier.go, internal/handlers/webhook_test.go, .planning/phases/13-harden-notification-delivery-path/13-CONTEXT.md</read_first>
  <action>Extend Go tests so Phase 13 reliability guarantees are explicitly verified. Cover at least one panic-in-notification path that proves the async boundary recovers instead of crashing the test/process path, and at least one failure path that proves alert/channel context can be associated with the failure boundary. Add notifier-level tests only where they materially lock in the failure contract or error messages needed by handler-level traceability. Keep tests local and deterministic; do not require live external notification endpoints.</action>
  <verify>go test ./internal/handlers ./internal/notifier</verify>
  <acceptance_criteria>
    - `internal/handlers/webhook_test.go` contains a direct reliability-path test for notification processing
    - tests prove panic recovery or equivalent protected failure boundary behavior
    - tests prove notification failure paths remain traceable to alert or channel context
    - `go test ./internal/handlers ./internal/notifier` exits 0
  </acceptance_criteria>
  <done>Notification hardening is backed by direct automated verification.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] reliability tests cover panic-safe notification processing
- [ ] failure-path tests cover alert/channel traceability
- [ ] `go test ./internal/handlers ./internal/notifier` passes
</verification>

<success_criteria>

- All tasks completed
- Reliability hardening is directly verified
- Future regressions in notification failure visibility are harder to reintroduce

</success_criteria>

<output>
After completion, create `.planning/phases/13-harden-notification-delivery-path/13-02-SUMMARY.md`
</output>
