---
phase: 06-secure-user-administration
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - internal/handlers/user.go
  - internal/handlers/user_test.go
  - internal/router/router.go
  - internal/router/router_test.go
autonomous: true
requirements: [USER-01, USER-02, USER-03, USER-04, USER-05, USER-06]
must_haves:
  truths:
    - "`admin` 有独立的用户管理接口，可以看列表、创建用户、修改其他用户、删除用户并处理账号状态。"
    - "普通用户只能更新自己的安全个人资料和密码，无法借 payload 修改角色或其它用户。"
    - "危险边界如 self-disable、self-demotion、stale session after security change 都有显式拒绝和回归覆盖。"
  artifacts:
    - path: "internal/handlers/user.go"
      provides: "分离后的 admin 管理接口与 self-service 资料/密码接口"
    - path: "internal/router/router.go"
      provides: "admin 路由与 self-service 路由清晰分组"
    - path: "internal/handlers/user_test.go"
      provides: "管理员与自助流程回归测试"
    - path: "internal/router/router_test.go"
      provides: "admin/self-service 路由鉴权矩阵"
  key_links:
    - from: "internal/router/router.go"
      to: "internal/handlers/user.go"
      via: "admin capability routes vs authenticated self-service routes"
      pattern: "RequireCapability\\(authz\\.CapabilityManageUsers\\)|/users/me"
    - from: "internal/handlers/user.go"
      to: "internal/models/user.go"
      via: "role/state mutation updates token invalidation state"
      pattern: "SetPassword|ForcePasswordReset|DisabledAt|TokenInvalidBefore"
---

<objective>
在 Phase 6 的后端层明确分开“管理员管人”和“普通用户维护自己”，并补齐高风险边界保护。

Purpose: 满足 USER-01 到 USER-04，并把 USER-05/USER-06 的账号状态控制接到具体管理与自助 API 上。
Output: 新的 admin/self-service 用户路由、清晰 DTO、危险边界保护和完整回归测试。
</objective>

<execution_context>
@C:/Users/doumaoxin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/doumaoxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-secure-user-administration/06-RESEARCH.md
@.planning/phases/05-normalize-role-model/05-03-SUMMARY.md
@.planning/phases/06-secure-user-administration/06-01-PLAN.md
@internal/handlers/user.go
@internal/router/router.go
@internal/handlers/user_test.go
@internal/router/router_test.go

<interfaces>
Phase 5/06-01 baseline to preserve:
```go
users := v1.Group("/users")
users.Use(middleware.JWTAuth(jwtAuth))
{
	users.GET("", middleware.RequireCapability(authz.CapabilityManageUsers), userHandler.ListUsers)
	users.GET("/me", userHandler.GetCurrentUser)
	users.POST("", middleware.RequireCapability(authz.CapabilityManageUsers), userHandler.CreateUser)
	users.PUT("/:id", userHandler.UpdateUser)
	users.DELETE("/:id", middleware.RequireCapability(authz.CapabilityManageUsers), userHandler.DeleteUser)
}
```

Current mixed update payload:
```go
var input struct {
	Name     string `json:"name"`
	Email    string `json:"email"`
	Role     string `json:"role"`
	Password string `json:"password"`
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the mixed user write path with explicit admin-managed contracts</name>
  <files>internal/handlers/user.go, internal/handlers/user_test.go</files>
  <read_first>internal/handlers/user.go, internal/handlers/user_test.go, .planning/phases/06-secure-user-administration/06-RESEARCH.md, .planning/phases/05-normalize-role-model/05-03-SUMMARY.md</read_first>
  <behavior>
    - Test 1: admin can list users with role plus account-control state, create users, and update another user’s role/profile/security flags.
    - Test 2: non-admin cannot call admin update/delete paths or smuggle admin-only fields through a self-service payload.
    - Test 3: self-delete, self-disable, and self-demotion attempts are rejected with explicit client-visible errors.
  </behavior>
  <action>Refactor `internal/handlers/user.go` so admin operations use dedicated DTOs and methods instead of `UpdateUser` mixing self-service and admin fields. Keep `ListUsers`, `CreateUser`, and `DeleteUser` under `CapabilityManageUsers`, and introduce an explicit admin update method that can modify `name`, `email`, `role`, `disabled_at`/enabled state, and `force_password_reset` for a target user. On admin security-sensitive changes, update the per-user token invalidation cutoff from 06-01. Block unsafe edge cases required by the phase: self-delete stays blocked, add self-disable rejection, and reject self-demotion from `admin` via the admin-update path. Keep error bodies explicit (`cannot disable yourself`, `cannot change your own role`, etc.).</action>
  <acceptance_criteria>`internal/handlers/user.go` no longer relies on a single mixed `UpdateUser` payload for both admin and self-service flows; admin update DTO binds role/status/reset fields explicitly; tests cover admin allow, non-admin deny, self-disable reject, self-demotion reject, and admin-triggered token invalidation behavior.</acceptance_criteria>
  <verify>
    <automated>go test ./internal/handlers -run "Test(Admin|CreateUser|DeleteUser|UpdateUser|Self)" -count=1 -timeout 60s</automated>
  </verify>
  <done>Admin-managed user operations are explicit, high-risk self-target mutations are blocked, and regression tests prove the boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add self-service profile and password endpoints that cannot mutate role or peer accounts</name>
  <files>internal/handlers/user.go, internal/handlers/user_test.go</files>
  <read_first>internal/handlers/user.go, internal/handlers/user_test.go, internal/models/user.go, .planning/phases/06-secure-user-administration/06-01-PLAN.md</read_first>
  <behavior>
    - Test 1: authenticated users can update only their own `name` and `email` through a profile endpoint.
    - Test 2: authenticated users can change their own password; successful change clears forced-reset state and invalidates older tokens.
    - Test 3: self-service payloads containing `role`, `disabled_at`, or `force_password_reset` are ignored or rejected rather than applied.
  </behavior>
  <action>Add dedicated self-service methods in `internal/handlers/user.go`: keep `GetCurrentUser`, add `UpdateOwnProfile`, and add `UpdateOwnPassword`. Restrict profile edits to safe fields (`name`, `email`) only. Move password changes into a separate endpoint and request DTO that requires the new password; if the repo already has old-password checking logic, reuse it, otherwise keep scope to the current contract while still clearing `ForcePasswordReset` and rotating the token invalidation cutoff on success. Ensure self-service code never binds or applies role/account-state fields and can operate while the account is in forced-reset limited mode.</action>
  <acceptance_criteria>`internal/handlers/user.go` exposes separate self-profile and self-password methods; self-service DTOs do not contain role or account-control fields; password success clears forced-reset state and refreshes token cutoff; tests prove a user cannot modify another user or elevate privileges through self-service endpoints.</acceptance_criteria>
  <verify>
    <automated>go test ./internal/handlers -run "Test(SelfService|Password|Profile|ForcedReset)" -count=1 -timeout 60s</automated>
  </verify>
  <done>Ordinary users now have a narrow self-service surface that supports profile maintenance and password completion without any privilege-escalation path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewire router paths to the split contracts and lock the auth matrix with router tests</name>
  <files>internal/router/router.go, internal/router/router_test.go</files>
  <read_first>internal/router/router.go, internal/router/router_test.go, internal/middleware/authorize.go, internal/middleware/auth.go, .planning/phases/05-normalize-role-model/05-03-SUMMARY.md</read_first>
  <behavior>
    - Test 1: admin-only user-management endpoints require `CapabilityManageUsers`.
    - Test 2: authenticated non-admin users can still reach self-service endpoints.
    - Test 3: disabled/stale/forced-reset principals get the correct `401`/limited-mode behavior on the new route set.
  </behavior>
  <action>Update `internal/router/router.go` to express the split explicitly: admin management routes remain under `/api/v1/users` with `RequireCapability(authz.CapabilityManageUsers)`, while self-service routes live under `/api/v1/users/me`, `/api/v1/users/me/profile`, and `/api/v1/users/me/password` and depend only on authenticated principals plus the 06-01 limited-mode rules. Remove or retire the mixed `PUT /users/:id` route so no ambiguous write surface remains. Expand router tests to cover admin allow/deny, self-service reachability, and the stale-session/forced-reset behavior on representative endpoints.</action>
  <acceptance_criteria>`internal/router/router.go` contains distinct admin and self-service user routes; no mixed write route remains for ordinary users to hit; `internal/router/router_test.go` covers unauthenticated, operator/viewer, admin, disabled, and forced-reset route outcomes.</acceptance_criteria>
  <verify>
    <automated>go test ./internal/router ./internal/handlers ./internal/middleware -run "Test(Router|Admin|SelfService|Disabled|ForcedReset)" -count=1 -timeout 60s</automated>
  </verify>
  <done>The route surface now mirrors the intended security model, and router-level regressions lock the boundary in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated principal→user-management handlers | Trusted identity may still attempt unauthorized peer or self-target mutations |
| admin payload→target user security state | Admin write paths can invalidate sessions or alter roles for another account |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | E | `internal/handlers/user.go` mixed update removal | mitigate | Split admin DTOs from self-service DTOs so non-admin payloads never bind role/security fields |
| T-06-06 | E | admin self-target mutations | mitigate | Explicitly reject self-disable and self-demotion in the admin update/delete handlers |
| T-06-07 | T | role/status changes | mitigate | When role/disable/reset/password actions succeed, rotate the per-user token invalidation cutoff from 06-01 |
| T-06-08 | R | route surface ambiguity | mitigate | Remove the old mixed write route and cover the new matrix in router tests with concrete status-code assertions |
</threat_model>

<verification>
- `go test ./internal/handlers ./internal/router ./internal/middleware -run "Test(Admin|SelfService|Router|Disabled|ForcedReset|Password|Profile)" -count=1 -timeout 60s`
- Grep check after implementation: `rg -n "/users/me|CapabilityManageUsers|cannot disable yourself|cannot change your own role" internal/handlers/user.go internal/router/router.go`
</verification>

<success_criteria>
- Admin and self-service write surfaces are no longer ambiguous.
- Non-admins cannot change other users, roles, or account-control fields.
- Security-sensitive user changes invalidate stale sessions and are regression-covered.
</success_criteria>

<output>
After completion, create `.planning/phases/06-secure-user-administration/06-02-SUMMARY.md`
</output>
