---
phase: 17-clean-truth-and-operational-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/MILESTONES.md
  - .planning/RETROSPECTIVE.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/TESTING.md
  - docs/CODE_REVIEW.md
  - scripts/verify_backend_no_ai.ps1
  - scripts/verify_frontend_no_ai.ps1
  - scripts/verify_backend_alert_flow.ps1
  - scripts/verify_frontend_console_baseline.ps1
  - internal/config/config_test.go
  - internal/router/router_test.go
autonomous: true
requirements:
  - DOCS-01
  - DOCS-03
must_haves:
  truths:
    - "维护者在 README 和 planning 真源入口中看到的是当前告警系统命名，而不是把现状绑定在 AI 移除对照语境上。"
    - "仓库内可执行的后端与前端验证入口使用当前命名，且所有 repo 内引用同步到新脚本路径。"
    - "历史归档事实与高风险运行时历史命名被明确标记为历史/暂缓迁移，而不是被误读成当前推荐状态。"
  artifacts:
    - path: "scripts/verify_backend_alert_flow.ps1"
      provides: "后端告警主链路验证入口"
    - path: "scripts/verify_frontend_console_baseline.ps1"
      provides: "前端控制台构建与残留扫描入口"
    - path: "README.md"
      provides: "当前仓库运行与验证入口"
    - path: "docs/CODE_REVIEW.md"
      provides: "明确标记为历史审查快照的文档入口"
  key_links:
    - from: "README.md"
      to: "scripts/verify_backend_alert_flow.ps1"
      via: "模板预览与验证 / 工程质量门禁章节"
      pattern: "verify_backend_alert_flow\\.ps1"
    - from: ".planning/codebase/TESTING.md"
      to: "scripts/verify_frontend_console_baseline.ps1"
      via: "verification entrypoints"
      pattern: "verify_frontend_console_baseline\\.ps1"
    - from: "docs/CODE_REVIEW.md"
      to: ".planning/phases/14-establish-alert-trace-context/14-VERIFICATION.md"
      via: "historical framing pointing maintainers to current truth"
      pattern: "14-VERIFICATION\\.md"
---

<objective>
收口当前真源入口和低风险验证命名，避免维护者继续从 README、planning 文档和脚本入口读到过期的“AI 移除对照语境”。

Purpose: 满足 D-01、D-02、D-03、D-08、D-09、D-10、D-11，让当前仓库入口先说真话，再让后续运维 runbook 有稳定落点。
Output: 已改名的验证脚本、同步更新的 README/codebase map/planning 真源、以及被明确标记为历史快照的 `docs/CODE_REVIEW.md`。
</objective>

<execution_context>
@C:/Users/doumaoxin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/doumaoxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-clean-truth-and-operational-docs/17-CONTEXT.md
@.planning/phases/17-clean-truth-and-operational-docs/17-RESEARCH.md
@README.md
@.planning/MILESTONES.md
@.planning/RETROSPECTIVE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/TESTING.md
@docs/CODE_REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename low-risk verification entrypoints and eliminate repo-visible old script names</name>
  <files>scripts/verify_backend_no_ai.ps1, scripts/verify_frontend_no_ai.ps1, scripts/verify_backend_alert_flow.ps1, scripts/verify_frontend_console_baseline.ps1, README.md, .planning/codebase/STRUCTURE.md, .planning/codebase/TESTING.md, internal/config/config_test.go, internal/router/router_test.go</files>
  <read_first>scripts/verify_backend_no_ai.ps1, scripts/verify_frontend_no_ai.ps1, README.md, .planning/codebase/STRUCTURE.md, .planning/codebase/TESTING.md, internal/config/config_test.go, internal/router/router_test.go, .planning/phases/17-clean-truth-and-operational-docs/17-CONTEXT.md, .planning/phases/17-clean-truth-and-operational-docs/17-RESEARCH.md</read_first>
  <action>Per D-02 and D-08, rename the PowerShell verification entrypoints to current-state names: `scripts/verify_backend_no_ai.ps1` -> `scripts/verify_backend_alert_flow.ps1` and `scripts/verify_frontend_no_ai.ps1` -> `scripts/verify_frontend_console_baseline.ps1`. Update the script internals so temp log filenames, generated temp filenames, run IDs, and human-readable step labels no longer contain `no_ai` or `VerifyNoAI`. Update every in-repo reference to the new paths in `README.md`, `.planning/codebase/STRUCTURE.md`, and `.planning/codebase/TESTING.md`, and rename the low-risk test identifiers `TestLoad_WithoutAIEnv` and `TestSetup_RoutesWithoutAIRuntime` to current baseline names without changing assertions or runtime behavior. Do not touch `go.mod`, Go import paths, JWT issuer, DB names, or any live runtime contract.</action>
  <acceptance_criteria>
    - `Test-Path scripts/verify_backend_alert_flow.ps1` and `Test-Path scripts/verify_frontend_console_baseline.ps1` both return true.
    - `rg -n "verify_backend_no_ai|verify_frontend_no_ai|WithoutAI|WithoutAIRuntime|VerifyNoAI|verify_no_ai" README.md .planning/codebase scripts internal/config internal/router` returns no matches.
    - `rg -n "verify_backend_alert_flow|verify_frontend_console_baseline|CurrentBaseline|AlertFlow" README.md .planning/codebase scripts internal/config internal/router` returns the expected new names.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "if (-not (Test-Path 'scripts/verify_backend_alert_flow.ps1')) { exit 1 }; if (-not (Test-Path 'scripts/verify_frontend_console_baseline.ps1')) { exit 1 }; rg -n 'verify_backend_no_ai|verify_frontend_no_ai|WithoutAI|WithoutAIRuntime|VerifyNoAI|verify_no_ai' README.md .planning/codebase scripts internal/config internal/router; if ($LASTEXITCODE -eq 0) { exit 1 }; rg -n 'verify_backend_alert_flow|verify_frontend_console_baseline|CurrentBaseline|AlertFlow' README.md .planning/codebase scripts internal/config internal/router"</automated>
  </verify>
  <done>New verification script filenames are the only maintainer-facing script paths left in the repo truth surface, and the renamed tests still describe the current baseline instead of AI-removal context.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh current truth documents and mark historical review surfaces explicitly</name>
  <files>README.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/MILESTONES.md, .planning/RETROSPECTIVE.md, docs/CODE_REVIEW.md</files>
  <read_first>README.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/MILESTONES.md, .planning/RETROSPECTIVE.md, docs/CODE_REVIEW.md, .planning/phases/17-clean-truth-and-operational-docs/17-CONTEXT.md, .planning/phases/17-clean-truth-and-operational-docs/17-RESEARCH.md, go.mod, internal/auth/jwt.go, .planning/phases/14-establish-alert-trace-context/14-VERIFICATION.md, .planning/phases/15-harden-notification-retry-boundaries/15-VERIFICATION.md, .planning/phases/16-standardize-alert-path-logging/16-VERIFICATION.md</read_first>
  <action>Per D-01, D-03, D-09, D-10, and D-11, rewrite the current-truth docs so current entrypoints describe the system as the ongoing game-ops alert platform, not as an AI-removal cleanup project. In `README.md`, update the verification section to the renamed script paths and add a short note that deeper runtime names such as `go.mod` module path and JWT issuer are historical/runtime contracts intentionally deferred. In `.planning/PROJECT.md`, `.planning/ROADMAP.md`, and `.planning/REQUIREMENTS.md`, keep v1.3 focused on reliability/observability and Phase 17 on truth-surface cleanup plus maintainer documentation. In `.planning/MILESTONES.md` and `.planning/RETROSPECTIVE.md`, preserve `v1.0 AI Removal Complete` as historical shipped context but add framing that it is archive history rather than the current operating narrative. In `docs/CODE_REVIEW.md`, do not refresh the old findings list as if it were current; instead add an explicit historical-header framing, timestamp scope, and pointers to the current verification truth for Phases 14-16 and the upcoming Phase 17 operational docs.</action>
  <acceptance_criteria>
    - `README.md` references only `verify_backend_alert_flow.ps1` and `verify_frontend_console_baseline.ps1` for current verification commands.
    - `docs/CODE_REVIEW.md` contains a visible historical framing string such as `历史审查快照` or `Historical`.
    - `.planning/MILESTONES.md` and `.planning/RETROSPECTIVE.md` still preserve `AI Removal Complete` as history, but current-entry wording points maintainers toward v1.3 reliability/observability truth.
    - `README.md` or `.planning/PROJECT.md` explicitly states that `go.mod` module path and JWT issuer remain deferred historical runtime names.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "rg -n 'verify_backend_alert_flow\\.ps1|verify_frontend_console_baseline\\.ps1' README.md; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n '历史审查快照|Historical' docs/CODE_REVIEW.md; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n 'AI Removal Complete' .planning/MILESTONES.md .planning/RETROSPECTIVE.md; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n 'go\\.mod|JWT issuer|Issuer: \"ai-alert-system\"|历史遗留|暂缓迁移' README.md .planning/PROJECT.md .planning/ROADMAP.md"</automated>
  </verify>
  <done>Current truth surfaces describe the live system and current verification entrypoints accurately, while historical archive language and deferred runtime names are clearly labeled instead of being treated as active guidance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer→repo entrypoints | Maintainers rely on README, planning docs, and script names to decide what to run. |
| archive→current truth | Historical milestone/review language can be mistaken for live operational guidance if not framed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | R | README.md / .planning truth surfaces | mitigate | Replace current-entry wording that still implies AI-removal framing, and point verification commands to the renamed script files per D-01/D-02. |
| T-17-02 | T | script path references | mitigate | Rename the script files and update every in-repo reference atomically so documentation cannot point to stale executable names. |
| T-17-03 | D | maintainer verification workflow | mitigate | Add grep-based acceptance checks proving old script names and old test identifiers are gone from maintainer-visible repo surfaces. |
| T-17-04 | S | historical naming boundaries | mitigate | Explicitly document `go.mod` module path and JWT issuer as deferred runtime contracts per D-07/D-09 instead of attempting silent renames. |
</threat_model>

<verification>
Run the grep/file existence command from each task. If the repo has Go available, re-run `go test ./internal/config ./internal/router -count=1` after the test renames to prove only identifiers changed.
</verification>

<success_criteria>
README, planning truth, codebase maps, tests, and verification scripts all use the new low-risk naming and current operational framing, while historical archive language is explicitly marked as historical and deep runtime naming remains documented as deferred.
</success_criteria>

<output>
After completion, create `.planning/phases/17-clean-truth-and-operational-docs/17-01-SUMMARY.md`
</output>
