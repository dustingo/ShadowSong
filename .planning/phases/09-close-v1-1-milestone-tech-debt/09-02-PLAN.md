---
phase: 09-close-v1-1-milestone-tech-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/test/setup.ts
  - frontend/src/App.test.tsx
  - frontend/src/pages/Alerts.test.tsx
  - frontend/src/pages/DataSources.test.tsx
autonomous: true
requirements: [VER-02]
must_haves:
  truths:
    - "前端权限矩阵测试继续可重复通过，但输出中的 React Router future-flag 与非阻塞 `act(...)` 噪音显著收敛。"
    - "warning 清理建立在真实测试 harness 修复之上，而不是简单全局静音控制台。"
    - "Phase 8 已有的菜单、告警动作和配置页权限覆盖不因 warning cleanup 而退化。"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "router configuration aligned with the test/runtime contract"
    - path: "frontend/src/test/setup.ts"
      provides: "stable jsdom test environment for Ant Design and router-driven permission tests"
    - path: "frontend/src/App.test.tsx"
      provides: "shell/menu permission regression tests with reduced warning noise"
    - path: "frontend/src/pages/Alerts.test.tsx"
      provides: "alert permission test coverage preserved after harness cleanup"
    - path: "frontend/src/pages/DataSources.test.tsx"
      provides: "config read-only/admin visibility coverage preserved after harness cleanup"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/App.test.tsx"
      via: "same router behavior and future-flag configuration"
      pattern: "BrowserRouter|future"
    - from: "frontend/src/test/setup.ts"
      to: "frontend/src/App.test.tsx"
      via: "test-environment shims used by Ant Design menu/layout rendering"
      pattern: "matchMedia|ResizeObserver|scrollIntoView"
---

<objective>
收敛前端验证噪音，让 `pnpm test -- --run` 继续保留 Phase 8 的权限矩阵覆盖，同时去掉影响可读性的非阻塞 warning。

Purpose: 审计已确认测试通过但输出噪音较大；Phase 9 需要把验证信号变得稳定、可信，而不是让真实失败淹没在 warning 里。
Output: 更稳定的前端测试 harness、warning 收敛后的权限矩阵测试输出。
</objective>

<context>
@frontend/package.json
@frontend/src/App.tsx
@frontend/src/test/setup.ts
@frontend/src/App.test.tsx
@frontend/src/pages/Alerts.test.tsx
@frontend/src/pages/DataSources.test.tsx
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/08-ship-permission-aware-ui-and-verification/08-03-SUMMARY.md

<interfaces>
From frontend/src/App.tsx:
```tsx
export default function App()
```

From frontend/src/test/setup.ts:
```ts
import '@testing-library/jest-dom/vitest'
globalThis.ResizeObserver = ResizeObserverMock
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Eliminate React Router future-flag warning at the app/test boundary</name>
  <files>frontend/src/App.tsx, frontend/src/App.test.tsx</files>
  <behavior>
    - Test 1: Existing route and forbidden-state assertions still pass for viewer/admin/forced-reset users.
    - Test 2: `pnpm test -- --run` output no longer includes React Router future-flag warnings.
  </behavior>
  <action>Adjust the app router/test boundary so React Router 6 future-flag warnings are removed at the source. Use the supported `future` configuration on the router actually rendered by `frontend/src/App.tsx` and keep test expectations aligned with the production route contract. Do not “fix” this by muting `console.warn` globally. If the change requires slight test harness updates in `frontend/src/App.test.tsx`, keep the role-matrix assertions intact and continue asserting the same permission outcomes introduced in Phase 8.</action>
  <verify>
    <automated>cd frontend; $output = (pnpm test -- --run 2>&1 | Out-String); $output; if ($output -match 'future flag' -or $output -match 'React Router will begin wrapping state updates') { exit 1 }</automated>
  </verify>
  <done>Router-related warning noise is removed without changing the permission matrix being tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stabilize Ant Design permission tests so benign act warnings stop obscuring results</name>
  <files>frontend/src/test/setup.ts, frontend/src/App.test.tsx, frontend/src/pages/Alerts.test.tsx, frontend/src/pages/DataSources.test.tsx</files>
  <behavior>
    - Test 1: App, Alerts, and DataSources permission tests still cover the same visible/hidden outcomes after the harness refactor.
    - Test 2: `pnpm test -- --run` output no longer includes non-blocking `act(...)` warnings from the known permission-test path.
  </behavior>
  <action>Refine the jsdom/test harness to match how Ant Design `Menu` and related layout effects settle during the existing permission tests. Prefer deterministic setup or awaited assertions over blanket console suppression. Update `frontend/src/test/setup.ts` and the three Phase 8 permission tests so render timing is explicit where needed, the tests still prove viewer/operator/admin behavior, and the suite remains fast and repeatable. Only if a warning is traced to unavoidable third-party internals after supported harness fixes should you narrowly filter that exact message, documented inline with why it is safe.</action>
  <verify>
    <automated>cd frontend; $output = (pnpm test -- --run 2>&1 | Out-String); $output; if ($output -match 'not wrapped in act') { exit 1 }; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }</automated>
    <automated>cd frontend && pnpm build</automated>
  </verify>
  <done>Frontend permission tests keep their coverage but stop emitting the known non-blocking act noise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA runtime router config -> test runner | Test output should reflect the same router contract the app uses in production |
| jsdom harness -> Ant Design permission UI | Incomplete DOM shims or async handling can create noisy false positives that obscure real regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | R | frontend warning-heavy verification output | mitigate | Remove warning sources through supported router config and deterministic test harness updates |
| T-09-04 | T | accidental coverage regression while cleaning warnings | mitigate | Keep the existing App/Alerts/DataSources permission assertions and require both test and build commands to pass |
| T-09-05 | D | blanket console suppression hiding real failures | mitigate | Forbid global warning silencing; only allow narrowly scoped filtering if root-cause fixes prove impossible and the message is exact-known benign third-party output |
</threat_model>

<verification>
- `cd frontend; $output = (pnpm test -- --run 2>&1 | Out-String); $output; if ($output -match 'future flag' -or $output -match 'React Router will begin wrapping state updates' -or $output -match 'not wrapped in act') { exit 1 }`
- `cd frontend && pnpm build`
</verification>

<success_criteria>
- Frontend role-matrix tests still pass.
- Known React Router and `act(...)` warning noise is removed or reduced below the current audited baseline.
- No broad console muting is introduced just to get a clean run.
</success_criteria>

<output>
After completion, create `.planning/phases/09-close-v1-1-milestone-tech-debt/09-02-SUMMARY.md`
</output>
