---
phase: 02-remove-frontend-ai-surfaces
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/AlertCard.tsx
  - frontend/src/pages/Dashboard.tsx
  - frontend/src/pages/Alerts.tsx
autonomous: true
requirements:
  - FEAI-02
  - FEAI-03
must_haves:
  truths:
    - "Dashboard alert cards expose only operational actions that still exist after backend AI removal."
    - "Alert list detail rows show operational alert data without AI summary, root-cause, or suggestion blocks."
    - "Removing AI content does not break dashboard refresh, websocket, ack, or quick-silence behavior."
  artifacts:
    - path: "frontend/src/components/AlertCard.tsx"
      provides: "Shared alert card without AI action/render blocks"
    - path: "frontend/src/pages/Dashboard.tsx"
      provides: "Dashboard without AI modal or aiApi flow"
    - path: "frontend/src/pages/Alerts.tsx"
      provides: "Alert detail rows without AI summary block"
  key_links:
    - from: "frontend/src/pages/Dashboard.tsx"
      to: "frontend/src/components/AlertCard.tsx"
      via: "AlertCard props"
      pattern: "onAskAI"
    - from: "frontend/src/pages/Dashboard.tsx"
      to: "frontend/src/api/client.ts"
      via: "aiApi import"
      pattern: "aiApi"
---

<objective>
Remove AI-derived rendering and action chains from the dashboard, alert list, and shared alert card while preserving the operational alert workflow.

Purpose: Fulfill FEAI-02 and eliminate page-level AI state usage before shared API/type contracts are deleted in the final cleanup plan.
Output: Alert-facing pages render only non-AI operational information and actions.
</objective>

<execution_context>
@C:/Users/doumaoxin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/doumaoxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-remove-frontend-ai-surfaces/02-RESEARCH.md
@.planning/phases/02-remove-frontend-ai-surfaces/02-UI-SPEC.md
@frontend/src/components/AlertCard.tsx
@frontend/src/pages/Dashboard.tsx
@frontend/src/pages/Alerts.tsx
@frontend/src/types/index.ts

<interfaces>
From frontend/src/components/AlertCard.tsx:
```tsx
interface AlertCardProps {
  alert: Alert
  onAck?: (alert: Alert) => void
  onQuickSilence?: (alert: Alert) => void
  onAskAI?: (alert: Alert) => void
  showActions?: boolean
}
```

From frontend/src/pages/Dashboard.tsx:
```tsx
<AlertCard
  key={alert.alert_id}
  alert={alert}
  showActions={true}
  onAck={handleAck}
  onQuickSilence={handleQuickSilence}
  onAskAI={handleAskAI}
/>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip AI actions and modal flow from dashboard-active alert rendering</name>
  <read_first>
    - frontend/src/components/AlertCard.tsx
    - frontend/src/pages/Dashboard.tsx
    - .planning/phases/02-remove-frontend-ai-surfaces/02-UI-SPEC.md
  </read_first>
  <files>frontend/src/components/AlertCard.tsx, frontend/src/pages/Dashboard.tsx</files>
  <acceptance_criteria>
    - `AlertCard` no longer accepts or invokes `onAskAI`.
    - Dashboard no longer imports `aiApi` or `react-markdown`, and no AI modal/local AI state remains.
    - Active alert cards still support `确认` and `静默`, with websocket/polling logic untouched.
  </acceptance_criteria>
  <action>Update `frontend/src/components/AlertCard.tsx` and `frontend/src/pages/Dashboard.tsx` together. Remove the `问 AI` button, the AI summary/root-cause/suggestion section, the `onAskAI` prop, and the dashboard AI modal/request flow. Preserve current ack, quick-silence, websocket reconnect, stats cards, trend chart, and active-alert ordering exactly as they exist. This implements FEAI-02 and also removes page-level AI API usage that would otherwise block FEAI-03 in the final plan.</action>
  <verify>
    <automated>powershell -NoProfile -Command "rg -n 'onAskAI|问 AI|AI 分析|AI 响应|aiApi|ReactMarkdown|handleAskAI|handleSendToAI|aiModalVisible|aiResponse' frontend/src/components/AlertCard.tsx frontend/src/pages/Dashboard.tsx; if ($LASTEXITCODE -eq 0) { exit 1 }"</automated>
  </verify>
  <done>Shared alert cards and the dashboard expose only non-AI alert operations and no longer contain AI rendering/state/call chains.</done>
</task>

<task type="auto">
  <name>Task 2: Remove AI detail fields from alert-list expanded content</name>
  <read_first>
    - frontend/src/pages/Alerts.tsx
    - .planning/phases/02-remove-frontend-ai-surfaces/02-UI-SPEC.md
  </read_first>
  <files>frontend/src/pages/Alerts.tsx</files>
  <acceptance_criteria>
    - Expanded rows retain message and labels.
    - Expanded rows no longer render `AI 分析` or read `record.ai_summary`.
    - Search/filter/table behavior remains unchanged.
  </acceptance_criteria>
  <action>Update `frontend/src/pages/Alerts.tsx` so the expandable row renders only operational alert fields. Remove the AI summary block entirely instead of leaving placeholder spacing or substitute text. Keep search filters, pagination, status tags, ack modal, and quick-silence modal unchanged, in line with FEAI-02 and the approved UI-SPEC cleanup contract.</action>
  <verify>
    <automated>powershell -NoProfile -Command "rg -n 'AI 分析|ai_summary|ai_root_cause|ai_suggestions' frontend/src/pages/Alerts.tsx; if ($LASTEXITCODE -eq 0) { exit 1 }"</automated>
  </verify>
  <done>`frontend/src/pages/Alerts.tsx` no longer renders AI-derived detail content and still supports the existing alert operations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend alert JSON -> shared alert UI | Removed fields must not leave broken UI branches or empty containers |
| operator action -> dashboard/list controls | Operational buttons must continue to trigger existing ack and silence flows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | D | `frontend/src/pages/Dashboard.tsx` | mitigate | Remove dead `aiApi` calls instead of letting the UI hit already-removed backend AI endpoints |
| T-02-04 | T | `frontend/src/components/AlertCard.tsx` | mitigate | Delete AI sections cleanly and preserve only `确认` and `静默` actions so card layout still maps to real operations |
| T-02-05 | R | `frontend/src/pages/Alerts.tsx` | accept | Alert detail rows become simpler-only; no audit-sensitive behavior changes are introduced because ack/silence workflows remain elsewhere in the same page |
</threat_model>

<verification>
Use grep-based checks to prove AI-rendering tokens and call chains are removed before contract deletion in Plan 03.
</verification>

<success_criteria>
Dashboard, alert cards, and alert-list expanded rows contain no AI actions or analysis content, while operational alert handling remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/02-remove-frontend-ai-surfaces/02-02-SUMMARY.md`
</output>
