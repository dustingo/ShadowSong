---
phase: 02-remove-frontend-ai-surfaces
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - frontend/src/api/client.ts
  - frontend/src/types/index.ts
  - frontend/src/pages/Login.tsx
  - frontend/index.html
autonomous: true
requirements:
  - FEAI-03
must_haves:
  truths:
    - "Frontend transport and shared types no longer define AI-only endpoints or fields."
    - "Touched product title strings no longer market the frontend as an AI alert system."
    - "The frontend still completes a production build after AI cleanup."
  artifacts:
    - path: "frontend/src/api/client.ts"
      provides: "API wrapper set without aiApi"
    - path: "frontend/src/types/index.ts"
      provides: "Alert interface without ai_* fields"
    - path: "frontend/index.html"
      provides: "Browser title aligned to the non-AI product name"
  key_links:
    - from: "frontend/src/pages/Dashboard.tsx"
      to: "frontend/src/api/client.ts"
      via: "named API imports"
      pattern: "aiApi"
    - from: "frontend/src/components/AlertCard.tsx"
      to: "frontend/src/types/index.ts"
      via: "Alert interface fields"
      pattern: "ai_"
---

<objective>
Delete the remaining frontend AI contracts and run the reliable build gate so Phase 2 ends with a self-consistent non-AI frontend.

Purpose: Fulfill FEAI-03 and close the phase with a static regression check that catches orphaned imports, fields, or routes.
Output: AI-free API/type layer plus successful `pnpm build`.
</objective>

<execution_context>
@C:/Users/doumaoxin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/doumaoxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-remove-frontend-ai-surfaces/02-RESEARCH.md
@.planning/phases/02-remove-frontend-ai-surfaces/02-UI-SPEC.md
@frontend/src/api/client.ts
@frontend/src/types/index.ts
@frontend/src/pages/Login.tsx
@frontend/index.html
@frontend/package.json

<interfaces>
From frontend/src/types/index.ts:
```ts
export interface Alert {
  alert_id: string
  message: string
  ai_summary?: string
  ai_root_cause?: string
  ai_suggestions?: string[]
}
```

From frontend/src/api/client.ts:
```ts
export const aiApi = {
  chat: (...) => apiClient.post('/ai/chat', data),
  logs: (...) => apiClient.get('/ai/logs', { params }),
}
```

From frontend/package.json:
```json
{
  "scripts": {
    "build": "tsc && vite build",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove AI API wrappers and shared alert AI fields</name>
  <read_first>
    - frontend/src/api/client.ts
    - frontend/src/types/index.ts
    - frontend/src/components/AlertCard.tsx
    - frontend/src/pages/Dashboard.tsx
    - frontend/src/pages/Alerts.tsx
  </read_first>
  <files>frontend/src/api/client.ts, frontend/src/types/index.ts</files>
  <acceptance_criteria>
    - `frontend/src/api/client.ts` no longer exports `aiApi` or references `/ai/*` endpoints.
    - `frontend/src/types/index.ts` removes `ai_*` fields from `Alert`.
    - Repo-wide frontend grep shows no remaining `ai_summary`, `ai_root_cause`, `ai_suggestions`, `ai_tags`, `ai_severity`, or `/ai` endpoint calls.
  </acceptance_criteria>
  <action>Delete the `aiApi` group from `frontend/src/api/client.ts` and remove all AI-only fields from the shared `Alert` interface in `frontend/src/types/index.ts`. Before finalizing, grep the frontend source tree to confirm Plans 01 and 02 already removed the corresponding call sites and render usage; if any residual references remain, clean them inside the owning files only when they are direct blockers to FEAI-03. Do not add compatibility shims or placeholder fields, because the backend AI runtime is already gone and FEAI-03 requires full contract removal.</action>
  <verify>
    <automated>powershell -NoProfile -Command "rg -n --hidden --glob '!frontend/dist/**' 'aiApi|/ai/|ai_summary|ai_root_cause|ai_suggestions|ai_tags|ai_severity' frontend/src frontend/index.html; if ($LASTEXITCODE -eq 0) { exit 1 }"</automated>
  </verify>
  <done>The shared frontend API/type layer no longer defines or references AI-only contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Clean touched frontend branding strings and pass the build gate</name>
  <read_first>
    - frontend/src/pages/Login.tsx
    - frontend/index.html
    - frontend/package.json
    - .planning/phases/02-remove-frontend-ai-surfaces/02-UI-SPEC.md
  </read_first>
  <files>frontend/src/pages/Login.tsx, frontend/index.html</files>
  <acceptance_criteria>
    - Touched login/title strings no longer include `AI`.
    - No broad documentation or unrelated branding sweep is attempted in this phase.
    - `pnpm build` passes in `frontend/`.
  </acceptance_criteria>
  <action>Update only the frontend strings directly touched by Phase 2 cleanup so the login heading and browser title no longer describe the product as an AI alert system. Keep this scoped to `frontend/src/pages/Login.tsx` and `frontend/index.html`; broader documentation alignment belongs to Phase 3. After the string cleanup, run `pnpm build` in `frontend/` as the required automated regression gate. Do not require `pnpm lint` unless you explicitly choose to fix pre-existing lint failures, which is out of scope for this plan.</action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>The touched frontend copy is non-AI, and the full frontend production build succeeds after all Phase 2 removals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| typed frontend contracts -> page implementations | Removing shared fields/endpoints can break consumers if any stale reference survives |
| browser entry title/login screen -> user-visible product identity | Touched copy must reflect the actual non-AI product without widening scope into docs work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | D | `frontend/src/api/client.ts` | mitigate | Delete dead `/ai/*` wrappers and prove absence with repo-wide grep so the UI cannot call removed backend endpoints |
| T-02-07 | T | `frontend/src/types/index.ts` | mitigate | Remove AI-only fields from `Alert` only after Plans 01-02 clean the render call sites, then verify with `pnpm build` |
| T-02-08 | I | `frontend/index.html`, `frontend/src/pages/Login.tsx` | accept | Product title exposure is low-risk informational copy; mitigation is scoped string replacement, not a broader content sweep |
</threat_model>

<verification>
Primary regression gate: `pnpm build` in `frontend/`. This is the reliable automated proof for Phase 2 because lint currently has known pre-existing failures.
</verification>

<success_criteria>
The frontend codebase no longer defines AI endpoints or AI alert fields, touched titles are non-AI, and `frontend/` builds successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/02-remove-frontend-ai-surfaces/02-03-SUMMARY.md`
</output>
