---
phase: 11-restore-frontend-quality-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/pages/Alerts.tsx
  - frontend/src/pages/Channels.tsx
  - frontend/src/pages/DataSources.tsx
  - frontend/src/pages/Dashboard.tsx
  - frontend/src/pages/Login.tsx
  - frontend/src/pages/OnDuty.tsx
  - frontend/src/pages/RouteRules.tsx
  - frontend/src/pages/Silences.tsx
  - frontend/src/pages/Users.tsx
  - frontend/src/types/index.ts
autonomous: true
requirements:
  - FEQ-01
  - FEQ-02
user_setup: []
must_haves:
  truths:
    - "`pnpm lint` no longer fails on current frontend sources"
    - "High-noise hook dependency warnings are fixed by aligning with real dependencies rather than suppressing lint"
    - "Obvious unused variables and unbounded `any` usages in active pages are reduced to maintainable typed boundaries"
  artifacts:
    - path: "frontend/src/pages/DataSources.tsx"
      provides: "Removal of current lint errors and typed preview error handling"
      contains: "handlePreview"
    - path: "frontend/src/App.tsx"
      provides: "Permission shell cleanup without dead capability variables"
      contains: "buildMenuItems"
    - path: "frontend/src/types/index.ts"
      provides: "Narrowed shared frontend types for active flows"
      contains: "DataSourcePreviewRequest"
  key_links:
    - from: "frontend/src/pages/Alerts.tsx"
      to: "frontend/src/stores/alertStore.ts"
      via: "stable fetch action used by effect dependencies"
      pattern: "fetchAlerts"
    - from: "frontend/src/pages/RouteRules.tsx"
      to: "frontend/src/stores/configStore.ts"
      via: "config page initialization actions"
      pattern: "fetchRouteRules"
    - from: "frontend/src/pages/DataSources.tsx"
      to: "frontend/src/types/index.ts"
      via: "preview payload and response typing"
      pattern: "DataSourcePreview"
---

<objective>
Restore the frontend lint baseline without expanding scope into feature work.

Purpose: Remove the current ESLint failure, eliminate the highest-value warning debt in active pages, and leave the React/Zustand codebase in a quieter, more maintainable state for later CI enforcement.
Output: Frontend source changes that make `pnpm lint` pass and reduce hook / unused-variable / `any` noise in the current phase scope.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-restore-frontend-quality-baseline/11-CONTEXT.md
@frontend/.eslintrc.cjs
@frontend/package.json
@frontend/src/App.tsx
@frontend/src/pages/Alerts.tsx
@frontend/src/pages/Channels.tsx
@frontend/src/pages/DataSources.tsx
@frontend/src/pages/Dashboard.tsx
@frontend/src/pages/Login.tsx
@frontend/src/pages/OnDuty.tsx
@frontend/src/pages/RouteRules.tsx
@frontend/src/pages/Silences.tsx
@frontend/src/pages/Users.tsx
@frontend/src/types/index.ts
@frontend/src/stores/alertStore.ts
@frontend/src/stores/configStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove current lint-blocking errors and warning debt in active frontend pages</name>
  <files>frontend/src/App.tsx, frontend/src/pages/Alerts.tsx, frontend/src/pages/Channels.tsx, frontend/src/pages/DataSources.tsx, frontend/src/pages/Dashboard.tsx, frontend/src/pages/Login.tsx, frontend/src/pages/OnDuty.tsx, frontend/src/pages/RouteRules.tsx, frontend/src/pages/Silences.tsx, frontend/src/pages/Users.tsx, frontend/src/types/index.ts</files>
  <read_first>frontend/.eslintrc.cjs, frontend/package.json, frontend/src/App.tsx, frontend/src/pages/DataSources.tsx, frontend/src/pages/RouteRules.tsx, frontend/src/stores/alertStore.ts, frontend/src/stores/configStore.ts, .planning/phases/11-restore-frontend-quality-baseline/11-CONTEXT.md</read_first>
  <action>Fix the existing frontend lint findings in place without changing user-facing product scope. Remove dead imports and unused capability variables from the app shell and route/config pages. Resolve `react-hooks/exhaustive-deps` warnings by wiring effects to the real Zustand actions they use, preferring minimal dependency-safe adjustments over lint suppression. Narrow obvious `any` usages in active pages and shared types to `unknown`, table render argument types, or existing domain types where the code already knows the shape. In `frontend/src/pages/DataSources.tsx`, eliminate the current `no-useless-escape` lint errors and leave the template help text semantically unchanged.</action>
  <verify>pnpm lint</verify>
  <acceptance_criteria>
    - `pnpm lint` exits 0
    - frontend/src/pages/DataSources.tsx no longer contains the current useless-escape failure
    - frontend/src/App.tsx no longer keeps unused capability imports
    - active pages no longer rely on empty dependency arrays where the effect uses fetched actions
  </acceptance_criteria>
  <done>The frontend codebase has no remaining lint failures and the current warning debt covered by Phase 11 is materially reduced.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint` passes
- [ ] Hook dependency warnings in active pages are resolved by code changes, not eslint suppression
- [ ] Unused variable and obvious `any` noise in phase scope are reduced without changing product behavior
</verification>

<success_criteria>

- All tasks completed
- Frontend lint is green in the default project environment
- The highest-noise warning debt in active pages has been cleaned up
- Phase 12 can consume this baseline directly for CI gate work

</success_criteria>

<output>
After completion, create `.planning/phases/11-restore-frontend-quality-baseline/11-01-SUMMARY.md`
</output>
