---
phase: 04-enable-raw-event-passthrough-in-notification-templates
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - internal/handlers/config.go
  - internal/router/router.go
  - frontend/src/api/client.ts
  - frontend/src/stores/configStore.ts
  - frontend/src/pages/DataSources.tsx
autonomous: true
requirements:
  - TMPL-02
must_haves:
  truths:
    - A user editing a data source can see how to reference standard fields and raw-event fields without reverse-engineering backend structs.
    - A user can run a template preview against sample webhook JSON before saving.
    - The preview path uses the same render contract as the live notification flow.
  artifacts:
    - path: internal/handlers/config.go
      provides: Authenticated datasource preview endpoint using the backend render contract.
    - path: internal/router/router.go
      provides: Datasource preview route wiring.
    - path: frontend/src/pages/DataSources.tsx
      provides: Template guidance plus preview UI for sample payloads and rendered output.
  key_links:
    - from: frontend/src/pages/DataSources.tsx
      to: frontend/src/api/client.ts
      via: datasource preview request
      pattern: "dataSourceApi\\."
    - from: internal/router/router.go
      to: internal/handlers/config.go
      via: authenticated datasource preview endpoint
      pattern: "datasources\\.POST"
---

<objective>
Make the new output-template contract usable from the product by adding a first-class preview/testing path and clear in-UI guidance.

Purpose: satisfy TMPL-02 so raw-event passthrough is discoverable and safe to adopt, not just hidden backend capability.
Output: datasource preview endpoint wiring plus frontend template ergonomics in the datasource editor/test flow.
</objective>

<execution_context>
@C:/Users/doumaoxin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/doumaoxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-enable-raw-event-passthrough-in-notification-templates/04-CONTEXT.md
@internal/handlers/config.go
@internal/router/router.go
@frontend/src/api/client.ts
@frontend/src/stores/configStore.ts
@frontend/src/pages/DataSources.tsx

<interfaces>
From `frontend/src/api/client.ts`:

```ts
export const dataSourceApi = {
  list: () => apiClient.get<DataSource[]>('/datasources'),
  get: (id: number) => apiClient.get<DataSource>(`/datasources/${id}`),
  create: (data: Partial<DataSource>) => apiClient.post<DataSource>('/datasources', data),
  update: (id: number, data: Partial<DataSource>) => apiClient.put<DataSource>(`/datasources/${id}`, data),
}
```

From `internal/router/router.go`:

```go
datasources := v1.Group("/datasources")
datasources.Use(middleware.JWTAuth(jwtAuth))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an authenticated datasource output-preview endpoint</name>
  <read_first>
    - internal/handlers/config.go
    - internal/router/router.go
    - internal/handlers/webhook.go
  </read_first>
  <files>internal/handlers/config.go, internal/router/router.go</files>
  <behavior>
    - Preview request with sample webhook JSON returns rendered title/content using the same output-template context as live notifications.
    - Preview request can use the datasource's saved templates while allowing the editor to send in-progress template text before save.
    - Preview errors return actionable JSON errors instead of silent failures.
  </behavior>
  <action>Add a protected datasource preview/test endpoint in `internal/router/router.go` and implement it in `internal/handlers/config.go`. The endpoint should accept datasource id plus sample webhook JSON, use the datasource `input_template` to normalize the sample into an `Alert`, then render the datasource `output_template` through the Phase 04 backend contract from `webhook.go`. Allow optional override strings for in-editor `input_template` and `output_template` so users can preview unsaved changes. Return structured JSON that includes at least the normalized alert payload, rendered notification title/content, and a concise context preview or top-level key list to reinforce the naming contract per TMPL-02. Do not introduce a separate rendering pipeline; call shared helper(s) from `webhook.go` so preview and live send stay in sync.</action>
  <verify>
    <automated>go test ./internal/handlers ./internal/router -run "TestWebhook|TestRouter" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The datasource route table includes one authenticated preview endpoint for template debugging.
    - Preview output is produced by the same contract as live notification rendering.
    - Error responses explain parse/template failures clearly enough for the UI to display them.
  </acceptance_criteria>
  <done>Backend preview/testing support exists for datasource templates and is wired through the authenticated config API surface.</done>
</task>

<task type="auto">
  <name>Task 2: Turn the datasource editor into a user-friendly template guide and preview flow</name>
  <read_first>
    - frontend/src/pages/DataSources.tsx
    - frontend/src/api/client.ts
    - frontend/src/stores/configStore.ts
    - .planning/phases/04-enable-raw-event-passthrough-in-notification-templates/04-CONTEXT.md
  </read_first>
  <files>frontend/src/api/client.ts, frontend/src/stores/configStore.ts, frontend/src/pages/DataSources.tsx</files>
  <action>Wire the new datasource preview endpoint into `frontend/src/api/client.ts` and `frontend/src/stores/configStore.ts`, then rework `frontend/src/pages/DataSources.tsx` so the existing template test drawer becomes a real notification-template preview tool. The UI should explain the stable naming contract explicitly per TMPL-02: standard top-level fields remain available, and raw event data is available through the chosen top-level raw variable from Plan 01. Include short inline examples for common nested fields like `event.annotations.runbook` or equivalent map-safe usage, a sample JSON editor, and a rendered preview result showing normalized alert plus final title/content. Keep the implementation within existing Ant Design patterns; do not broaden this into unrelated datasource UX cleanup.</action>
  <verify>
    <automated>pnpm.cmd --dir frontend build</automated>
  </verify>
  <acceptance_criteria>
    - The datasource page exposes concrete template-variable guidance instead of requiring users to infer backend field names.
    - Users can preview rendered notification output from sample webhook JSON before saving.
    - The frontend build passes without broken datasource imports, routes, or API contracts.
  </acceptance_criteria>
  <done>The datasource editor teaches and previews the new contract in-product, making raw passthrough practically usable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated operator -> preview endpoint | User-provided sample JSON and unsaved templates are executed through the backend preview path |
| preview API -> browser UI | Template errors and rendered content are returned to the editor for display |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | D | datasource preview endpoint | mitigate | Reuse existing template helpers and return bounded structured output instead of arbitrary execution paths or extra side effects |
| T-04-05 | I | preview response payload | accept | Endpoint is JWT-protected config tooling for operators; returning normalized alert/context preview is required for template authoring |
| T-04-06 | T | frontend datasource editor | mitigate | Keep preview wired to the shared backend contract so docs/examples cannot drift from live notification rendering |
</threat_model>

<verification>
Run `go test ./internal/handlers ./internal/router -run "TestWebhook|TestRouter" -count=1` for backend wiring and `pnpm.cmd --dir frontend build` for frontend contract integrity.
</verification>

<success_criteria>
- Operators have one authenticated preview workflow for datasource templates.
- The datasource editor documents the stable standard-field/raw-event naming contract.
- Preview uses shared backend rendering logic rather than a duplicated frontend-only approximation.
</success_criteria>

<output>
After completion, create `.planning/phases/04-enable-raw-event-passthrough-in-notification-templates/04-02-SUMMARY.md`
</output>
