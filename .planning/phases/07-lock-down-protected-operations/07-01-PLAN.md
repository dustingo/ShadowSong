---
phase: 07-lock-down-protected-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/router/router.go
  - internal/router/router_test.go
  - internal/middleware/authorize.go
  - internal/authz/capabilities.go
  - internal/authz/capabilities_test.go
autonomous: true
requirements: [PERM-01, PERM-02, PERM-03, PERM-04]
must_haves:
  truths:
    - "只有 admin 可以执行配置类写操作。"
    - "operator 可以确认和快速静默告警，但 viewer 不可以。"
    - "未授权请求在后端得到明确的 403，而不是仅凭登录状态放行。"
  artifacts:
    - path: "internal/router/router.go"
      provides: "alert actions and config writes wired to explicit capability guards"
    - path: "internal/router/router_test.go"
      provides: "HTTP allow/deny matrix for protected operations"
  key_links:
    - from: "internal/router/router.go"
      to: "internal/authz/capabilities.go"
      via: "RequireCapability(authz.CapabilityProcessAlerts|CapabilityManageConfig|CapabilityViewConfig)"
      pattern: "RequireCapability"
---

<objective>
将告警动作和配置接口从“只要登录即可”收口到明确的 capability matrix 上，建立 Phase 7 的后端权限面。

Purpose: 先把真实的访问边界立住，避免后续审计日志只记录了一个本来就不该被放行的动作。
Output: 路由 capability 收口、角色矩阵测试、明确的 401/403 拒绝结果。
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire alert-action endpoints to process-alert capability and keep read routes readable</name>
  <files>internal/router/router.go, internal/router/router_test.go</files>
  <read_first>internal/router/router.go, internal/router/router_test.go, internal/middleware/authorize.go, internal/authz/capabilities.go, internal/handlers/alert.go</read_first>
  <action>Update `internal/router/router.go` so alert read routes (`GET /alerts`, `/alerts/stats`, `/alerts/active`, `/alerts/:id`) stay available to authenticated roles with alert-view ability, while `POST /alerts/:id/ack` and `POST /alerts/:id/quick-silence` require `middleware.RequireCapability(authz.CapabilityProcessAlerts)`. Add router tests that prove `admin` and `operator` receive the current handler result on these action endpoints, while `viewer` receives `403 {"error":"insufficient permissions"}` and unauthenticated callers receive `401`.</action>
  <acceptance_criteria>`internal/router/router.go` contains `RequireCapability(authz.CapabilityProcessAlerts)` on both alert action endpoints; `internal/router/router_test.go` asserts viewer-denied and operator-allowed outcomes for `ack` and `quick-silence`; unauthorized requests still return `401`.</acceptance_criteria>
  <verify>
    <automated>go test ./internal/router ./internal/middleware ./internal/authz -run "Test(Router|Capability)" -count=1 -timeout 60s</automated>
  </verify>
  <done>Alert actions are no longer available to viewers, and the HTTP-level behavior is locked by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restrict configuration mutations to manage-config capability without breaking read access</name>
  <files>internal/router/router.go, internal/router/router_test.go, internal/authz/capabilities.go, internal/authz/capabilities_test.go</files>
  <read_first>internal/router/router.go, internal/router/router_test.go, internal/authz/capabilities.go, internal/authz/capabilities_test.go, internal/handlers/config.go, .planning/REQUIREMENTS.md</read_first>
  <action>Use the existing config route groups in `internal/router/router.go` to differentiate reads from writes. Keep list/get style routes readable for `admin`, `operator`, and `viewer` via existing authenticated access and `CapabilityViewConfig` semantics where needed. Protect every mutating config route with `middleware.RequireCapability(authz.CapabilityManageConfig)`, including datasource/channel/route-rule/silence-rule/on-duty create, update, delete, reorder, toggle, and test operations. Extend `internal/router/router_test.go` and `internal/authz/capabilities_test.go` so the capability matrix explicitly proves `operator` and `viewer` cannot mutate config while `admin` can.</action>
  <acceptance_criteria>`internal/router/router.go` applies `RequireCapability(authz.CapabilityManageConfig)` to all config write endpoints; `internal/authz/capabilities_test.go` still shows only `admin` can `manage_config`; router tests assert `403` for non-admin config writes and non-403 for admin writes on representative datasource/channel/silence/on-duty endpoints.</acceptance_criteria>
  <verify>
    <automated>go test ./internal/router ./internal/authz -run "Test(Router|Capability)" -count=1 -timeout 60s</automated>
  </verify>
  <done>Configuration write access is strictly admin-only at the router boundary, matching Phase 7 requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated principal -> protected routes | Logged-in users may still attempt actions outside their role |
| route guard -> handler logic | Handler behavior must only execute after the correct capability check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | E | alert action routes | mitigate | Require `CapabilityProcessAlerts` for ack/quick-silence |
| T-07-02 | E | config mutation routes | mitigate | Require `CapabilityManageConfig` on every write endpoint |
| T-07-03 | R | inconsistent deny behavior | mitigate | Lock 401/403 responses with router tests |
</threat_model>

<verification>
- `go test ./internal/router ./internal/middleware ./internal/authz -run "Test(Router|Capability)" -count=1 -timeout 60s`
- `rg -n "CapabilityProcessAlerts|CapabilityManageConfig" internal/router/router.go`
</verification>

<success_criteria>
- Alert actions are limited to roles with process-alert ability.
- Config writes are limited to `admin`.
- Viewer remains read-only at the backend permission surface.
</success_criteria>

<output>
After completion, create `.planning/phases/07-lock-down-protected-operations/07-01-SUMMARY.md`
</output>
