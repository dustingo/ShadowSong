---
phase: 08-ship-permission-aware-ui-and-verification
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - frontend/src/pages/Alerts.tsx
  - frontend/src/pages/DataSources.tsx
  - frontend/src/pages/Channels.tsx
  - frontend/src/pages/RouteRules.tsx
  - frontend/src/pages/Silences.tsx
  - frontend/src/pages/OnDuty.tsx
  - frontend/src/pages/Users.tsx
  - frontend/src/pages/Profile.tsx
  - frontend/src/stores/configStore.ts
  - frontend/src/stores/alertStore.ts
autonomous: true
requirements: [FEACL-01, FEACL-02, FEACL-03, VER-04]
must_haves:
  truths:
    - "viewer 看得到告警和配置页面，但不会再看到确认、快速静默、创建、编辑、删除、启停、测试等无权操作入口。"
    - "operator 在告警页能处理告警，但在配置页只读，并能看到清晰的一致化提示。"
    - "admin 保持现有配置和用户管理操作面，不因为权限显隐重构而丢失入口。"
  artifacts:
    - path: "frontend/src/pages/Alerts.tsx"
      provides: "alert action buttons pruned by capability"
    - path: "frontend/src/pages/DataSources.tsx"
      provides: "read-only config page behavior for non-admin roles"
    - path: "frontend/src/pages/Users.tsx"
      provides: "admin-only user-management surface with consistent forbidden feedback"
  key_links:
    - from: "frontend/src/pages/Alerts.tsx"
      to: "frontend/src/authz/capabilities.ts"
      via: "canProcessAlerts gating for ack and quick silence"
      pattern: "process_alerts"
    - from: "frontend/src/pages/DataSources.tsx"
      to: "frontend/src/components/PermissionNotice.tsx"
      via: "shared read-only notice"
      pattern: "PermissionNotice"
---

<objective>
把 Phase 8 的权限感知真正落到页面内部：同一路由下，不同角色只能看到或触达自己有权的按钮、表单和反馈。

Purpose: 单靠壳层和路由不足以完成最小权限体验，页面内部动作必须和 Phase 7 的后端能力边界一致。
Output: alert/config/user pages 的显隐收口、只读提示、统一 forbidden 反馈。
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prune alert-action UI so only processing roles can acknowledge or quick-silence</name>
  <files>frontend/src/pages/Alerts.tsx, frontend/src/stores/alertStore.ts</files>
  <read_first>frontend/src/pages/Alerts.tsx, frontend/src/stores/alertStore.ts, frontend/src/authz/capabilities.ts, frontend/src/components/PermissionNotice.tsx, internal/router/router.go</read_first>
  <action>Update `frontend/src/pages/Alerts.tsx` so the `确认` and `静默` action buttons only render for users who have `process_alerts` and the alert is in `firing` state. When the current role lacks that capability, keep the table readable but show a compact read-only or permission note rather than empty confusing whitespace. Ensure modal entrypoints for ack and quick silence cannot be opened by `viewer`, and normalize failed `403` responses through the same user-facing copy. In `frontend/src/stores/alertStore.ts`, preserve existing fetch behavior but let ack and quick-silence callers surface backend `403` messages without silently replacing them with a generic failure string.</action>
  <acceptance_criteria>`frontend/src/pages/Alerts.tsx` references the shared capability helper and does not render ack/quick-silence buttons for `viewer`; the file contains the read-only or forbidden guidance copy; `frontend/src/stores/alertStore.ts` still exports `ackAlert` and `quickSilence` but does not discard backend `403` messages.</acceptance_criteria>
  <verify>
    <automated>cd frontend && pnpm build</automated>
    <automated>rg -n "ack|quickSilence|process_alerts|PermissionNotice|无权|只读" frontend/src/pages/Alerts.tsx frontend/src/stores/alertStore.ts</automated>
  </verify>
  <done>Alert processing UI now matches the backend `process_alerts` boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert config and user-management pages to permission-aware read-only/admin-only behavior</name>
  <files>frontend/src/pages/DataSources.tsx, frontend/src/pages/Channels.tsx, frontend/src/pages/RouteRules.tsx, frontend/src/pages/Silences.tsx, frontend/src/pages/OnDuty.tsx, frontend/src/pages/Users.tsx, frontend/src/pages/Profile.tsx, frontend/src/stores/configStore.ts</files>
  <read_first>frontend/src/pages/DataSources.tsx, frontend/src/pages/Channels.tsx, frontend/src/pages/RouteRules.tsx, frontend/src/pages/Silences.tsx, frontend/src/pages/OnDuty.tsx, frontend/src/pages/Users.tsx, frontend/src/pages/Profile.tsx, frontend/src/stores/configStore.ts, frontend/src/authz/capabilities.ts, frontend/src/components/PermissionNotice.tsx, .planning/REQUIREMENTS.md</read_first>
  <action>For `frontend/src/pages/DataSources.tsx`, `Channels.tsx`, `RouteRules.tsx`, `Silences.tsx`, and `OnDuty.tsx`, keep list/detail viewing available to roles with `view_config`, but hide or disable every write control that maps to backend `manage_config`: create buttons, edit buttons, delete buttons, enable/disable toggles, test channel actions, reorder actions, save modal footers, and any create-from-alert entrypoint. Render a shared read-only notice near the top of each config page for `operator` and `viewer`, with copy equivalent to `当前角色可查看配置，但不能修改`. In `frontend/src/pages/Users.tsx`, keep the page admin-only and add a direct forbidden or redirect-safe fallback if the page is ever mounted without `manage_users`. In `frontend/src/pages/Profile.tsx`, preserve self-service editing and forced-reset copy without exposing admin-only fields. In `frontend/src/stores/configStore.ts`, keep the same API methods but avoid burying permission errors so pages can show consistent messages when backend `403` still occurs.</action>
  <acceptance_criteria>Each config page file contains either the shared permission notice or capability gating around create/edit/delete/toggle/test controls; non-admin roles no longer render config write buttons; `frontend/src/pages/Users.tsx` still requires admin intent and contains explicit forbidden handling; `frontend/src/pages/Profile.tsx` remains self-service only; `frontend/src/stores/configStore.ts` does not swallow `403` details.</acceptance_criteria>
  <verify>
    <automated>cd frontend && pnpm build</automated>
    <automated>rg -n "PermissionNotice|manage_config|当前角色可查看配置|新建|编辑|删除|禁用|启用|测试" frontend/src/pages/DataSources.tsx frontend/src/pages/Channels.tsx frontend/src/pages/RouteRules.tsx frontend/src/pages/Silences.tsx frontend/src/pages/OnDuty.tsx frontend/src/pages/Users.tsx</automated>
  </verify>
  <done>Config and user-management pages now present a coherent role-aware UI instead of relying solely on backend rejection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route-level access -> page-level actions | A readable route can still expose unsafe buttons if action-level gating is missing |
| backend 403 -> page feedback | Permission-denied calls must become consistent UX instead of generic failure toasts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | E | alert actions in `Alerts.tsx` | mitigate | Hide processing controls for roles lacking `process_alerts` |
| T-08-05 | E | config write buttons in config pages | mitigate | Hide or disable all `manage_config` actions and show read-only notice |
| T-08-06 | R | ambiguous page-level denial feedback | mitigate | Preserve backend `403` details and normalize the UI copy across pages |
</threat_model>

<verification>
- `cd frontend && pnpm build`
- `rg -n "PermissionNotice|manage_config|process_alerts|当前角色可查看配置|当前角色无权执行该操作" frontend/src/pages frontend/src/stores`
</verification>

<success_criteria>
- Page-level buttons and forms match the backend role matrix.
- Read-only users get explicit guidance instead of silent no-ops.
- Admin surfaces remain intact while operator/viewer surfaces stop advertising forbidden actions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ship-permission-aware-ui-and-verification/08-02-SUMMARY.md`
</output>
