---
phase: 15-harden-notification-retry-boundaries
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - internal/handlers/webhook_test.go
  - internal/notifier/notifier_test.go
  - .planning/phases/15-harden-notification-retry-boundaries/15-VERIFICATION.md
autonomous: true
requirements:
  - NTFY-02
  - NTFY-03
requirements_addressed:
  - NTFY-02
  - NTFY-03
must_haves:
  truths:
    - "重试耗尽后会留下明确的最终失败日志落点，而不是只有零散 attempt 报错。"
    - "验证产物覆盖首次成功、重试后成功、重试耗尽三类行为，并说明最终失败只落日志。"
    - "值班人员可以从最终失败日志回查到 Phase 14 的 trace/lifecycle 证据。"
  artifacts:
    - path: internal/handlers/webhook_test.go
      provides: "终态失败日志与诊断回查测试"
    - path: internal/notifier/notifier_test.go
      provides: "错误分类和重试边界补充回归测试"
    - path: .planning/phases/15-harden-notification-retry-boundaries/15-VERIFICATION.md
      provides: "Phase 15 行为验证与日志诊断说明"
  key_links:
    - from: internal/handlers/webhook_test.go
      to: internal/handlers/webhook.go
      via: "retry exhausted / terminal failure assertions"
      pattern: "terminal|final|max_attempts|attempt"
    - from: .planning/phases/15-harden-notification-retry-boundaries/15-VERIFICATION.md
      to: .planning/phases/14-establish-alert-trace-context/14-VERIFICATION.md
      via: "trace-based troubleshooting narrative"
      pattern: "trace_id"
    - from: internal/notifier/notifier_test.go
      to: internal/notifier/notifier.go
      via: "retryability regression coverage"
      pattern: "Retryable|retryable"
---

<objective>
验证 Phase 15 的重试边界、最终失败落点和诊断路径，确保“可恢复”和“最终失败只落日志”的约束都被自动化与文档真相锁定。

Purpose: 按 D-03、D-04、D-07、D-09 和 roadmap，把 Phase 15 自然拆成实现后验证/诊断 plan，避免重试逻辑上线后又回到只能靠读源码判断行为的状态。
Output: 终态失败与尝试日志的自动化测试、Phase 15 验证报告、以及覆盖首发成功/重试成功/重试耗尽的回归命令。
</objective>

<execution_context>
@C:/Users/doumaoxin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/doumaoxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-harden-notification-retry-boundaries/15-CONTEXT.md
@.planning/phases/14-establish-alert-trace-context/14-VERIFICATION.md
@.planning/phases/15-harden-notification-retry-boundaries/15-01-PLAN.md
@internal/handlers/webhook.go
@internal/handlers/webhook_test.go
@internal/notifier/notifier.go
@internal/notifier/notifier_test.go

<interfaces>
From expected `15-01` implementation:
```go
func (h *WebhookHandler) sendNotification(alert *models.Alert, channel *models.Channel)
func SendToChannel(channel *models.Channel, title, content string) error
```

Expected terminal evidence contract for D-03/D-04:
```text
stage or final-failure marker
trace_id
alert_id
channel_id
attempt
max_attempts
error
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock terminal failure behavior and attempt diagnostics with focused regression tests</name>
  <read_first>
    .planning/phases/15-harden-notification-retry-boundaries/15-01-PLAN.md
    internal/handlers/webhook.go
    internal/handlers/webhook_test.go
    internal/notifier/notifier.go
    internal/notifier/notifier_test.go
  </read_first>
  <files>internal/handlers/webhook_test.go, internal/notifier/notifier_test.go</files>
  <behavior>
    - Test first-attempt success emits no extra retries.
    - Test retry exhaustion exercises the final failure log implemented by `15-01` and asserts it includes trace_id, alert_id, channel_id, attempt, max_attempts, and error.
    - Test retry exhaustion does not create durable delivery records or any new persistence side effect.
  </behavior>
  <action>Per D-03, D-04, D-07, D-09 and NTFY-02/NTFY-03, extend the focused Go tests so Phase 15 is locked by behavior rather than implementation detail. Treat the final-failure behavior as implementation owned by `15-01`, and in this plan only verify it. Add one handler test for immediate success, one for transient failure then success, and one for retry exhaustion that asserts both attempt logs and the explicit terminal failure log already implemented in code. Add any notifier regression test still needed to prove unsupported/config errors stay terminal and never consume multiple attempts. Keep all tests deterministic with existing seams; do not depend on live HTTP endpoints or sleeps that make CI flaky.</action>
  <verify>
    <automated>go test ./internal/handlers -run "TestWebhookHandler(.*Retry.*|.*Terminal.*|.*Success.*)" -count=1 && go test ./internal/notifier -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Terminal|Final|RetryExhaust|ImmediateSuccess|RetrySuccess" internal/handlers/webhook_test.go` finds explicit scenario tests for the three roadmap cases.
    - At least one handler test asserts the final failure log includes `trace_id`, `alert_id`, `channel_id`, `attempt`, `max_attempts`, and `error`.
    - At least one test proves a non-retryable error still results in a single send attempt.
    - No new database model, migration, Redis stream, or durable failure artifact is introduced by the verification work.
  </acceptance_criteria>
  <done>Phase 15 behavior is pinned by automated tests that distinguish immediate success, retry-assisted success, and retry-exhausted terminal failure without adding new persistence surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Record Phase 15 verification truth and operator-facing diagnostics</name>
  <read_first>
    .planning/phases/15-harden-notification-retry-boundaries/15-CONTEXT.md
    .planning/phases/14-establish-alert-trace-context/14-VERIFICATION.md
    internal/handlers/webhook_test.go
    internal/notifier/notifier_test.go
    internal/handlers/webhook.go
  </read_first>
  <files>.planning/phases/15-harden-notification-retry-boundaries/15-VERIFICATION.md</files>
  <action>Create `15-VERIFICATION.md` as the phase truth document after tests are green. Document the exact commands used, the three required scenarios from the roadmap, and the diagnostic rule that final failure lands in logs only per D-03, with example field names aligned to the minimum attempt contract from D-07. Explicitly connect the terminal failure diagnosis path back to Phase 14 trace/lifecycle evidence so an operator can move from a final failure log to earlier `notification_entry`, `route_match`, and ingest/persist stages. Do not claim any queueing, durable delivery records, frontend surfacing, or per-channel policy support.</action>
  <verify>
    <automated>go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/15-harden-notification-retry-boundaries/15-VERIFICATION.md` exists and names the three scenarios: first-attempt success, retry success, retry exhausted.
    - The verification doc states clearly that terminal failure lands in logs only and names the minimum final-failure fields.
    - The verification doc references Phase 14 trace-based observability as the upstream troubleshooting path.
    - The final automated command for the plan is a backend regression command, not a manual-only check.
  </acceptance_criteria>
  <done>The phase has a reusable verification record that explains how to prove retry behavior and how to diagnose a terminal failure using Phase 14 trace context, while staying within current stack and scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retry loop outcome -> terminal failure log | Runtime behavior is summarized into the only allowed durable evidence surface for this phase. |
| automated tests -> roadmap truth | Verification artifacts define the contract future changes must preserve. |
| Phase 15 verification -> Phase 14 trace evidence | Operators depend on cross-phase log linkage for diagnosis. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | R | terminal failure diagnosis | mitigate | Require explicit retry-exhausted tests and a verification doc that records the final-failure log contract per D-03/D-04. |
| T-15-07 | T | verification drift | mitigate | Use focused automated tests plus `go test ./... -count=1` so the contract is enforced by CI-visible commands, not prose alone. |
| T-15-08 | I | verification examples | mitigate | Document only bounded field names and troubleshooting steps; do not paste raw payload bodies or secrets into verification examples. |
| T-15-09 | D | retry-scope creep | mitigate | Verification must explicitly state that no durable delivery record, queue, or per-channel config was added, preventing later misreadings of phase scope. |
| T-15-10 | S | cross-phase trace correlation | mitigate | Reference Phase 14 `trace_id` lifecycle path directly so operators can trust one correlation field across notification-entry and final-failure logs. |
</threat_model>

<verification>
Run focused handler/notifier retry regression first, then `go test ./... -count=1`. Only write `15-VERIFICATION.md` after the commands pass so the document reflects executed behavior instead of intended behavior.
</verification>

<success_criteria>
`NTFY-02` and the verification side of `NTFY-03` are satisfied when retry exhaustion produces an explicit final failure log, the three roadmap scenarios are automated and documented, and the diagnostic path back to Phase 14 trace evidence is spelled out without introducing any new durable failure storage.
</success_criteria>

<output>
After completion, create `.planning/phases/15-harden-notification-retry-boundaries/15-02-SUMMARY.md`
</output>
