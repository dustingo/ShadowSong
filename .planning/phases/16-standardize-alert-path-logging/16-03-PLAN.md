---
phase: 16-standardize-alert-path-logging
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/handlers/webhook.go
  - internal/handlers/webhook_test.go
autonomous: true
gap_closure: true
requirements:
  - OBS-03
must_haves:
  truths:
    - "异步通知 panic 日志仍沿用现有 `stage=async_panic`，但必须保留 `trace_id`、`alert_id`、`fingerprint`、`source` 以及可用的 channel 字段，值班人员能从失败点回查到 webhook ingest。"
    - "Phase 13/14/15 已建立的 `trace_id`、`send_attempt`、`terminal_failure` 和现有 stage taxonomy 继续可搜索，不因 gap 修复而改名或断链。"
    - "修复范围限定在 `WebhookHandler` 的异步通知失败路径，不扩展成新的日志框架或 repo-wide logging cleanup。"
  artifacts:
    - path: internal/handlers/webhook.go
      provides: "带告警/渠道上下文的 async panic 记录逻辑"
    - path: internal/handlers/webhook_test.go
      provides: "async panic 相关字段回归测试"
  key_links:
    - from: internal/handlers/webhook.go
      to: internal/handlers/webhook.go
      via: "processAlertNotificationsAsync panic recovery -> canonical writer"
      pattern: "processAlertNotificationsAsync|async_panic|logNotification|logAlertEvent"
    - from: internal/handlers/webhook_test.go
      to: internal/handlers/webhook.go
      via: "panic regression asserts correlation fields on emitted log line"
      pattern: "async_panic|trace_id|alert_id|channel_id"
---

<objective>
关闭 Phase 16 的 `OBS-03` / `T-16-01` blocker，让 `async_panic` 失败路径与其他通知日志一样保留完整关联字段。

Purpose: 当前 `processAlertNotificationsAsync` 在 panic recovery 时把 `alert` 和 `channel` 作为 `nil` 传给 canonical writer，导致失败通知无法按 `trace_id` 与 `alert_id` 回溯到 webhook 主链路。
Output: 带上下文的 `async_panic` 日志实现，以及锁定该行为的 handler regression tests。
</objective>

<execution_context>
@C:/Users/doumaoxin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/doumaoxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-standardize-alert-path-logging/16-VERIFICATION.md
@.planning/phases/16-standardize-alert-path-logging/16-SECURITY.md
@.planning/phases/16-standardize-alert-path-logging/16-01-SUMMARY.md
@.planning/phases/16-standardize-alert-path-logging/16-02-SUMMARY.md
@.planning/phases/13-harden-notification-delivery-path/13-VERIFICATION.md
@.planning/phases/14-establish-alert-trace-context/14-VERIFICATION.md
@.planning/phases/15-harden-notification-retry-boundaries/15-VERIFICATION.md
@internal/handlers/webhook.go
@internal/handlers/webhook_test.go

<interfaces>
From `internal/handlers/webhook.go`:
```go
func (h *WebhookHandler) processAlertNotificationsAsync(alerts []models.Alert)
func (h *WebhookHandler) logAlertEvent(stage string, fields map[string]string, format string, args ...interface{})
func (h *WebhookHandler) logNotification(stage string, alert *models.Alert, channel *models.Channel, format string, args ...interface{})
func (h *WebhookHandler) baseAlertLogFields(alert *models.Alert, channel *models.Channel) map[string]string
```

Current failing site:
```go
h.logNotification("async_panic", nil, nil, "recovered panic=%v stack=%s", r, string(debug.Stack()))
```

Existing correlation vocabulary that must survive unchanged:
```text
stage
trace_id
alert_id
fingerprint
source
channel_id
channel_name
channel_type
async_panic
send_attempt
terminal_failure
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve alert and channel context on async panic recovery</name>
  <read_first>
    internal/handlers/webhook.go
    .planning/phases/16-standardize-alert-path-logging/16-VERIFICATION.md
    .planning/phases/16-standardize-alert-path-logging/16-SECURITY.md
    .planning/phases/13-harden-notification-delivery-path/13-VERIFICATION.md
    .planning/phases/14-establish-alert-trace-context/14-VERIFICATION.md
  </read_first>
  <files>internal/handlers/webhook.go</files>
  <behavior>
    - A recovered panic in `processAlertNotificationsAsync` logs `stage=async_panic` without losing the current alert correlation envelope.
    - When the panic happens while processing a concrete channel, the emitted line also keeps `channel_id`, `channel_name`, and `channel_type`.
    - The fix stays inside `WebhookHandler` and reuses the canonical writer per the existing Phase 16 decision, without introducing a new logger package or changing stage names.
  </behavior>
  <action>Refactor `processAlertNotificationsAsync` so the panic recovery closure has access to the currently processed alert and, when available, the current channel being sent. Pass that context into the existing canonical writer for `async_panic`, preserving `stage=async_panic` and the current message intent while satisfying `OBS-03` and threat `T-16-01`. Do not paper over the gap by copying identifiers into only the free-text message; the correlation data must be emitted through the structured field envelope. Keep this scoped to the async panic path in `internal/handlers/webhook.go` and do not modify unrelated logging sites or packages.</action>
  <verify>
    <automated>go test ./internal/handlers -run "TestWebhookHandler(.*Panic.*|.*Trace.*)" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "async_panic" internal/handlers/webhook.go` shows the panic recovery site no longer passes `nil, nil` into the logging path.
    - `rg -n "trace_id|alert_id|fingerprint|source|channel_id|channel_name|channel_type" internal/handlers/webhook.go` confirms the async panic code path routes real context into the canonical field envelope.
    - No `log/slog` import, no new logging package, and no edits outside `internal/handlers/webhook.go` are introduced by this task.
  </acceptance_criteria>
  <done>`async_panic` logs become traceable failure evidence instead of context-free panic text, closing the implementation side of `OBS-03`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression coverage for async panic correlation fields</name>
  <read_first>
    internal/handlers/webhook_test.go
    internal/handlers/webhook.go
    .planning/phases/16-standardize-alert-path-logging/16-VERIFICATION.md
    .planning/phases/16-standardize-alert-path-logging/16-SECURITY.md
  </read_first>
  <files>internal/handlers/webhook_test.go</files>
  <behavior>
    - The panic regression fails if `async_panic` ever drops `trace_id` or `alert_id` again.
    - The test proves the emitted line keeps channel fields when the panic occurs during concrete channel processing.
    - Assertions operate on the canonical emitted fields, not only on human-readable panic text.
  </behavior>
  <action>Extend the existing panic regression in `internal/handlers/webhook_test.go` so it captures the `async_panic` line and asserts the correlation contract required by `OBS-03`: at minimum `trace_id` and `alert_id`, plus `fingerprint`, `source`, and channel metadata when the test drives a channel-aware panic. Reuse the handler’s in-memory logger seam and the current helper style already established in Phase 16 tests. If the existing parser cannot represent the assertion cleanly because of the later LOG-01 blocker, keep the new test focused on stable field keys and prepare it to survive the safer serialization introduced in 16-04.</action>
  <verify>
    <automated>go test ./internal/handlers -run "TestWebhookHandler(.*Panic.*|.*SendNotification.*)" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "async_panic" internal/handlers/webhook_test.go` finds a regression test that asserts the panic line itself, not just generic log presence.
    - `rg -n "trace_id|alert_id|fingerprint|source|channel_id|channel_name|channel_type" internal/handlers/webhook_test.go` shows explicit assertions tied to the `async_panic` test case.
    - The focused panic/send notification test command passes without changing any frontend file or non-webhook backend package.
  </acceptance_criteria>
  <done>The Phase 16 test suite now fails if async panic logs ever lose their correlation envelope again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| async notification processing -> panic recovery log | In-flight alert/channel context is converted into the operator-visible failure record. |
| canonical alert fields -> troubleshooting workflow | Logged identifiers must let operators trace a panic back to ingest and routing stages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | R | `processAlertNotificationsAsync` panic recovery | mitigate | Keep `alert` and `channel` context reachable inside the recovery path so `async_panic` emits `trace_id`, `alert_id`, fingerprint, source, and channel metadata through the canonical writer. |
| T-16-05 | S | alert-path stage taxonomy | mitigate | Preserve `stage=async_panic` and existing Phase 13-15 stage names; do not rename stages while fixing the correlation gap. |
| T-16-06 | R | handler regression coverage | mitigate | Add field-level tests on the emitted `async_panic` line so future changes cannot silently regress correlation evidence. |
</threat_model>

<verification>
Run the focused panic/trace handler tests after the implementation change, then confirm the new `async_panic` assertions are inspecting emitted fields rather than only raw panic text.
</verification>

<success_criteria>
`OBS-03` is implementation-complete when a recovered async notification panic emits the same correlation envelope operators already use on other failure paths, and the handler regression suite fails if that envelope disappears.
</success_criteria>

<output>
After completion, create `.planning/phases/16-standardize-alert-path-logging/16-03-SUMMARY.md`
</output>
