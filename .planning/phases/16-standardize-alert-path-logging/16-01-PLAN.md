---
phase: 16-standardize-alert-path-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/handlers/webhook.go
  - internal/handlers/webhook_test.go
autonomous: true
requirements:
  - OBS-03
  - LOG-01
  - LOG-02
requirements_addressed:
  - OBS-03
  - LOG-01
  - LOG-02
must_haves:
  truths:
    - "Webhook 到通知发送的关键日志继续沿用既有 `stage` 名称和 `trace_id`，但同类事件改为共享一套字段命名和输出入口。"
    - "高风险 alert-path 日志不再在 `logNotification` 与 `logTraceStage` 两条 helper 路径间漂移，机器可读值会进入稳定字段而不是只埋在消息文本里。"
    - "值班人员可以从 `terminal_failure` 或 `send_attempt` 日志继续按 `trace_id` 回查到 ingest、persist、route_match、notification_entry 等上游阶段。"
    - "本 phase 只收口 `internal/handlers/webhook.go` 主链路日志契约，不扩展成新日志平台、`slog` 迁移或 websocket 清理。"
  artifacts:
    - path: internal/handlers/webhook.go
      provides: "统一 alert-path event writer、基础 envelope helper 和高风险 call site 迁移"
    - path: internal/handlers/webhook_test.go
      provides: "共享日志契约与关键字段迁移的自动化断言"
  key_links:
    - from: internal/handlers/webhook.go
      to: internal/handlers/webhook.go
      via: "alert-path call sites -> shared event writer"
      pattern: "stage=|logAlertEvent|baseAlertLogFields|eventFields"
    - from: internal/handlers/webhook.go
      to: internal/handlers/webhook.go
      via: "send_attempt / terminal_failure -> existing trace context"
      pattern: "trace_id|attempt|max_attempts|terminal_failure"
    - from: internal/handlers/webhook_test.go
      to: internal/handlers/webhook.go
      via: "logger buffer assertions against canonical field names"
      pattern: "matched_channels|mode|channel_type|trace_id|stage="
---

<objective>
把 `internal/handlers/webhook.go` 中分裂的 alert-path 日志 helper 收口成一条共享写入路径，并把高风险 call sites 迁移到同一字段契约。

Purpose: 在不改技术栈、不改现有 `log.Logger` seam、也不改 Phase 14/15 `stage`/`trace_id`/retry 字段语义的前提下，完成一次 brownfield 日志契约标准化，为运维排障提供稳定可检索的日志真源。
Output: 一个 canonical alert-path event writer、统一 base envelope、迁移后的 webhook 主链路日志 call sites，以及覆盖关键字段契约的 handler tests。
</objective>

<execution_context>
@C:/Users/doumaoxin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/doumaoxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-standardize-alert-path-logging/16-RESEARCH.md
@.planning/phases/14-establish-alert-trace-context/14-VERIFICATION.md
@.planning/phases/15-harden-notification-retry-boundaries/15-VERIFICATION.md
@.planning/phases/15-harden-notification-retry-boundaries/15-01-SUMMARY.md
@.planning/phases/15-harden-notification-retry-boundaries/15-02-SUMMARY.md
@internal/handlers/webhook.go
@internal/handlers/webhook_test.go

<interfaces>
From `internal/handlers/webhook.go`:
```go
func (h *WebhookHandler) logNotification(stage string, alert *models.Alert, channel *models.Channel, format string, args ...interface{})
func (h *WebhookHandler) logTraceStage(stage string, fields map[string]string, format string, args ...interface{})
func (h *WebhookHandler) traceFieldsForAlert(alert models.Alert) map[string]string
func (h *WebhookHandler) traceFieldsForNotification(alert *models.Alert, channel *models.Channel) map[string]string
func (h *WebhookHandler) traceFieldsForAttempt(alert *models.Alert, channel *models.Channel, attempt, maxAttempts int, err error) map[string]string
```

Phase 14/15 field and stage contract that must remain intact:
```text
stage
trace_id
alert_id
fingerprint
source
attempt
max_attempts
terminal_failure
ingest
persist
dedup
redis_publish
route_match
notification_entry
send_attempt
send_notification
```

Observed field drift to remove:
```text
logNotification emits channel_type while traceFieldsForNotification does not
matched_channels is currently trapped in free-text message text
mode is currently trapped in free-text message text
two helper APIs choose field assembly differently
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce one canonical alert-path event writer and base envelope helpers</name>
  <read_first>
    .planning/phases/16-standardize-alert-path-logging/16-RESEARCH.md
    internal/handlers/webhook.go
    internal/handlers/webhook_test.go
    .planning/phases/14-establish-alert-trace-context/14-VERIFICATION.md
    .planning/phases/15-harden-notification-retry-boundaries/15-VERIFICATION.md
  </read_first>
  <files>internal/handlers/webhook.go, internal/handlers/webhook_test.go</files>
  <behavior>
    - Test alert-path logs still emit existing `stage` values and `trace_id` continuity after the helper refactor.
    - Test a shared base envelope can populate `trace_id`, `alert_id`, `fingerprint`, `source`, `channel_id`, `channel_name`, and `channel_type` deterministically from alert/channel context.
    - Test the new writer skips empty optional fields without changing stable key names or ordering rules.
  </behavior>
  <action>Implement one shared event writer inside `internal/handlers/webhook.go` for webhook-to-notification operational logs, keeping the current `log.Logger` seam and deterministic `key=value` text output. Replace the split `logNotification` / `logTraceStage` field-assembly contract with a single writer plus explicit helper(s) for a canonical base envelope. Preserve the existing `stage` taxonomy and Phase 14/15 correlation keys (`trace_id`, retry fields) exactly; do not rename stages, do not switch to JSON logs, and do not introduce `slog`, new packages, or a repo-wide logging abstraction. If compatibility requires temporary wrapper functions, make them delegate to the shared writer and mark the parallel path as removed/deprecated in code comments or call-site usage so Phase 16 no longer has two independent assembly paths.</action>
  <verify>
    <automated>go test ./internal/handlers -run "TestWebhookHandler(.*Lifecycle.*|.*Trace.*|.*Redis.*|.*Logging.*)" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "func \\(h \\*WebhookHandler\\) (logAlertEvent|writeAlertEvent|alertPathEvent)" internal/handlers/webhook.go` finds one canonical writer for alert-path logs.
    - `rg -n "channel_type|channel_name|channel_id|trace_id|alert_id|fingerprint|source" internal/handlers/webhook.go` shows these fields are assembled through shared helper logic rather than duplicated ad hoc fragments.
    - `rg -n "func \\(h \\*WebhookHandler\\) logNotification|func \\(h \\*WebhookHandler\\) logTraceStage" internal/handlers/webhook.go` shows at most one active field-assembly implementation path; any retained wrapper delegates into the canonical writer.
    - `internal/handlers/webhook_test.go` contains at least one test that exercises the shared writer contract through real handler behavior instead of only checking helper internals.
    - No new package under `internal/observability`, no `log/slog` import, and no websocket/bootstrap logging changes are introduced in this task.
  </acceptance_criteria>
  <done>`WebhookHandler` owns exactly one canonical alert-path log emission path with a deterministic base envelope, while Phase 14/15 stage and correlation contracts remain intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate high-risk webhook call sites and promote machine-readable values into named fields</name>
  <read_first>
    internal/handlers/webhook.go
    internal/handlers/webhook_test.go
    .planning/phases/16-standardize-alert-path-logging/16-RESEARCH.md
    .planning/phases/14-establish-alert-trace-context/14-VERIFICATION.md
    .planning/phases/15-harden-notification-retry-boundaries/15-VERIFICATION.md
  </read_first>
  <files>internal/handlers/webhook.go, internal/handlers/webhook_test.go</files>
  <behavior>
    - Test `route_match` logs expose `matched_channels` as a field, not only as message text.
    - Test `send_attempt`, `send_notification`, and `terminal_failure` logs expose `mode` as a field while preserving `attempt`, `max_attempts`, and `error`.
    - Test datasource lookup, render fallback, channel lookup, redis publish, and async panic paths all emit through the shared writer without dropping existing trace context.
  </behavior>
  <action>Move the high-risk alert-path call sites in `HandleWebhook`, `publishToRedis`, `processAlertNotificationsAsync`, `processAlertNotifications`, `findMatchedChannels`, `sendNotification`, and `sendChannelNotification` onto the shared writer from Task 1. Promote machine-readable values currently trapped in message text into deterministic fields: at minimum move `matched_channels` and `mode` into named fields, and ensure notification-context logs consistently include `channel_type` alongside `channel_id` and `channel_name` where a channel exists. Preserve existing message intent for humans, but make the field contract the machine-readable truth. Scope LOG-02 to the alert-path `Printf`-style call sites in `internal/handlers/webhook.go`; do not broaden this task to unrelated packages such as websocket or server bootstrap logging.</action>
  <verify>
    <automated>go test ./internal/handlers -run "TestWebhookHandler(.*Route.*|.*SendNotification.*|.*Terminal.*|.*Panic.*|.*Redis.*)" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "matched_channels" internal/handlers/webhook.go internal/handlers/webhook_test.go` shows `matched_channels` is asserted as a named field in route-match logging.
    - `rg -n "mode" internal/handlers/webhook.go internal/handlers/webhook_test.go` shows `mode` is emitted and asserted as a named field on notification send-path logs.
    - `rg -n "channel_type" internal/handlers/webhook.go internal/handlers/webhook_test.go` shows channel-bearing logs now include `channel_type` through the shared contract.
    - `rg -n "logNotification\\(|logTraceStage\\(" internal/handlers/webhook.go` returns only canonical-writer delegation or migrated call sites; there must not be parallel ad hoc field assembly at the migrated high-risk sites.
    - Focused handler tests continue to assert `stage=terminal_failure` and `trace_id=` on retry-exhausted send paths, proving Phase 15 traceability still works after migration.
    - No changes are made to `internal/handlers/websocket.go`, `cmd/server/main.go`, or any frontend file.
  </acceptance_criteria>
  <done>The webhook-to-notification main path emits one consistent field contract, with machine-readable event values promoted into named fields and high-risk ad hoc logging paths migrated off the old split helper pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webhook request handling -> alert-path event logs | Runtime processing state is transformed into operator-facing evidence. |
| alert/channel context -> shared event writer | Internal objects are flattened into a stable text log contract. |
| retry and route metadata -> machine-readable fields | Numeric and mode data must remain searchable without relying on free-text parsing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | R | `internal/handlers/webhook.go` field contract | mitigate | Preserve `stage` and `trace_id` from Phases 14/15 and route all high-risk call sites through one writer so operators can reconstruct the path from a failure log. |
| T-16-02 | T | shared event writer migration | mitigate | Remove or delegate the parallel helper path so future edits cannot silently emit different field sets for the same event family. |
| T-16-03 | I | alert-path logging payload | mitigate | Keep the canonical envelope limited to IDs, routing metadata, retry fields, and bounded error strings; do not add raw payload or secret material to the contract. |
| T-16-04 | D | scope of LOG-02 cleanup | mitigate | Limit migration to webhook alert-path `Printf`-style sites so the plan closes the real drift without turning into a whole-repo logging rewrite. |
| T-16-05 | S | event taxonomy continuity | mitigate | Keep existing stage names (`ingest`, `persist`, `route_match`, `notification_entry`, `send_attempt`, `terminal_failure`) instead of inventing a new event namespace. |
</threat_model>

<verification>
Run focused handler tests after each task. Task 1 should prove the canonical writer preserves trace/stage continuity; Task 2 should prove route/send/redis/panic call sites expose the standardized fields while keeping the previous stage evidence searchable.
</verification>

<success_criteria>
`OBS-03`, `LOG-01`, and the implementation side of `LOG-02` are satisfied when `internal/handlers/webhook.go` uses one canonical alert-path event writer, high-risk call sites emit the same field vocabulary, and operators can still trace retry failures backward through the existing Phase 14/15 stages by `trace_id`.
</success_criteria>

<output>
After completion, create `.planning/phases/16-standardize-alert-path-logging/16-01-SUMMARY.md`
</output>
