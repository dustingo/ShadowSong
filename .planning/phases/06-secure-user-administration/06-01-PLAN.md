---
phase: 06-secure-user-administration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/models/user.go
  - internal/models/user_test.go
  - internal/auth/jwt.go
  - internal/auth/jwt_test.go
  - internal/middleware/auth.go
  - internal/middleware/auth_test.go
  - internal/handlers/user.go
  - internal/handlers/user_test.go
autonomous: true
requirements: [USER-05, USER-06]
must_haves:
  truths:
    - "被禁用账号不能再登录，也不能继续用旧 JWT 访问受保护接口。"
    - "被标记强制改密的账号拿到的会话只能完成改密或退出，不能继续正常使用系统。"
    - "现有 JWT claim key、登录响应外形和 principal 上下文键保持兼容。"
  artifacts:
    - path: "internal/models/user.go"
      provides: "账号禁用、强制改密、令牌失效基线字段与辅助方法"
    - path: "internal/middleware/auth.go"
      provides: "JWT 校验后基于数据库的账号状态复核"
    - path: "internal/handlers/user.go"
      provides: "登录与 refresh 对 disabled/forced-reset 的 fail-closed 处理"
    - path: "internal/middleware/auth_test.go"
      provides: "disabled、forced-reset、stale token 回归覆盖"
  key_links:
    - from: "internal/auth/jwt.go"
      to: "internal/middleware/auth.go"
      via: "claims.IssuedAt compared against user token cutoff"
      pattern: "IssuedAt|TokenInvalidBefore|token_invalid_before"
    - from: "internal/handlers/user.go"
      to: "internal/models/user.go"
      via: "login and refresh account-state checks"
      pattern: "ForcePasswordReset|DisabledAt"
---

<objective>
在不改变当前 JWT 合同的前提下，为 Phase 6 建立账号禁用、强制改密、旧会话立即失效的后端基础。

Purpose: 满足 USER-05、USER-06，并为后续管理员操作和前端受限模式提供统一的账号状态真源。
Output: 可持久化的用户安全状态字段、请求期账号状态复核、登录/刷新 fail-closed 回归测试。
</objective>

<execution_context>
@C:/Users/doumaoxin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/doumaoxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/phases/06-secure-user-administration/06-RESEARCH.md
@.planning/phases/05-normalize-role-model/05-02-SUMMARY.md
@.planning/phases/05-normalize-role-model/05-03-SUMMARY.md
@internal/models/user.go
@internal/auth/jwt.go
@internal/middleware/auth.go
@internal/handlers/user.go

<interfaces>
From internal/auth/jwt.go:
```go
type Claims struct {
	UserID   uint   `json:"user_id"`
	Username string `json:"username"`
	Role     string `json:"role"`
	jwt.RegisteredClaims
}

func (j *JWT) GenerateToken(userID uint, username, role string) (string, error)
func (j *JWT) ValidateToken(tokenString string) (*Claims, error)
func (j *JWT) RefreshToken(tokenString string) (string, error)
```

From internal/middleware/auth.go:
```go
type Principal struct {
	UserID   uint   `json:"user_id"`
	Username string `json:"username"`
	Role     string `json:"role"`
}

func JWTAuth(jwtAuth *auth.JWT) gin.HandlerFunc
func SetPrincipal(c *gin.Context, principal Principal)
func GetPrincipal(c *gin.Context) (Principal, bool)
```

From internal/models/user.go:
```go
type User struct {
	ID           uint
	Username     string
	PasswordHash string
	Name         string
	Email        string
	Role         string
	CreatedAt    time.Time
	UpdatedAt    time.Time
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add persistent account-control fields and model helpers</name>
  <files>internal/models/user.go, internal/models/user_test.go</files>
  <read_first>.planning/phases/06-secure-user-administration/06-RESEARCH.md, internal/models/user.go, internal/models/user_test.go, .planning/phases/05-normalize-role-model/05-01-SUMMARY.md</read_first>
  <behavior>
    - Test 1: `User` persists nullable disabled/reset control fields with safe zero/default semantics on existing rows.
    - Test 2: helper logic distinguishes active, disabled, forced-reset, and stale-token states without changing role validation behavior.
  </behavior>
  <action>Extend `models.User` with the minimum Phase 6 state required by D-Phase6-research and USER-05/USER-06: a nullable disabled timestamp, a boolean forced-reset flag, and a per-user token freshness cutoff or equivalent timestamp that can invalidate already-issued JWTs. Keep existing role validation and password hashing intact. Add model-level helpers such as `IsDisabled`, freshness comparison, and password/security-state update helpers so later handler code does not duplicate timestamp logic. Use nullable/safe defaults so GORM `AutoMigrate` remains rollout-safe for existing users.</action>
  <acceptance_criteria>`internal/models/user.go` contains persistent fields for disabled and force-reset state plus a token invalidation cutoff; `internal/models/user_test.go` has table-driven coverage for active vs disabled vs forced-reset/stale-token helper behavior; existing role validation tests still pass.</acceptance_criteria>
  <verify>
    <automated>go test ./internal/models -run "TestUser" -count=1 -timeout 60s</automated>
  </verify>
  <done>`models.User` can represent Phase 6 account-control state without breaking Phase 5 role semantics, and helper tests prove the new state transitions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-check current user state during login, refresh, and protected-request auth</name>
  <files>internal/auth/jwt.go, internal/auth/jwt_test.go, internal/middleware/auth.go, internal/middleware/auth_test.go, internal/handlers/user.go, internal/handlers/user_test.go</files>
  <read_first>internal/auth/jwt.go, internal/middleware/auth.go, internal/handlers/user.go, internal/auth/jwt_test.go, internal/middleware/auth_test.go, internal/handlers/user_test.go, .planning/phases/05-normalize-role-model/05-02-SUMMARY.md</read_first>
  <behavior>
    - Test 1: disabled users cannot log in and cannot refresh/continue with a previously valid JWT.
    - Test 2: forced-reset users keep the current login response contract but are limited to an explicit allowlist path until password change finishes.
    - Test 3: tokens issued before the per-user invalidation cutoff are rejected even when signature and role claims are valid.
  </behavior>
  <action>Keep Phase 5 contracts stable: JWT claims stay `user_id`/`username`/`role`, login still returns `{token,user,expire_at}`. Update `UserHandler.Login` and `UserHandler.RefreshToken` to load the current `User` row and fail closed for disabled users; for forced-reset users, preserve successful login but expose the reset-required state on the returned user payload. Refactor `middleware.JWTAuth` to accept the dependencies needed to load the current user from PostgreSQL on every protected request, reject disabled users, reject tokens older than the stored invalidation cutoff, and block forced-reset users from all protected routes except the narrow Phase 6 allowlist path for password reset, logout, and self-context. Add focused auth/handler tests for stale sessions, disabled login, forced-reset limited mode, and claim-key compatibility.</action>
  <acceptance_criteria>`internal/middleware/auth.go` performs a database lookup before `SetPrincipal`; protected requests with a pre-cutoff token return `401`; `internal/handlers/user.go` rejects disabled login and refresh while keeping the existing response envelope; tests explicitly cover disabled accounts, stale sessions, and forced-reset limited mode.</acceptance_criteria>
  <verify>
    <automated>go test ./internal/auth ./internal/middleware ./internal/handlers -run "Test(Login|Refresh|JWT|Auth)" -count=1 -timeout 60s</automated>
  </verify>
  <done>Server-side auth becomes state-aware: disabled and stale sessions fail immediately, forced-reset users are limited without changing the public token/claims contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→auth handlers | Untrusted credentials and bearer tokens enter login/refresh flows |
| JWT claims→database user state | Signed token data must be reconciled against current persisted account state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | E | `internal/middleware/auth.go` | mitigate | Load current `models.User` on every protected request and reject disabled or stale-token sessions before principal creation |
| T-06-02 | E | `internal/handlers/user.go` login/refresh | mitigate | Fail closed for disabled accounts and refuse refresh when account state no longer permits normal use |
| T-06-03 | T | `internal/auth/jwt.go` + per-user cutoff | mitigate | Compare JWT `IssuedAt` against persisted invalidation timestamp so old tokens cannot outlive password/security actions |
| T-06-04 | D | forced-reset limited mode | mitigate | Allow only explicit self-password/self-context/logout paths so users can recover without keeping wider access |
</threat_model>

<verification>
- `go test ./internal/models ./internal/auth ./internal/middleware ./internal/handlers -run "Test(User|Login|Refresh|JWT|Auth)" -count=1 -timeout 60s`
- Confirm middleware signature/route call sites are updated consistently before later plans modify router wiring.
</verification>

<success_criteria>
- Disabled accounts are denied at both login time and request-auth time.
- Forced-reset accounts can no longer use old normal sessions to browse protected resources.
- JWT claim keys and request principal compatibility from Phase 5 remain intact.
</success_criteria>

<output>
After completion, create `.planning/phases/06-secure-user-administration/06-01-SUMMARY.md`
</output>
