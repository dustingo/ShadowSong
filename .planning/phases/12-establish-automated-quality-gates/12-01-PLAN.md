---
phase: 12-establish-automated-quality-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/quality-gates.yml
autonomous: true
requirements:
  - CIV-01
  - CIV-02
  - CIV-03
user_setup: []
must_haves:
  truths:
    - "Repository has an automated CI workflow that runs backend tests plus frontend lint, test, and build"
    - "CI failures are attributable to a named step or job instead of a single opaque script blob"
    - "The workflow reuses the repository's real commands rather than inventing a second build contract"
  artifacts:
    - path: ".github/workflows/quality-gates.yml"
      provides: "GitHub Actions quality gate workflow"
      contains: "go test ./..."
  key_links:
    - from: ".github/workflows/quality-gates.yml"
      to: "Makefile"
      via: "backend test command truth"
      pattern: "go test ./..."
    - from: ".github/workflows/quality-gates.yml"
      to: "frontend/package.json"
      via: "frontend lint/test/build command truth"
      pattern: "pnpm lint|pnpm test -- --run|pnpm build"
---

<objective>
Add automated quality gates for the current non-AI alert system baseline.

Purpose: Turn the already-validated backend and frontend local checks into a repository-native CI workflow that blocks regressions before merge.
Output: A GitHub Actions workflow that runs backend tests and frontend lint/test/build as explicit, traceable gates.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-establish-automated-quality-gates/12-CONTEXT.md
@Makefile
@frontend/package.json
@.planning/phases/11-restore-frontend-quality-baseline/11-VERIFICATION.md
@go.mod
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GitHub Actions workflow for backend tests and frontend quality gates</name>
  <files>.github/workflows/quality-gates.yml</files>
  <read_first>.planning/phases/12-establish-automated-quality-gates/12-CONTEXT.md, Makefile, frontend/package.json, go.mod, .planning/phases/11-restore-frontend-quality-baseline/11-VERIFICATION.md</read_first>
  <action>Create `.github/workflows/quality-gates.yml` to run on push and pull_request for the main development branches. Use GitHub Actions with explicit jobs or clearly separated steps so failures are directly attributable. The workflow must cover `go test ./...` at repo root and `pnpm lint`, `pnpm test -- --run`, `pnpm build` in the frontend workspace. Reuse current repository commands and toolchain versions implied by `go.mod`, README, and frontend package metadata. Add reasonable dependency caching where it improves repeatability without obscuring behavior, but do not invent a parallel build script contract.</action>
  <verify>Get-Content .github/workflows/quality-gates.yml</verify>
  <acceptance_criteria>
    - `.github/workflows/quality-gates.yml` exists
    - the workflow contains `go test ./...`
    - the workflow contains `pnpm lint`
    - the workflow contains `pnpm test -- --run`
    - the workflow contains `pnpm build`
    - backend and frontend checks are separated enough that failure origin is obvious
  </acceptance_criteria>
  <done>The repository has a first-class CI workflow that enforces the agreed quality gates.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.github/workflows/quality-gates.yml` exists and is syntactically coherent
- [ ] The workflow references the same commands validated locally in earlier phases
- [ ] Failure points are attributable to explicit jobs or named steps
</verification>

<success_criteria>

- All tasks completed
- CI gate coverage matches the roadmap and requirements truth
- Workflow design makes regressions easy to localize when a gate fails

</success_criteria>

<output>
After completion, create `.planning/phases/12-establish-automated-quality-gates/12-01-SUMMARY.md`
</output>
