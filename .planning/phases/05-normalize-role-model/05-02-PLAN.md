---
phase: 05-normalize-role-model
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - internal/auth/jwt.go
  - internal/middleware/auth.go
  - internal/middleware/auth_test.go
  - internal/handlers/user_test.go
  - internal/database/postgres.go
  - cmd/roleaudit/main.go
  - internal/database/postgres_test.go
  - cmd/roleaudit/main_test.go
autonomous: false
requirements:
  - AUTHZ-02
must_haves:
  truths:
    - Existing users can continue logging in with the same role strings and JWT claim keys.
    - Middleware rejects unsupported role claims instead of copying arbitrary strings into request context.
    - There is one executable audit path to inspect current persisted `users.role` values before or during rollout.
    - Phase completion is blocked until the persisted-role audit runs against the target database and finds no unsupported `users.role` values.
  artifacts:
    - path: internal/auth/jwt.go
      provides: Stable JWT claims contract with explicit role validation boundaries.
    - path: internal/middleware/auth.go
      provides: Principal extraction that preserves existing context keys while validating supported roles.
    - path: internal/middleware/auth_test.go
      provides: Regression coverage for bearer parsing, claim extraction, and invalid-role rejection.
    - path: internal/handlers/user_test.go
      provides: Login and refresh regression coverage proving the existing handler contract remains compatible after JWT hardening.
    - path: cmd/roleaudit/main.go
      provides: Read-only persisted-role audit command for rollout compatibility checks.
    - path: internal/database/postgres.go
      provides: Bootstrap admin creation aligned with the canonical role constants.
    - path: internal/database/postgres_test.go
      provides: Regression coverage proving bootstrap admin creation uses the canonical role constant.
    - path: cmd/roleaudit/main_test.go
      provides: Command-level coverage for read-only audit output and supported vs unsupported exit-code behavior.
  key_links:
    - from: internal/auth/jwt.go
      to: internal/middleware/auth.go
      via: `Claims.Role` validation before principal/context creation
      pattern: "Role"
    - from: internal/database/postgres.go
      to: internal/authz/roles.go
      via: bootstrap default admin role constant
      pattern: "RoleAdmin"
    - from: cmd/roleaudit/main.go
      to: `users` table
      via: grouped role query for rollout inspection
      pattern: "GROUP BY role"
    - from: checkpoint rollout verification
      to: cmd/roleaudit/main.go
      via: required target-environment audit run before summary approval
      pattern: "go run ./cmd/roleaudit"
---

<objective>
Preserve login and session compatibility while hardening JWT principal extraction and giving rollout a concrete persisted-role audit path.

Purpose: satisfy AUTHZ-02 without changing claim keys, frontend localStorage behavior, or the existing role names.
Output: validated principal extraction, bootstrap compatibility alignment, and a read-only role-audit command.
</objective>

<execution_context>
@C:/Users/doumaoxin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/doumaoxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-normalize-role-model/05-RESEARCH.md
@internal/auth/jwt.go
@internal/middleware/auth.go
@internal/database/postgres.go
@frontend/src/stores/userStore.ts
@frontend/src/api/auth.ts
@frontend/src/types/index.ts

<interfaces>
From `internal/auth/jwt.go`:

```go
type Claims struct {
	UserID   uint   `json:"user_id"`
	Username string `json:"username"`
	Role     string `json:"role"`
}

func (j *JWT) GenerateToken(userID uint, username, role string) (string, error)
func (j *JWT) ValidateToken(tokenString string) (*Claims, error)
```

From `internal/middleware/auth.go`:

```go
const (
	UserIDKey   = "user_id"
	UsernameKey = "username"
	RoleKey     = "role"
)

func JWTAuth(jwtAuth *auth.JWT) gin.HandlerFunc
func GetUserID(c *gin.Context) uint
func GetUserRole(c *gin.Context) string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate JWT role claims and extract a reusable principal without breaking session shape</name>
  <read_first>
    - internal/auth/jwt.go
    - internal/middleware/auth.go
    - frontend/src/stores/userStore.ts
    - frontend/src/api/auth.ts
  </read_first>
  <files>internal/auth/jwt.go, internal/middleware/auth.go, internal/middleware/auth_test.go, internal/handlers/user_test.go</files>
  <behavior>
    - Test 1: tokens still use `user_id`, `username`, and `role` claim keys, and valid supported roles authenticate successfully.
    - Test 2: middleware rejects tokens whose `role` claim is not one of `admin`, `operator`, `viewer`.
    - Test 3: login and refresh handler request tests still return the existing response shape and a supported role after the JWT hardening changes.
    - Test 4: middleware exposes a principal/helper path that later phases can extend with account flags while preserving `user_id`, `username`, and `role` context keys for compatibility.
  </behavior>
  <action>Keep the `Claims` JSON field names and the frontend-observed login/session contract unchanged, but harden `internal/auth/jwt.go` and `internal/middleware/auth.go` so unsupported `role` claims cannot flow into request authorization. Add a small reusable principal abstraction or principal-construction helper in the auth/middleware layer that captures `UserID`, `Username`, and `Role`, preserves the existing Gin context keys, and becomes the hook for later Phase 6 account-status flags. Update `internal/middleware/auth_test.go` with table-driven tests for bearer parsing, valid principal extraction, invalid-role rejection, and compatibility of the existing context getters. Extend `internal/handlers/user_test.go` with login and refresh request-level regression coverage that proves AUTHZ-02 compatibility: successful login still emits the existing token/user payload shape with a supported role string, refresh still reissues a compatible token, and invalid-role token handling now fails closed with the existing unauthorized/error contract. Do not change `localStorage` keys (`token`, `user`), the login response JSON shape, or JWT claim key names.</action>
  <verify>
    <automated>go test ./internal/middleware ./internal/auth ./internal/handlers -run "TestJWT|TestJWTAuth|TestPrincipal|TestLogin|TestRefresh" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `Claims` still serializes `user_id`, `username`, and `role`.
    - Middleware has an explicit invalid-role rejection path before setting Gin auth context.
    - `internal/handlers/user_test.go` proves login and refresh keep the existing response contract while invalid-role tokens are rejected.
    - Tests prove existing getter helpers still work with the normalized principal extraction path.
  </acceptance_criteria>
  <done>JWT-authenticated requests keep their existing session contract while unsupported role claims are blocked and principal extraction becomes reusable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Audit persisted role values and align bootstrap assumptions to the canonical contract</name>
  <read_first>
    - internal/database/postgres.go
    - internal/authz/roles.go
    - .planning/phases/05-normalize-role-model/05-RESEARCH.md
    - .planning/STATE.md
  </read_first>
  <files>internal/database/postgres.go, internal/database/postgres_test.go, cmd/roleaudit/main.go, cmd/roleaudit/main_test.go</files>
  <behavior>
    - Test 1: bootstrap admin creation uses `authz.RoleAdmin` and preserves the existing admin account initialization path.
    - Test 2: the audit command executes a grouped read-only `users.role` query and exits `0` when all observed roles are within `admin`, `operator`, `viewer`.
    - Test 3: the audit command exits non-zero and prints offending role buckets when unsupported persisted role values are present.
    - Test 4: the audit command exits non-zero on database/query failure and tests prove it does not execute write statements.
  </behavior>
  <action>Update `internal/database/postgres.go` so the default admin bootstrap path uses the canonical `authz.RoleAdmin` constant rather than a raw string, preserving current bootstrap behavior. Add regression coverage in `internal/database/postgres_test.go` that proves the bootstrap path still creates the default admin with the canonical role constant, so AUTHZ-02 compatibility is locked by test rather than inspection. Add a read-only audit utility at `cmd/roleaudit/main.go` that connects with the existing app database configuration pattern and prints grouped counts for `users.role`, explicitly surfacing any values outside `admin`, `operator`, `viewer`. Add `cmd/roleaudit/main_test.go` with a concrete harness for the audit query and CLI exit behavior: success/exit `0` for supported-role datasets, failure/non-zero for unsupported-role datasets, failure/non-zero for DB/query errors, and assertions that the command path only performs the grouped read audit and not inserts/updates/deletes. Keep the command output operationally useful for rollout by naming unsupported role values in the failure path. Do not add a migration or auto-remediation in this plan; unsupported live rows must block completion until manually remediated per AUTHZ-02.</action>
  <verify>
    <automated>go test ./internal/database ./cmd/roleaudit -run "TestCreateDefaultAdminUser|TestRoleAudit" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `createDefaultAdminUser` uses the canonical admin role constant.
    - `internal/database/postgres_test.go` fails if bootstrap stops assigning the canonical `admin` role.
    - `cmd/roleaudit/main_test.go` fails if supported-role datasets do not exit `0`.
    - `cmd/roleaudit/main_test.go` fails if unsupported-role datasets do not exit non-zero and report offending role values.
    - `cmd/roleaudit/main_test.go` fails if the audit command attempts any write behavior instead of a grouped read-only role query.
  </acceptance_criteria>
  <done>Phase 5 has an executable persisted-role audit path and bootstrap code aligned to the canonical role contract.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the persisted-role audit against the target rollout database before closing AUTHZ-02</name>
  <what-built>Task 2 delivers a read-only audit command with deterministic exit behavior for supported vs unsupported persisted roles.</what-built>
  <action>Use the Task 2 audit command as a rollout gate for AUTHZ-02. Phase completion must pause until the command is executed against the target database and returns a clean supported-role result. Unsupported persisted role values are an operational blocker for this phase, not a warning to defer.</action>
  <how-to-verify>
    1. Run `go run ./cmd/roleaudit` with the same database environment variables that the target deployment uses.
    2. Confirm the command exits `0` and the output contains only supported buckets for `admin`, `operator`, and `viewer`.
    3. If the command exits non-zero or prints any unsupported role values, stop phase completion, capture the offending role rows, remediate the data outside this phase, and rerun the audit until it exits `0`.
    4. Only mark the plan complete after a clean rerun proves the persisted data is compatible with the narrowed role contract.
  </how-to-verify>
  <verify>
    <automated>MISSING — automated target-environment DB verification is environment-dependent; run `go run ./cmd/roleaudit` against the rollout database and require exit 0 before approval</automated>
  </verify>
  <done>The target rollout database has been audited with `go run ./cmd/roleaudit`, the command exited `0`, and no unsupported persisted role values remain.</done>
  <resume-signal>Type `approved` only after the target-environment audit exits 0 with no unsupported roles; otherwise report the offending role values.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JWT bearer token -> request principal | Untrusted token content becomes authorization state for protected routes |
| rollout audit CLI -> production-like database | Read-only operational tooling inspects live user role values and must not mutate data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | S | `internal/middleware/auth.go` | mitigate | Validate claim role against the canonical set before populating Gin context and cover the rejection path in middleware tests |
| T-05-05 | E | JWT-authenticated routes | mitigate | Preserve claim keys but reject unsupported roles so stale or forged claims cannot gain undefined authorization behavior |
| T-05-06 | T | `cmd/roleaudit/main.go` | mitigate | Keep the audit command strictly read-only and make unsupported-role findings explicit with a non-zero exit code |
| T-05-10 | T | rollout database contents | mitigate | Block phase completion on a real audit run so unsupported persisted role rows cannot silently survive the contract tightening |
</threat_model>

<verification>
Run `go test ./internal/middleware ./internal/auth ./internal/handlers -run "TestJWT|TestJWTAuth|TestPrincipal|TestLogin|TestRefresh" -count=1` and `go test ./internal/database ./cmd/roleaudit -run "TestCreateDefaultAdminUser|TestRoleAudit" -count=1`. During rollout, execute `go run ./cmd/roleaudit` against the target DB and do not close the phase until it exits `0` with no unsupported persisted role values.
</verification>

<success_criteria>
- JWT claim keys and frontend session storage assumptions remain unchanged.
- Unsupported role claims are rejected before entering request context.
- There is one concrete command to audit persisted `users.role` values and bootstrap compatibility assumptions.
- Phase completion is blocked until the target-environment audit run reports only supported `admin`, `operator`, and `viewer` rows.
</success_criteria>

<output>
After completion, create `.planning/phases/05-normalize-role-model/05-02-SUMMARY.md`
</output>
