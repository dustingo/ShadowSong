---
phase: 11-restore-frontend-quality-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - frontend/src/pages/Dashboard.test.tsx
  - frontend/src/test/setup.ts
  - frontend/vite.config.ts
  - frontend/tsconfig.json
autonomous: true
requirements:
  - FEQ-03
user_setup: []
must_haves:
  truths:
    - "Frontend lint cleanup does not break the existing test flow"
    - "Frontend production build still succeeds after Phase 11 code changes"
    - "Any fixes needed for test or build regressions remain within the current frontend baseline scope"
  artifacts:
    - path: "frontend/package.json"
      provides: "Verification command truth for lint, test, and build"
      contains: "\"build\""
    - path: "frontend/src/pages/Dashboard.test.tsx"
      provides: "Representative frontend test coverage that must remain green after cleanup"
      contains: "Dashboard"
  key_links:
    - from: "frontend/package.json"
      to: "frontend/src/pages/Dashboard.test.tsx"
      via: "vitest execution path"
      pattern: "\"test\""
    - from: "frontend/package.json"
      to: "frontend/vite.config.ts"
      via: "frontend build pipeline"
      pattern: "\"build\""
---

<objective>
Re-verify frontend test and build health after the lint cleanup work lands.

Purpose: Prove the Phase 11 cleanup did not only satisfy ESLint, but also preserved the current frontend test flow and production build path that Phase 12 will later gate in CI.
Output: Passing frontend lint, test, and build verification, plus only the minimal follow-up fixes needed to restore those paths if cleanup exposed latent issues.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-restore-frontend-quality-baseline/11-CONTEXT.md
@.planning/phases/11-restore-frontend-quality-baseline/11-01-PLAN.md
@frontend/package.json
@frontend/vite.config.ts
@frontend/tsconfig.json
@frontend/src/pages/Dashboard.test.tsx
@frontend/src/test/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run frontend lint, test, and build flows and fix only Phase 11 regressions</name>
  <files>frontend/src/pages/Dashboard.test.tsx, frontend/src/test/setup.ts, frontend/vite.config.ts, frontend/tsconfig.json</files>
  <read_first>frontend/package.json, frontend/src/pages/Dashboard.test.tsx, frontend/src/test/setup.ts, frontend/vite.config.ts, frontend/tsconfig.json, .planning/phases/11-restore-frontend-quality-baseline/11-01-PLAN.md</read_first>
  <action>After the lint cleanup is complete, run the existing frontend verification commands in sequence: `pnpm lint`, `pnpm test -- --run`, and `pnpm build`. If any test or build step fails because the lint cleanup exposed a real regression, apply only the minimal follow-up fix needed to restore the existing baseline, keeping changes inside the current frontend scope and avoiding new feature work. Do not add a new test runner, change the product surface, or broaden into CI setup in this phase.</action>
  <verify>pnpm lint && pnpm test -- --run && pnpm build</verify>
  <acceptance_criteria>
    - `pnpm lint` exits 0
    - `pnpm test -- --run` exits 0
    - `pnpm build` exits 0
    - any follow-up changes remain within frontend baseline repair scope
  </acceptance_criteria>
  <done>Frontend lint, test, and build all pass after the Phase 11 cleanup work.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint` passes
- [ ] `pnpm test -- --run` passes
- [ ] `pnpm build` passes
</verification>

<success_criteria>

- All tasks completed
- Lint cleanup does not leave hidden frontend regressions behind
- The repository now has a stable local frontend quality baseline for CI to enforce next

</success_criteria>

<output>
After completion, create `.planning/phases/11-restore-frontend-quality-baseline/11-02-SUMMARY.md`
</output>
