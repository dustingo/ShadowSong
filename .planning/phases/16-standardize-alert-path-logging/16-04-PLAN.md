---
phase: 16-standardize-alert-path-logging
plan: 04
type: execute
wave: 2
depends_on:
  - 16-03
files_modified:
  - internal/handlers/webhook.go
  - internal/handlers/webhook_test.go
  - .planning/phases/16-standardize-alert-path-logging/16-VERIFICATION.md
  - .planning/phases/16-standardize-alert-path-logging/16-SECURITY.md
  - .planning/phases/16-standardize-alert-path-logging/16-UAT.md
autonomous: true
gap_closure: true
requirements:
  - LOG-01
  - LOG-03
must_haves:
  truths:
    - "Canonical alert-path writer 继续使用稳定字段名，但值中包含空格时也能被确定性解析，不会再被 `strings.Fields` 截断。"
    - "Webhook logging tests 按同一输出契约解析字段，能证明 `error`、`channel_name` 等包含空格的值完整保留。"
    - "Phase 16 的 verification、security 和 UAT 文档与修复后的实现及自动化命令保持一致，不再错误地把已知 blocker 标成通过。"
    - "修复仍限定在 `internal/handlers/webhook.go` 告警主链路及其 phase-local truth artifacts，不扩展成 JSON logging 或全仓库日志迁移。"
  artifacts:
    - path: internal/handlers/webhook.go
      provides: "安全可解析的 canonical field serialization"
    - path: internal/handlers/webhook_test.go
      provides: "能处理空格值的 log parser 与序列化回归测试"
    - path: .planning/phases/16-standardize-alert-path-logging/16-VERIFICATION.md
      provides: "post-fix verification truth"
    - path: .planning/phases/16-standardize-alert-path-logging/16-SECURITY.md
      provides: "post-fix threat closure record"
    - path: .planning/phases/16-standardize-alert-path-logging/16-UAT.md
      provides: "与实际证据一致的 Phase 16 UAT truth"
  key_links:
    - from: internal/handlers/webhook.go
      to: internal/handlers/webhook_test.go
      via: "serialized field format -> parser-safe regression assertions"
      pattern: "logAlertEvent|parseWebhookLogFields|error=|channel_name="
    - from: .planning/phases/16-standardize-alert-path-logging/16-VERIFICATION.md
      to: .planning/phases/16-standardize-alert-path-logging/16-SECURITY.md
      via: "same closed blockers reflected in verification and threat status"
      pattern: "T-16-10|LOG-01|OBS-03"
---

<objective>
关闭 Phase 16 的 `LOG-01` / `T-16-10` blocker，并在自动化验证后刷新 phase-local truth artifacts，让后续 verify/security 重跑能直接通过。

Purpose: 当前 canonical writer 虽然统一了字段名，但输出仍是裸 `key=value` 空格分隔；包含空格的值会被歧义解析，现有测试还用 `strings.Fields` 掩盖了问题。
Output: 一套可安全解析的 alert-path 字段序列化规则、匹配的新测试解析器/回归用例，以及更新后的 `16-VERIFICATION.md`、`16-SECURITY.md`、`16-UAT.md`。
</objective>

<execution_context>
@C:/Users/doumaoxin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/doumaoxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-standardize-alert-path-logging/16-RESEARCH.md
@.planning/phases/16-standardize-alert-path-logging/16-VERIFICATION.md
@.planning/phases/16-standardize-alert-path-logging/16-SECURITY.md
@.planning/phases/16-standardize-alert-path-logging/16-UAT.md
@.planning/phases/16-standardize-alert-path-logging/16-03-SUMMARY.md
@internal/handlers/webhook.go
@internal/handlers/webhook_test.go

<interfaces>
From `internal/handlers/webhook.go`:
```go
func (h *WebhookHandler) logAlertEvent(stage string, fields map[string]string, format string, args ...interface{})
```

From `internal/handlers/webhook_test.go`:
```go
func findWebhookLogLine(logOutput string, needle string) string
func parseWebhookLogFields(logLine string) map[string]string
```

Current blocker contract:
```text
Raw space-delimited key=value tokens are ambiguous when values contain spaces.
Tests currently parse via strings.Fields, which loses value tails and hides the ambiguity.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the canonical writer safely parseable for values containing spaces</name>
  <read_first>
    internal/handlers/webhook.go
    internal/handlers/webhook_test.go
    .planning/phases/16-standardize-alert-path-logging/16-VERIFICATION.md
    .planning/phases/16-standardize-alert-path-logging/16-SECURITY.md
    .planning/phases/16-standardize-alert-path-logging/16-RESEARCH.md
  </read_first>
  <files>internal/handlers/webhook.go, internal/handlers/webhook_test.go</files>
  <behavior>
    - A field value containing spaces survives serialization and parsing intact.
    - Canonical key ordering and current field names stay stable while only the value encoding/quoting rule changes.
    - Regression tests stop relying on `strings.Fields` and instead validate the real serialization contract used by `logAlertEvent`.
  </behavior>
  <action>Change the Phase 16 canonical writer in `internal/handlers/webhook.go` so emitted fields are deterministically parseable even when values contain spaces. Accept either quoted/escaped text values or an equally deterministic field encoding, but keep the overall contract text-based and `key=value`-searchable per the existing Phase 16 direction; do not switch the alert-path logs to JSON and do not rename existing keys. In `internal/handlers/webhook_test.go`, replace `parseWebhookLogFields` so tests decode the same serialization rule the writer now emits, and add regression coverage with a space-containing value such as `error` or `channel_name` to prove the ambiguity is gone. Keep this scoped to webhook alert-path logging and its tests only.</action>
  <verify>
    <automated>go test ./internal/handlers -run "TestWebhookHandler(.*Logging.*|.*SendNotification.*|.*Terminal.*|.*Panic.*)" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "strings\\.Fields" internal/handlers/webhook_test.go` returns no matches for the canonical field parser.
    - `rg -n "parseWebhookLogFields" internal/handlers/webhook_test.go` shows the parser now understands quoted/escaped or otherwise deterministic field encoding rather than naïve whitespace splitting.
    - `rg -n "error|channel_name" internal/handlers/webhook_test.go` finds at least one regression that proves a value containing spaces round-trips through log serialization and parsing.
    - `rg -n "logAlertEvent" internal/handlers/webhook.go` still shows one canonical writer; there is no JSON logger migration and no edits to non-webhook packages.
  </acceptance_criteria>
  <done>The logging contract becomes safely machine-parseable for space-containing values, closing the implementation side of `LOG-01` and threat `T-16-10`.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh verification, security, and UAT truth after the gap fixes</name>
  <read_first>
    .planning/phases/16-standardize-alert-path-logging/16-VERIFICATION.md
    .planning/phases/16-standardize-alert-path-logging/16-SECURITY.md
    .planning/phases/16-standardize-alert-path-logging/16-UAT.md
    .planning/phases/16-standardize-alert-path-logging/16-03-SUMMARY.md
    internal/handlers/webhook.go
    internal/handlers/webhook_test.go
  </read_first>
  <files>.planning/phases/16-standardize-alert-path-logging/16-VERIFICATION.md, .planning/phases/16-standardize-alert-path-logging/16-SECURITY.md, .planning/phases/16-standardize-alert-path-logging/16-UAT.md</files>
  <action>After Task 1 passes and `16-03` has been completed, rerun the focused and broad handler commands needed to prove both remaining blockers are closed, then update the Phase 16 truth artifacts to match that evidence. `16-VERIFICATION.md` must mark the previously failed truths for `OBS-03` and machine-parseable output as verified, remove the two recorded blockers from the open gaps list, and record the exact commands/results used. `16-SECURITY.md` must close `T-16-01` and `T-16-10` with concrete evidence lines that reference the repaired implementation and tests. `16-UAT.md` must be corrected so its operator-facing expectations and results align with the post-fix reality instead of the earlier false pass. Keep the update factual: only mark pass/closed after the commands in this task succeed.</action>
  <verify>
    <automated>go test ./internal/handlers -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/16-standardize-alert-path-logging/16-VERIFICATION.md` no longer lists the `async_panic` correlation gap or the parse-ambiguity gap as open blockers.
    - `.planning/phases/16-standardize-alert-path-logging/16-SECURITY.md` marks `T-16-01` and `T-16-10` closed with evidence tied to current files/commands.
    - `.planning/phases/16-standardize-alert-path-logging/16-UAT.md` is updated from the earlier inaccurate state so its recorded results match the rerun evidence after the fixes.
    - The commands documented in the verification artifacts are the same ones actually run during execution, and `go test ./internal/handlers -count=1` passes.
  </acceptance_criteria>
  <done>Phase 16 truth artifacts become trustworthy again, so later verification/security reruns can pass against the repaired logging contract instead of inheriting stale false positives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical field map -> serialized log line | Internal alert metadata crosses into the observable text format operators and tools will parse. |
| serialized log line -> test parser and verification docs | The same format must be interpreted consistently by automated tests and phase truth artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | S | canonical writer serialization | mitigate | Encode or quote field values so space-containing data remains unambiguous while preserving `key=value` searchability and existing key names. |
| T-16-01 | R | Phase 16 truth artifacts | mitigate | Update verification/security/UAT only after passing commands confirm the async panic correlation fix from `16-03`, preventing stale false positives from persisting. |
| T-16-03 | I | logged field values | mitigate | Keep the format fix limited to encoding existing bounded fields; do not widen the contract to raw payloads or secrets. |
| T-16-09 | D | phase scope | mitigate | Restrict edits to `webhook.go`, `webhook_test.go`, and Phase 16 truth files rather than using the parseability fix as a pretext for repo-wide logging cleanup. |
</threat_model>

<verification>
Run the focused serialization regressions first, then the full `go test ./internal/handlers -count=1` command. Update the truth artifacts only from those observed results.
</verification>

<success_criteria>
`LOG-01` is complete when space-containing field values remain parseable under the canonical writer and the tests prove it. Phase 16 is closeable when `16-VERIFICATION.md`, `16-SECURITY.md`, and `16-UAT.md` all reflect the repaired behavior with matching automated evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/16-standardize-alert-path-logging/16-04-SUMMARY.md`
</output>
