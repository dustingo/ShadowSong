---
phase: 05-normalize-role-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/authz/roles.go
  - internal/authz/roles_test.go
  - internal/models/user.go
  - internal/models/user_test.go
  - internal/handlers/user.go
  - internal/handlers/user_test.go
autonomous: true
requirements:
  - AUTHZ-01
must_haves:
  truths:
    - The backend accepts only `admin`, `operator`, or `viewer` when users are created or updated.
    - Unsupported role strings cannot be persisted through model hooks or user-management handlers.
    - Existing role names remain unchanged in API payloads and stored records.
  artifacts:
    - path: internal/authz/roles.go
      provides: Canonical role constants and supported-role helpers for backend reuse.
    - path: internal/authz/roles_test.go
      provides: Direct unit coverage for the canonical role constants and helper behavior.
    - path: internal/models/user.go
      provides: User model hooks and validation that enforce the supported role set.
    - path: internal/models/user_test.go
      provides: Regression coverage for default role assignment and invalid-role rejection.
    - path: internal/handlers/user_test.go
      provides: Request-level coverage for create/update role validation behavior.
  key_links:
    - from: internal/handlers/user.go
      to: internal/authz/roles.go
      via: shared role validation before user persistence
      pattern: "authz\\."
    - from: internal/models/user.go
      to: internal/authz/roles.go
      via: model hook defaulting and validation
      pattern: "authz\\."
---

<objective>
Create one canonical backend role vocabulary and enforce it on user writes without renaming any existing role values.

Purpose: satisfy AUTHZ-01 while preserving the current login/session contract and giving later phases a single source of truth for role semantics.
Output: exported role helpers, model-level enforcement, and focused tests for invalid-role rejection.
</objective>

<execution_context>
@C:/Users/doumaoxin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/doumaoxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-normalize-role-model/05-RESEARCH.md
@internal/models/user.go
@internal/handlers/user.go

<interfaces>
From `internal/models/user.go`:

```go
type User struct {
	ID           uint
	Username     string
	PasswordHash string
	Name         string
	Email        string
	Role         string
}

func (u *User) BeforeCreate(tx *gorm.DB) error
func (u *User) Validate() error
func (u *User) SetPassword(password string) error
```

From `internal/handlers/user.go`:

```go
func (h *UserHandler) CreateUser(c *gin.Context)
func (h *UserHandler) UpdateUser(c *gin.Context)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the canonical role contract for Phase 5 and later phases</name>
  <read_first>
    - internal/models/user.go
    - .planning/phases/05-normalize-role-model/05-RESEARCH.md
    - .planning/ROADMAP.md
  </read_first>
  <files>internal/authz/roles.go, internal/authz/roles_test.go</files>
  <behavior>
    - Test 1: supported roles are exactly `admin`, `operator`, `viewer`.
    - Test 2: the default role helper resolves empty input to `viewer` without renaming valid existing values.
    - Test 3: helper APIs expose a stable path that later phases can call instead of reusing raw string literals.
  </behavior>
  <action>Create `internal/authz/roles.go` as the canonical backend role contract per AUTHZ-01 and the locked phase constraints, and add `internal/authz/roles_test.go` with direct table-driven coverage for the exported constants/helpers before wiring downstream consumers. Export string constants `RoleAdmin`, `RoleOperator`, and `RoleViewer`; export helpers such as `SupportedRoles()`, `IsSupportedRole(role string)`, and a normalization/default helper that only fills empty roles with `viewer` and does not alias or rename any other value. Keep the accepted strings exactly `admin`, `operator`, `viewer` per D-locked constraint. Do not introduce custom roles, database migrations, or frontend-facing enum churn. This file is the base interface that Phase 6 account flags and Phase 7 capability guards will build on.</action>
  <verify>
    <automated>go test ./internal/authz -run TestRoles -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `internal/authz/roles.go` exports constants for `admin`, `operator`, and `viewer`.
    - `internal/authz/roles_test.go` fails if supported roles or defaulting behavior drift from the canonical contract.
    - No helper in this file maps supported roles to different stored values.
    - `rg "admin|operator|viewer" internal/authz/roles.go` shows the canonical role set and no extra role names.
  </acceptance_criteria>
  <done>The backend has one importable role contract that later plans can reuse without reintroducing scattered string checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce supported roles on model persistence and user write handlers</name>
  <read_first>
    - internal/authz/roles.go
    - internal/models/user.go
    - internal/handlers/user.go
    - .planning/codebase/TESTING.md
  </read_first>
  <files>internal/models/user.go, internal/models/user_test.go, internal/handlers/user.go, internal/handlers/user_test.go</files>
  <behavior>
    - Test 1: `User.Validate()` rejects unsupported roles and defaults empty roles to `viewer`.
    - Test 2: create-user requests with unsupported roles fail before persistence.
    - Test 3: update-user requests cannot save unsupported roles even when issued by an `admin`.
  </behavior>
  <action>Refactor `internal/models/user.go` to depend on `internal/authz/roles.go` instead of the current package-local `validRoles` map. Add save-hook coverage that applies the default role and rejects unsupported roles on both create and update paths, matching the repository pattern used by other validated models. Update `internal/handlers/user.go` so `CreateUser` and `UpdateUser` surface clear 400-level errors for invalid roles before hitting `db.Create` or `db.Save`, while keeping request/response field names and the returned `user.role` value unchanged for compatibility. Add table-driven tests in `internal/models/user_test.go` and focused request tests in `internal/handlers/user_test.go` for model validation, create/update rejection, and valid-role pass cases. Do not change login response shape, JWT claim keys, or frontend storage keys in this plan.</action>
  <verify>
    <automated>go test ./internal/models ./internal/handlers -run "TestUser|TestCreateUser|TestUpdateUser" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test` fails if an unsupported role can be saved through `User` model hooks.
    - `go test` fails if `CreateUser` or `UpdateUser` accepts a role outside `admin|operator|viewer`.
    - `rg "invalid role" internal/models/user.go internal/handlers/user.go` shows explicit rejection paths instead of silent fallback.
  </acceptance_criteria>
  <done>User persistence and user write endpoints consistently enforce the canonical Phase 5 role set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-management request -> persisted `users.role` | Client-controlled JSON can attempt to store unsupported or privilege-escalating role values |
| model hook -> database | Invalid role strings can become durable state if validation is not centralized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | E | `internal/handlers/user.go` | mitigate | Reject unsupported role strings with explicit 400 responses before persistence and cover create/update cases with request tests |
| T-05-02 | T | `internal/models/user.go` | mitigate | Centralize role validation and defaulting in model hooks backed by tests so overlooked write paths cannot persist unsupported roles |
| T-05-03 | R | user write regression coverage | accept | This plan does not add audit logging yet; Phase 7 will handle durable audit trails after the role contract is stable |
</threat_model>

<verification>
Run `go test ./internal/models ./internal/handlers -run "TestUser|TestCreateUser|TestUpdateUser" -count=1` to verify the canonical role contract and invalid-role rejection on write paths.
</verification>

<success_criteria>
- Only `admin`, `operator`, and `viewer` are accepted by user model validation and write handlers.
- The default role remains `viewer`.
- No API or stored role value is renamed in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/05-normalize-role-model/05-01-SUMMARY.md`
</output>
