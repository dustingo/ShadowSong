---
phase: 09-close-v1-1-milestone-tech-debt
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/middleware/authorize.go
  - internal/middleware/authorize_test.go
  - internal/router/router_test.go
  - internal/handlers/user_test.go
  - .planning/codebase/ARCHITECTURE.md
autonomous: true
requirements: [VER-04]
must_haves:
  truths:
    - "后端权限收口的公开 seam 只剩 capability-first 路径，不再保留未被实际路由使用的 role-based 兼容分支作为漂移点。"
    - "禁用账号、强制改密受限模式和关键拒绝/允许审计路径在 authz seam cleanup 后仍有明确的自动化回归覆盖。"
    - "代码与代码库文档对当前权限实现的一致描述都是 `JWTAuth + RequireCapability`，而不是混杂旧的 `RequireRole` 说法。"
  artifacts:
    - path: "internal/middleware/authorize.go"
      provides: "capability-only authorization middleware seam without stale role-based compatibility wrapper"
    - path: "internal/router/router_test.go"
      provides: "route-level security regression coverage for forced-reset/disabled/capability-protected endpoints"
    - path: "internal/handlers/user_test.go"
      provides: "audit-log regression coverage for critical allowed and denied user-security actions"
    - path: ".planning/codebase/ARCHITECTURE.md"
      provides: "codebase map aligned with the real capability-based backend authz path"
  key_links:
    - from: "internal/router/router.go"
      to: "internal/middleware/authorize.go"
      via: "protected routes enforced through capability middleware only"
      pattern: "RequireCapability"
    - from: "internal/router/router_test.go"
      to: "internal/middleware/auth.go"
      via: "disabled and forced-reset requests exercising the runtime auth boundary"
      pattern: "account disabled|forced reset"
    - from: "internal/handlers/user_test.go"
      to: "internal/handlers/user.go"
      via: "audit evidence for self-protection and admin-managed account-control flows"
      pattern: "user.disable|user.role_change|user.password_change"
---

<objective>
把当前树里真实还存在的权限实现漂移收口掉：退役未被使用的 `RequireRole` 兼容层，统一为 capability-first authz seam，并把 VER-04 相关关键安全路径锁进自动化回归。

Purpose: `user.go` 里的 raw role check 已经不再是问题，但后端 authz 仍保留一个旧的 role-based middleware seam 和相应陈旧描述。Phase 9 应收掉这个真实漂移点，而不是继续围绕一个已解决的问题空转。
Output: capability-only middleware seam、更新后的 security regression tests、与当前实现一致的 codebase architecture note。
</objective>

<context>
@internal/middleware/authorize.go
@internal/middleware/authorize_test.go
@internal/router/router.go
@internal/router/router_test.go
@internal/handlers/user.go
@internal/handlers/user_test.go
@internal/middleware/auth.go
@.planning/codebase/ARCHITECTURE.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/08-ship-permission-aware-ui-and-verification/08-VERIFICATION.md

<interfaces>
From internal/middleware/authorize.go:
```go
func RequireCapability(capability authz.Capability) gin.HandlerFunc
```

From internal/router/router.go:
```go
users.GET("", middleware.RequireCapability(authz.CapabilityManageUsers), userHandler.ListUsers)
alerts.POST("/:id/ack", middleware.RequireCapability(authz.CapabilityProcessAlerts), alertHandler.Ack)
datasources.POST("", middleware.RequireCapability(authz.CapabilityManageConfig), configHandler.CreateDataSource)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retire the stale role-based compatibility middleware and document the capability-only backend seam</name>
  <files>internal/middleware/authorize.go, internal/middleware/authorize_test.go, .planning/codebase/ARCHITECTURE.md</files>
  <behavior>
    - Test 1: Capability middleware still returns `401` without a principal, `403` without capability, and `200` for allowed principals.
    - Test 2: No exported `RequireRole` compatibility shim remains in the backend authz seam after cleanup.
  </behavior>
  <action>Remove the unused `RequireRole(...)` compatibility wrapper from `internal/middleware/authorize.go` and keep `RequireCapability(...)` as the single route-facing authorization entry point. Simplify `internal/middleware/authorize_test.go` so it asserts the surviving capability middleware contract only. Update `.planning/codebase/ARCHITECTURE.md` anywhere it still describes protected routes as using `RequireRole`; the codebase map should match the current tree and explicitly describe `JWTAuth` plus `RequireCapability` as the backend authz path. Do not redesign router policy or change capability names.</action>
  <verify>
    <automated>go test ./internal/middleware -count=1</automated>
    <automated>powershell -Command "$hits = rg -n '\\bRequireRole\\b' internal .planning/codebase/ARCHITECTURE.md; if ($LASTEXITCODE -eq 0) { $hits; exit 1 }"</automated>
  </verify>
  <done>The backend no longer advertises or ships a stale role-based middleware seam alongside the capability baseline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strengthen backend regression coverage for VER-04-critical security paths at the capability boundary</name>
  <files>internal/router/router_test.go, internal/handlers/user_test.go</files>
  <behavior>
    - Test 1: Disabled users are rejected on representative protected routes after auth middleware and capability checks compose.
    - Test 2: Forced-reset users remain limited to allowed self-service paths.
    - Test 3: Critical allowed and denied user-security actions still emit the expected audit log records.
  </behavior>
  <action>Expand the existing backend regression suites so the authz seam cleanup is tied to real VER-04 evidence instead of a vacuous grep. In `internal/router/router_test.go`, add or tighten route-level cases that prove disabled accounts and forced-reset users behave correctly on capability-protected endpoints, not just ordinary authenticated paths. In `internal/handlers/user_test.go`, keep the existing user-security tests but make the audit assertions explicit for the key actions that matter to VER-04: denied self-disable or self-role-change, successful admin-managed security changes, and successful self-service password change clearing forced-reset state. Keep the scope narrow to verification hardening around existing behavior; do not redesign handlers or route structure.</action>
  <verify>
    <automated>go test ./internal/router ./internal/handlers ./internal/middleware -count=1 -run "Test(RouterCapabilityProtectedRoutes|AdminUpdateUser|UpdateOwnPassword|AdminUpdateUserDeniedWritesAuditLog)"</automated>
    <automated>powershell -Command "$router = Get-Content 'internal/router/router_test.go' -Raw; $user = Get-Content 'internal/handlers/user_test.go' -Raw; if ($router -notmatch 'disabled' -or $router -notmatch 'forced reset') { Write-Error 'router VER-04 security cases incomplete'; exit 1 }; if ($user -notmatch 'user.disable' -or $user -notmatch 'user.role_change' -or $user -notmatch 'user.password_change') { Write-Error 'audit assertions incomplete'; exit 1 }"</automated>
  </verify>
  <done>Phase 9 closes a real authz drift point and leaves VER-04 anchored to concrete backend security regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| router-facing authz API -> protected endpoints | Routes should expose one clear authorization seam so policy drift does not hide behind unused compatibility wrappers |
| auth middleware state -> security-sensitive handlers | Disabled-account denial, forced-reset limited mode, and audit logging must survive authz seam cleanup unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | T | stale `RequireRole` compatibility path in `internal/middleware/authorize.go` | mitigate | Remove the unused role-based wrapper and leave capability middleware as the single backend authz seam |
| T-09-07 | R | docs/code drift about how routes are actually protected | mitigate | Update `authorize_test.go` and `.planning/codebase/ARCHITECTURE.md` to describe only the real `JWTAuth + RequireCapability` flow |
| T-09-08 | E | authz cleanup accidentally weakens disabled/forced-reset or audit-covered security behavior | mitigate | Expand router and handler regression coverage for disabled-user denial, forced-reset limits, and audit-log critical actions |
</threat_model>

<verification>
- `go test ./internal/middleware -count=1`
- `go test ./internal/router ./internal/handlers ./internal/middleware -count=1 -run "Test(RouterCapabilityProtectedRoutes|AdminUpdateUser|UpdateOwnPassword|AdminUpdateUserDeniedWritesAuditLog)"`
- `powershell -Command "$hits = rg -n '\\bRequireRole\\b' internal .planning/codebase/ARCHITECTURE.md; if ($LASTEXITCODE -eq 0) { $hits; exit 1 }"`
- `powershell -Command "$router = Get-Content 'internal/router/router_test.go' -Raw; $user = Get-Content 'internal/handlers/user_test.go' -Raw; if ($router -notmatch 'disabled' -or $router -notmatch 'forced reset') { exit 1 }; if ($user -notmatch 'user.disable' -or $user -notmatch 'user.role_change' -or $user -notmatch 'user.password_change') { exit 1 }"`
</verification>

<success_criteria>
- The backend authz seam is capability-only, with no stale exported `RequireRole` compatibility layer left behind.
- Route and handler regression coverage explicitly protects VER-04 critical paths during and after the cleanup.
- Codebase architecture notes no longer describe a role-based route-guard path that the current tree no longer uses.
</success_criteria>

<output>
After completion, create `.planning/phases/09-close-v1-1-milestone-tech-debt/09-03-SUMMARY.md`
</output>
