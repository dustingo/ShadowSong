---
phase: 02-remove-frontend-ai-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
  - frontend/src/pages/index.ts
  - frontend/src/pages/AIAssistant.tsx
autonomous: true
requirements:
  - FEAI-01
must_haves:
  truths:
    - "Authenticated users no longer see an AI menu item or /ai route in the frontend shell."
    - "The app shell title matches the non-AI alert-system positioning on screens touched by route cleanup."
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Route table and sidebar menu without AI entry points"
    - path: "frontend/src/pages/index.ts"
      provides: "Page barrel without AIAssistant export"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/index.ts"
      via: "named page imports"
      pattern: "AIAssistant"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/AIAssistant.tsx"
      via: "protected route registration"
      pattern: "path=\"/ai\""
---

<objective>
Remove the frontend AI entry surface so the authenticated shell no longer exposes AI navigation or the AI page.

Purpose: Fulfill FEAI-01 at the route/menu boundary with the smallest possible file ownership on a dirty worktree.
Output: Updated shell routing/menu files and deleted AI page entry file.
</objective>

<execution_context>
@C:/Users/doumaoxin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/doumaoxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-remove-frontend-ai-surfaces/02-RESEARCH.md
@.planning/phases/02-remove-frontend-ai-surfaces/02-UI-SPEC.md
@.planning/phases/01-remove-backend-ai-runtime/01-03-SUMMARY.md
@frontend/src/App.tsx
@frontend/src/pages/index.ts
@frontend/src/pages/AIAssistant.tsx

<interfaces>
From frontend/src/App.tsx:
```tsx
const menuItems = [
  { key: '/', icon: <DashboardOutlined />, label: '告警大盘' },
  { key: '/alerts', icon: <AlertOutlined />, label: '告警管理' },
  { key: '/ai', icon: <RobotOutlined />, label: 'AI 助手' },
]

<Route path="/ai" element={<RequireAuth><AIAssistant /></RequireAuth>} />
```

From frontend/src/pages/index.ts:
```ts
export { AIAssistant } from './AIAssistant'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the AI page export surface</name>
  <read_first>
    - frontend/src/pages/index.ts
    - frontend/src/pages/AIAssistant.tsx
    - .planning/phases/02-remove-frontend-ai-surfaces/02-RESEARCH.md
  </read_first>
  <files>frontend/src/pages/index.ts, frontend/src/pages/AIAssistant.tsx</files>
  <acceptance_criteria>
    - `frontend/src/pages/index.ts` no longer exports `AIAssistant`.
    - `frontend/src/pages/AIAssistant.tsx` is removed from the repo.
    - No replacement page or placeholder is introduced; the capability is deleted per FEAI-01.
  </acceptance_criteria>
  <action>Delete the `AIAssistant` barrel export and remove `frontend/src/pages/AIAssistant.tsx` entirely. Do not replace it with a stub route or an empty component. This is a full removal per FEAI-01 and Phase 1 already proved the backend AI endpoints are gone, so any retained page would only become a broken dead end.</action>
  <verify>
    <automated>powershell -NoProfile -Command "if (Test-Path 'frontend/src/pages/AIAssistant.tsx') { exit 1 }; rg -n 'AIAssistant' frontend/src/pages/index.ts"</automated>
  </verify>
  <done>`frontend/src/pages/AIAssistant.tsx` no longer exists and the pages barrel has no `AIAssistant` export.</done>
</task>

<task type="auto">
  <name>Task 2: Remove shell route, menu item, icon, and touched AI title copy</name>
  <read_first>
    - frontend/src/App.tsx
    - .planning/phases/02-remove-frontend-ai-surfaces/02-UI-SPEC.md
  </read_first>
  <files>frontend/src/App.tsx</files>
  <acceptance_criteria>
    - The sidebar no longer shows an AI-related item.
    - The protected route table no longer registers `/ai`.
    - Unused `RobotOutlined` and `AIAssistant` imports are removed.
    - The touched shell title string uses non-AI wording per the UI-SPEC naming contract.
  </acceptance_criteria>
  <action>Update `frontend/src/App.tsx` to remove the `/ai` menu item, `RobotOutlined`, the `AIAssistant` import, and the `/ai` protected route registration. Keep the existing shell layout, navigation order, auth guard, and non-AI routes unchanged. While this file is open, change the brand/title string from `游戏运维 AI 告警系统` to non-AI wording per FEAI-01 and the approved UI-SPEC; do not broaden this into general copy editing beyond the strings touched by the route cleanup.</action>
  <verify>
    <automated>powershell -NoProfile -Command "rg -n '/ai|AIAssistant|RobotOutlined|AI 助手|游戏运维 AI 告警系统' frontend/src/App.tsx; if ($LASTEXITCODE -eq 0) { exit 1 }"</automated>
  </verify>
  <done>`frontend/src/App.tsx` contains no AI page/menu wiring and still defines the authenticated shell for the remaining modules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user -> React Router shell | Navigation targets must stay aligned with actual route registrations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | E | `frontend/src/App.tsx` | mitigate | Remove the `/ai` route and matching menu item together so no protected but unintended surface remains reachable |
| T-02-02 | T | `frontend/src/App.tsx` | mitigate | Limit shell edits to the AI-specific import/menu/title strings and preserve existing route order plus `RequireAuth` usage |
</threat_model>

<verification>
Confirm the AI route entry cluster is gone by grep before downstream plans remove shared types or API contracts.
</verification>

<success_criteria>
`frontend/src/App.tsx` and `frontend/src/pages/index.ts` no longer reference the AI page, and the AI page file itself is removed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-remove-frontend-ai-surfaces/02-01-SUMMARY.md`
</output>
