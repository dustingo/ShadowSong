---
phase: 17-clean-truth-and-operational-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/TESTING.md
  - scripts/verify_backend_no_ai.ps1
  - scripts/verify_frontend_no_ai.ps1
  - scripts/verify_backend_alert_flow.ps1
  - scripts/verify_frontend_console_baseline.ps1
  - internal/config/config_test.go
  - internal/router/router_test.go
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "仓库内可执行的后端与前端验证入口使用当前命名，且所有 repo 内引用同步到新脚本路径。"
    - "低风险测试名和 codebase/testing 入口描述不再把当前基线写成 AI 移除对照语境。"
    - "本计划只触达低风险命名表面，不改动 `go.mod`、Go import、JWT issuer 等运行时契约。"
  artifacts:
    - path: "scripts/verify_backend_alert_flow.ps1"
      provides: "后端告警主链路验证入口"
    - path: "scripts/verify_frontend_console_baseline.ps1"
      provides: "前端控制台构建与残留扫描入口"
  key_links:
    - from: ".planning/codebase/TESTING.md"
      to: "scripts/verify_frontend_console_baseline.ps1"
      via: "verification entrypoints"
      pattern: "verify_frontend_console_baseline\\.ps1"
    - from: ".planning/codebase/STRUCTURE.md"
      to: "scripts/verify_backend_alert_flow.ps1"
      via: "repo entrypoint map"
      pattern: "verify_backend_alert_flow\\.ps1"
---

<objective>
先完成低风险验证入口与测试命名清理，把可执行脚本和 codebase/testing 地图收口到当前基线命名。

Purpose: 满足 D-02、D-08，并遵守 D-07 的高风险边界，让后续 README/planning truth refresh 和 runbook 能引用稳定的新验证入口。
Output: 已改名的验证脚本、同步更新的 codebase/testing 引用、以及采用当前基线命名的低风险测试标识。
</objective>

<execution_context>
@C:/Users/doumaoxin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/doumaoxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-clean-truth-and-operational-docs/17-CONTEXT.md
@.planning/phases/17-clean-truth-and-operational-docs/17-RESEARCH.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/TESTING.md
@scripts/verify_backend_no_ai.ps1
@scripts/verify_frontend_no_ai.ps1
@internal/config/config_test.go
@internal/router/router_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename low-risk verification entrypoints and eliminate repo-visible old script names</name>
  <files>scripts/verify_backend_no_ai.ps1, scripts/verify_frontend_no_ai.ps1, scripts/verify_backend_alert_flow.ps1, scripts/verify_frontend_console_baseline.ps1, .planning/codebase/STRUCTURE.md, .planning/codebase/TESTING.md, internal/config/config_test.go, internal/router/router_test.go</files>
  <read_first>scripts/verify_backend_no_ai.ps1, scripts/verify_frontend_no_ai.ps1, .planning/codebase/STRUCTURE.md, .planning/codebase/TESTING.md, internal/config/config_test.go, internal/router/router_test.go, .planning/phases/17-clean-truth-and-operational-docs/17-CONTEXT.md, .planning/phases/17-clean-truth-and-operational-docs/17-RESEARCH.md</read_first>
  <action>Per D-02 and D-08, rename the PowerShell verification entrypoints to current-state names: `scripts/verify_backend_no_ai.ps1` -> `scripts/verify_backend_alert_flow.ps1` and `scripts/verify_frontend_no_ai.ps1` -> `scripts/verify_frontend_console_baseline.ps1`. Update the script internals so temp log filenames, generated temp filenames, run IDs, and human-readable step labels no longer contain `no_ai` or `VerifyNoAI`. Update the repo-owned reference maps in `.planning/codebase/STRUCTURE.md` and `.planning/codebase/TESTING.md` to the new paths, and rename the low-risk test identifiers `TestLoad_WithoutAIEnv` and `TestSetup_RoutesWithoutAIRuntime` to current baseline names without changing assertions or runtime behavior. Do not touch `README.md`, planning truth docs, `go.mod`, Go import paths, JWT issuer, DB names, or any live runtime contract in this plan.</action>
  <acceptance_criteria>
    - `Test-Path scripts/verify_backend_alert_flow.ps1` and `Test-Path scripts/verify_frontend_console_baseline.ps1` both return true.
    - `rg -n "verify_backend_no_ai|verify_frontend_no_ai|WithoutAI|WithoutAIRuntime|VerifyNoAI|verify_no_ai" .planning/codebase scripts internal/config internal/router` returns no matches.
    - `rg -n "verify_backend_alert_flow|verify_frontend_console_baseline|CurrentBaseline|AlertFlow" .planning/codebase scripts internal/config internal/router` returns the expected new names.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "if (-not (Test-Path 'scripts/verify_backend_alert_flow.ps1')) { exit 1 }; if (-not (Test-Path 'scripts/verify_frontend_console_baseline.ps1')) { exit 1 }; rg -n 'verify_backend_no_ai|verify_frontend_no_ai|WithoutAI|WithoutAIRuntime|VerifyNoAI|verify_no_ai' .planning/codebase scripts internal/config internal/router; if ($LASTEXITCODE -eq 0) { exit 1 }; rg -n 'verify_backend_alert_flow|verify_frontend_console_baseline|CurrentBaseline|AlertFlow' .planning/codebase scripts internal/config internal/router"</automated>
  </verify>
  <done>New verification script filenames are the repo-owned executable entrypoints and the renamed tests/codebase maps now describe the current baseline instead of AI-removal context.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer→repo entrypoints | Maintainers rely on README, planning docs, and script names to decide what to run. |
| archive→current truth | Historical milestone/review language can be mistaken for live operational guidance if not framed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | R | codebase/testing maps | mitigate | Replace repo-owned reference-map wording that still implies AI-removal framing, and point executable references to the renamed script files per D-02/D-08. |
| T-17-02 | T | script path references | mitigate | Rename the script files and update every in-repo reference atomically so documentation cannot point to stale executable names. |
| T-17-03 | D | maintainer verification workflow | mitigate | Add grep-based acceptance checks proving old script names and old test identifiers are gone from maintainer-visible repo surfaces. |
| T-17-04 | S | historical naming boundaries | mitigate | Explicitly document `go.mod` module path and JWT issuer as deferred runtime contracts per D-07/D-09 instead of attempting silent renames. |
</threat_model>

<verification>
Run the grep/file existence command from the task. If the repo has Go available, re-run `go test ./internal/config ./internal/router -count=1` after the test renames to prove only identifiers changed.
</verification>

<success_criteria>
Verification scripts, low-risk test names, and codebase/testing maps all use the new low-risk naming, without crossing into runtime-identity changes reserved for deferred work.
</success_criteria>

<output>
After completion, create `.planning/phases/17-clean-truth-and-operational-docs/17-01-SUMMARY.md`
</output>
