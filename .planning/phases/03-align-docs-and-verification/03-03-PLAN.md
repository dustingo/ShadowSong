---
phase: 03-align-docs-and-verification
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - scripts/verify_backend_no_ai.ps1
  - scripts/verify_frontend_no_ai.ps1
  - .planning/phases/03-align-docs-and-verification/03-VERIFICATION.md
autonomous: true
requirements:
  - DATA-01
  - VER-01
  - VER-02
must_haves:
  truths:
    - "后端存在一条可重复执行的非 AI 验证路径，并在 Phase 3 结束时再次给出结果"
    - "前端存在一条显式的非 AI 验证路径，不依赖新增测试框架也能证明构建/静态检查通过"
    - "Phase 3 有最终验证证据，能够同时说明后端和前端都在无 AI 状态下工作"
  artifacts:
    - path: scripts/verify_backend_no_ai.ps1
      provides: "无 AI 后端闭环验证入口"
    - path: scripts/verify_frontend_no_ai.ps1
      provides: "无 AI 前端构建与残留扫描验证入口"
    - path: .planning/phases/03-align-docs-and-verification/03-VERIFICATION.md
      provides: "Phase 3 最终验证证据"
  key_links:
    - from: scripts/verify_backend_no_ai.ps1
      to: cmd/server/main.go
      via: "真实服务启动与 API 校验"
      pattern: "go run cmd/server/main.go|/api/v1/ai/chat"
    - from: scripts/verify_frontend_no_ai.ps1
      to: frontend/package.json
      via: "pnpm build"
      pattern: "pnpm build|rg"
    - from: .planning/phases/03-align-docs-and-verification/03-VERIFICATION.md
      to: scripts/verify_backend_no_ai.ps1
      via: "命令输出证据"
      pattern: "verify_backend_no_ai|verify_frontend_no_ai"
---

<objective>
建立并执行 Phase 3 的显式非 AI 验证路径，形成阶段收尾证据，证明后端和前端在无 AI 状态下都可验证。

Purpose: 完成 VER-01 与 VER-02 的阶段级封口，让“无 AI 版本”不仅在代码上清理完成，也有可重复执行的验证资产。
Output: 更新后的 `scripts/verify_backend_no_ai.ps1`、新增 `scripts/verify_frontend_no_ai.ps1`、新增 `03-VERIFICATION.md`。
</objective>

<execution_context>
@C:/Users/doumaoxin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/doumaoxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-align-docs-and-verification/03-RESEARCH.md
@.planning/phases/01-remove-backend-ai-runtime/01-VERIFICATION.md
@.planning/phases/02-remove-frontend-ai-surfaces/02-VERIFICATION.md
@scripts/verify_backend_no_ai.ps1
@frontend/package.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 固化前后端的非 AI 验证入口</name>
  <files>scripts/verify_backend_no_ai.ps1, scripts/verify_frontend_no_ai.ps1</files>
  <read_first>
    scripts/verify_backend_no_ai.ps1
    .planning/phases/01-remove-backend-ai-runtime/01-VERIFICATION.md
    .planning/phases/02-remove-frontend-ai-surfaces/02-VERIFICATION.md
    frontend/package.json
  </read_first>
  <action>保留现有后端 PowerShell 闭环脚本的主路径，但把其中仍使用 AI 品牌化数据库名或仅用于清空历史 AI 配置的说明更新为“无 AI 告警系统”语义；新增 `scripts/verify_frontend_no_ai.ps1`，执行前端依赖前提检查、`pnpm build`，并对 `frontend/` 与 `frontend/index.html` 做 AI 残留 grep 扫描，返回非零退出码以阻断失败。不要引入新的前端测试框架；前端验证以现有构建门和残留扫描为准。</action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/verify_frontend_no_ai.ps1</automated>
  </verify>
  <acceptance_criteria>存在两个可直接运行的验证脚本：后端脚本继续验证无 AI API/闭环，前端脚本成功跑过 `pnpm build` 且 AI 残留扫描通过；新增脚本失败时会返回非零退出码。</acceptance_criteria>
  <done>仓库具备显式、可复用的前后端非 AI 验证入口，执行者无需自行拼装命令链即可验证 Phase 3 的结果。</done>
</task>

<task type="auto">
  <name>Task 2: 执行验证并产出 Phase 3 证据报告</name>
  <files>.planning/phases/03-align-docs-and-verification/03-VERIFICATION.md</files>
  <read_first>
    scripts/verify_backend_no_ai.ps1
    scripts/verify_frontend_no_ai.ps1
    .planning/phases/01-remove-backend-ai-runtime/01-VERIFICATION.md
    .planning/phases/02-remove-frontend-ai-surfaces/02-VERIFICATION.md
  </read_first>
  <action>执行后端与前端的非 AI 验证命令，并把命令、结果、关键证据和 requirement 覆盖写入 `.planning/phases/03-align-docs-and-verification/03-VERIFICATION.md`。报告必须明确记录后端命令、前端命令、是否通过、关键输出摘要，以及仍然接受的非阻塞残余风险。若命令失败，先修复脚本或说明阻塞原因，不要伪造通过结论。</action>
  <verify>
    <automated>pwsh -NoProfile -ExecutionPolicy Bypass -Command "& { ./scripts/verify_backend_no_ai.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; ./scripts/verify_frontend_no_ai.ps1; exit $LASTEXITCODE }"</automated>
  </verify>
  <acceptance_criteria>`03-VERIFICATION.md` 存在，并明确记录后端与前端各至少一条通过的非 AI 验证路径、执行时间和结果摘要；双脚本串行执行成功。</acceptance_criteria>
  <done>Phase 3 结束时存在可追溯的最终验证证据，能证明无 AI 版本的后端与前端都已通过显式验证。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification script→local services | 脚本直接启动/访问本地后端、数据库、前端构建环境 |
| verification report→phase closure decision | 报告是是否认为 Phase 3 收尾完成的重要依据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | T | scripts/verify_frontend_no_ai.ps1 | mitigate | 脚本必须执行真实 `pnpm build` 和 AI 残留扫描，失败返回非零，避免只写占位验证 |
| T-03-08 | R | .planning/phases/03-align-docs-and-verification/03-VERIFICATION.md | mitigate | 报告只记录实际执行过的命令与结果摘要，不允许写未执行的通过结论 |
| T-03-09 | D | scripts/verify_backend_no_ai.ps1 | accept | 后端脚本依赖本地 Docker/数据库/Redis；若环境不可用可阻塞执行，但必须在报告中显式记录，而不是降级为假验证 |
</threat_model>

<verification>
`powershell -ExecutionPolicy Bypass -File scripts/verify_backend_no_ai.ps1`

`powershell -ExecutionPolicy Bypass -File scripts/verify_frontend_no_ai.ps1`
</verification>

<success_criteria>
Phase 3 完成时，后端和前端都各有一条显式的非 AI 验证命令可重复执行，且 `.planning/phases/03-align-docs-and-verification/03-VERIFICATION.md` 记录了实际通过的结果和证据摘要。
</success_criteria>

<output>
After completion, create `.planning/phases/03-align-docs-and-verification/03-03-SUMMARY.md`
</output>
