---
phase: 16-standardize-alert-path-logging
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - internal/handlers/webhook_test.go
  - .planning/phases/16-standardize-alert-path-logging/16-VERIFICATION.md
autonomous: true
requirements:
  - OBS-03
  - LOG-03
requirements_addressed:
  - OBS-03
  - LOG-03
must_haves:
  truths:
    - "日志契约在自动化测试里被明确定义，后续修改不能再随意漂移字段名或把机器值塞回消息文本。"
    - "Phase 16 留下可复用的验证记录，明确说明如何从 `terminal_failure` 或 `send_attempt` 通过 `trace_id` 回查到 webhook ingest 生命周期。"
    - "验证产物只覆盖 webhook-to-notification logging contract，不声称已完成 repo-wide 日志统一或新日志平台接入。"
  artifacts:
    - path: internal/handlers/webhook_test.go
      provides: "字段级 logging contract regression tests"
    - path: .planning/phases/16-standardize-alert-path-logging/16-VERIFICATION.md
      provides: "Phase 16 logging contract truth, sample fields, and operator troubleshooting path"
  key_links:
    - from: internal/handlers/webhook_test.go
      to: internal/handlers/webhook.go
      via: "contract assertions for canonical field names and preserved stage taxonomy"
      pattern: "matched_channels|mode|channel_type|terminal_failure|trace_id"
    - from: .planning/phases/16-standardize-alert-path-logging/16-VERIFICATION.md
      to: .planning/phases/14-establish-alert-trace-context/14-VERIFICATION.md
      via: "trace-based upstream troubleshooting narrative"
      pattern: "trace_id"
    - from: .planning/phases/16-standardize-alert-path-logging/16-VERIFICATION.md
      to: .planning/phases/15-harden-notification-retry-boundaries/15-VERIFICATION.md
      via: "retry and terminal failure carry-forward contract"
      pattern: "attempt|max_attempts|terminal_failure"
---

<objective>
把 Phase 16 的 alert-path logging contract 锁进自动化测试和验证文档，避免 Phase 14/15/16 形成的可观测性契约再次回到“靠记忆维护”。

Purpose: 满足 LOG-03，并把 OBS-03 的诊断路径沉淀成仓库内真源，使后续开发者能按同一字段契约扩展日志而不是重新发明格式。
Output: handler contract tests、`16-VERIFICATION.md`、以及一套明确的回归命令和日志字段样例。
</objective>

<execution_context>
@C:/Users/doumaoxin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/doumaoxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-standardize-alert-path-logging/16-RESEARCH.md
@.planning/phases/16-standardize-alert-path-logging/16-01-PLAN.md
@.planning/phases/14-establish-alert-trace-context/14-VERIFICATION.md
@.planning/phases/15-harden-notification-retry-boundaries/15-VERIFICATION.md
@internal/handlers/webhook.go
@internal/handlers/webhook_test.go

<interfaces>
Expected contract after `16-01`:
```text
stage
trace_id
alert_id
fingerprint
source
channel_id
channel_name
channel_type
attempt
max_attempts
error
matched_channels
mode
```

Stage names that must remain searchable:
```text
ingest
persist
dedup
redis_publish
route_match
notification_entry
send_attempt
send_notification
terminal_failure
async_panic
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the standardized logging contract with field-level handler regressions</name>
  <read_first>
    .planning/phases/16-standardize-alert-path-logging/16-01-PLAN.md
    internal/handlers/webhook.go
    internal/handlers/webhook_test.go
    .planning/phases/14-establish-alert-trace-context/14-VERIFICATION.md
    .planning/phases/15-harden-notification-retry-boundaries/15-VERIFICATION.md
  </read_first>
  <files>internal/handlers/webhook_test.go</files>
  <behavior>
    - Test route-match logs assert `matched_channels` as a named field while preserving `stage=route_match`.
    - Test notification send logs assert `mode`, `channel_type`, `attempt`, `max_attempts`, and `trace_id` on retry and terminal-failure paths.
    - Test contract assertions do not depend on the exact human message wording when a named field already exists.
  </behavior>
  <action>Extend `internal/handlers/webhook_test.go` so the Phase 16 contract is enforced at field level rather than only by coarse substring smoke checks. Keep the tests focused on webhook alert-path behavior and reuse the existing logger-buffer seam. Add or tighten assertions for the canonical field names introduced by `16-01`, especially `matched_channels`, `mode`, and `channel_type`, while preserving Phase 14/15 guarantees around `stage`, `trace_id`, `attempt`, `max_attempts`, and `terminal_failure`. Prefer assertions that prove machine-readable values exist as fields, and avoid over-coupling tests to exact English/Chinese message text if the message is no longer the contract source of truth.</action>
  <verify>
    <automated>go test ./internal/handlers -run "TestWebhookHandler(.*Lifecycle.*|.*Route.*|.*SendNotification.*|.*Terminal.*|.*Panic.*)" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "matched_channels|mode|channel_type" internal/handlers/webhook_test.go` finds explicit field-level assertions for the Phase 16 contract.
    - At least one handler test asserts `stage=route_match` together with `matched_channels=`.
    - At least one handler test asserts `stage=terminal_failure` or `stage=send_attempt` together with `mode=`, `attempt=`, and `max_attempts=`.
    - The tightened tests still use the logger buffer seam in `newWebhookTestHandler` and do not introduce network dependencies or manual-only checks.
    - No frontend tests, websocket tests, or unrelated backend packages are modified in this task.
  </acceptance_criteria>
  <done>The Phase 16 logging contract is pinned by focused handler regressions that fail if canonical field names drift or move back into free-text-only messages.</done>
</task>

<task type="auto">
  <name>Task 2: Record Phase 16 verification truth and operator search path</name>
  <read_first>
    .planning/phases/16-standardize-alert-path-logging/16-RESEARCH.md
    .planning/phases/14-establish-alert-trace-context/14-VERIFICATION.md
    .planning/phases/15-harden-notification-retry-boundaries/15-VERIFICATION.md
    internal/handlers/webhook.go
    internal/handlers/webhook_test.go
  </read_first>
  <files>.planning/phases/16-standardize-alert-path-logging/16-VERIFICATION.md</files>
  <action>Create `.planning/phases/16-standardize-alert-path-logging/16-VERIFICATION.md` after the contract tests pass. Document the exact automated commands used, the canonical field vocabulary for webhook-to-notification logs, and a concise troubleshooting path that starts from `terminal_failure` or `send_attempt` logs and walks backward via `trace_id` to `notification_entry`, `route_match`, `redis_publish`, `persist`, and `ingest`. Include representative field examples for LOG-03, but do not paste full raw alert payloads or claim repo-wide logging standardization. State explicitly that this phase keeps the existing `log.Logger` seam and remains scoped to `internal/handlers/webhook.go` alert-path logging.</action>
  <verify>
    <automated>go test ./internal/handlers -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/16-standardize-alert-path-logging/16-VERIFICATION.md` exists and names the canonical Phase 16 field set, including `stage`, `trace_id`, `matched_channels`, `mode`, and retry-related fields.
    - The verification doc references both Phase 14 and Phase 15 verification artifacts as the upstream carry-forward contract for trace and retry observability.
    - The verification doc explicitly states the phase stays on `log.Logger` and does not introduce `slog`, JSON logs, websocket cleanup, or external observability infrastructure.
    - The verification doc includes at least one example search path from `terminal_failure` back to `ingest` using `trace_id`.
    - The final verification command is automated and backend-only; there is no manual-only gate.
  </acceptance_criteria>
  <done>Phase 16 leaves a reusable verification artifact that documents the standardized alert-path logging contract and the operator diagnosis path without expanding scope beyond webhook-to-notification logging.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implementation log contract -> regression tests | Tests become the enforcement layer for future logging changes. |
| verification document -> operator troubleshooting | Maintainers rely on docs to search the right fields and stages under pressure. |
| phase-local logging truth -> future phases | Later work may build on these artifacts as the canonical contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | R | `internal/handlers/webhook_test.go` | mitigate | Add field-level assertions for `matched_channels`, `mode`, `channel_type`, `trace_id`, and `terminal_failure` so contract drift becomes test-visible. |
| T-16-07 | T | `16-VERIFICATION.md` | mitigate | Write the doc only after automated tests pass and tie it directly to Phase 14/15 verification truth instead of speculative prose. |
| T-16-08 | I | verification examples | mitigate | Keep examples limited to field names and bounded values; do not include raw payloads, secrets, or full alert bodies. |
| T-16-09 | D | scope interpretation | mitigate | State explicitly in the verification artifact that Phase 16 covers only webhook-to-notification logging and does not imply repo-wide standardization. |
| T-16-10 | S | future logging extensions | mitigate | Document the canonical field vocabulary so later contributors extend the same contract rather than inventing incompatible names. |
</threat_model>

<verification>
Run the focused contract tests first, then the broader `go test ./internal/handlers -count=1` command. Only after both are green should `16-VERIFICATION.md` be finalized so the document reflects executed behavior.
</verification>

<success_criteria>
`LOG-03` and the verification/documentation side of `OBS-03` are satisfied when handler tests lock the canonical field names and `16-VERIFICATION.md` shows exactly how to search a failed notification backward through the existing Phase 14/15 lifecycle using `trace_id`.
</success_criteria>

<output>
After completion, create `.planning/phases/16-standardize-alert-path-logging/16-02-SUMMARY.md`
</output>
