---
phase: 04-enable-raw-event-passthrough-in-notification-templates
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - scripts/verify_template_passthrough.ps1
  - .planning/phases/04-enable-raw-event-passthrough-in-notification-templates/04-VERIFICATION.md
autonomous: true
requirements:
  - TMPL-02
  - TMPL-03
must_haves:
  truths:
    - There is an executable verification path proving a template can render raw webhook fields end to end.
    - There is an executable verification path proving legacy standard-field templates still work after the contract change.
    - Phase 04 closes with evidence, not just code changes.
  artifacts:
    - path: scripts/verify_template_passthrough.ps1
      provides: Repeatable backend/frontend verification flow for raw passthrough and compatibility.
    - path: .planning/phases/04-enable-raw-event-passthrough-in-notification-templates/04-VERIFICATION.md
      provides: Recorded outputs and pass/fail evidence for the phase.
  key_links:
    - from: scripts/verify_template_passthrough.ps1
      to: /webhook/:source_name
      via: sample webhook posts that exercise raw-field rendering
      pattern: "/webhook/"
    - from: scripts/verify_template_passthrough.ps1
      to: notification receiver
      via: assertion on rendered title/content
      pattern: "notification"
---

<objective>
Prove the raw-event passthrough feature works in the real notification path and does not regress legacy templates.

Purpose: close the phase with repeatable evidence for TMPL-03 and retain the project’s script-first verification pattern.
Output: one reusable verification script and one evidence report for Phase 04.
</objective>

<execution_context>
@C:/Users/doumaoxin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/doumaoxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-remove-backend-ai-runtime/01-03-SUMMARY.md
@.planning/phases/03-align-docs-and-verification/03-03-SUMMARY.md
@scripts/verify_backend_no_ai.ps1
@scripts/verify_frontend_no_ai.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Script the Phase 04 passthrough and compatibility verification path</name>
  <read_first>
    - scripts/verify_backend_no_ai.ps1
    - .planning/phases/01-remove-backend-ai-runtime/01-03-SUMMARY.md
    - .planning/phases/03-align-docs-and-verification/03-03-SUMMARY.md
  </read_first>
  <files>scripts/verify_template_passthrough.ps1</files>
  <action>Create `scripts/verify_template_passthrough.ps1` using the same self-contained PowerShell style established in prior phases. The script should prepare a minimal datasource/channel/route setup, then prove two cases against the live backend: one datasource output template that uses only legacy standard fields still renders and dispatches successfully, and one datasource output template that references nested raw webhook fields through the new stable passthrough contract also renders and dispatches the expected notification content. Reuse the lightweight local HTTP listener pattern from Phase 01 for notification capture, and keep the script responsible for setup plus cleanup.</action>
  <verify>
    <automated>pwsh -ExecutionPolicy Bypass -File scripts/verify_template_passthrough.ps1</automated>
  </verify>
  <acceptance_criteria>
    - The script asserts both legacy-template compatibility and raw-event passthrough in one run.
    - The script is self-contained enough to prepare test data, capture outbound notifications, and clean up afterwards.
    - Failures identify which path broke: normalization, output rendering, routing, or notification dispatch.
  </acceptance_criteria>
  <done>A single command can verify the real notification path for both compatibility and raw-field passthrough.</done>
</task>

<task type="auto">
  <name>Task 2: Run verification and record evidence for the phase</name>
  <read_first>
    - scripts/verify_template_passthrough.ps1
    - scripts/verify_frontend_no_ai.ps1
  </read_first>
  <files>.planning/phases/04-enable-raw-event-passthrough-in-notification-templates/04-VERIFICATION.md</files>
  <action>Execute the new passthrough verification script and the existing frontend build gate, then record concise evidence in `04-VERIFICATION.md`: commands run, pass/fail status, and the observed notification proof points for both the legacy-template case and the raw-event passthrough case. Keep the evidence concrete, including the exact raw field(s) validated in the rendered notification output. Do not modify unrelated verification assets outside this phase unless the new script genuinely needs a shared helper.</action>
  <verify>
    <automated>pwsh -ExecutionPolicy Bypass -File scripts/verify_template_passthrough.ps1 && pnpm.cmd --dir frontend build</automated>
  </verify>
  <acceptance_criteria>
    - `04-VERIFICATION.md` names the commands executed and the observed results.
    - Evidence includes one passing legacy-template notification and one passing raw-field notification.
    - The frontend build still passes after datasource UX changes.
  </acceptance_criteria>
  <done>Phase 04 has reusable evidence that the feature works and compatibility is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification script -> local services | Script provisions local test data and drives the real backend notification path |
| local backend -> temporary notification receiver | Verification captures outbound messages and inspects rendered notification content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | R | verification evidence | mitigate | Write explicit command/result evidence to `04-VERIFICATION.md` so the phase can be audited after execution |
| T-04-08 | D | verification script | mitigate | Keep the script self-cleaning and reuse the proven local listener pattern to avoid hanging Windows processes |
| T-04-09 | T | compatibility claim | mitigate | Assert both old-template and raw-template notification bodies in the same scripted run |
</threat_model>

<verification>
Primary: `pwsh -ExecutionPolicy Bypass -File scripts/verify_template_passthrough.ps1`
Secondary: `pnpm.cmd --dir frontend build`
</verification>

<success_criteria>
- A repeatable script proves end-to-end raw-field passthrough in notifications.
- The same evidence proves standard-field-only templates still dispatch successfully.
- Phase 04 ends with a verification report tied to concrete command output.
</success_criteria>

<output>
After completion, create `.planning/phases/04-enable-raw-event-passthrough-in-notification-templates/04-03-SUMMARY.md`
</output>
