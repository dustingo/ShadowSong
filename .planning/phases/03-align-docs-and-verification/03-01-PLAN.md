---
phase: 03-align-docs-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/CODE_REVIEW.md
  - .env
  - frontend/index.html
  - internal/handlers/config.go
autonomous: true
requirements:
  - DATA-01
must_haves:
  truths:
    - "README 和当前用户可见文案不再把产品描述为 AI 告警系统"
    - "本地环境配置参考不再要求 OPENAI 或 AI_* 变量才能完成常规启动"
    - "页面标题与系统测试文案不会再把当前产品表述为 AI 告警系统"
    - "仓库根部的当前文档入口不会再把用户引向已过时的 AI 设计说明"
  artifacts:
    - path: README.md
      provides: "非 AI 版本的项目介绍、启动说明和结构说明"
    - path: docs/CODE_REVIEW.md
      provides: "与当前产品定位一致的审查报告标题与亮点描述"
    - path: .env
      provides: "无 AI 变量的本地开发配置基线"
    - path: frontend/index.html
      provides: "与当前产品定位一致的浏览器页面标题"
    - path: internal/handlers/config.go
      provides: "与无 AI 产品定位一致的测试通知文案"
  key_links:
    - from: README.md
      to: .env
      via: "快速开始与环境变量说明"
      pattern: "OPENAI_|AI_|ai-alert-system|internal/ai"
    - from: README.md
      to: docs/CODE_REVIEW.md
      via: "用户可见命名一致性"
      pattern: "游戏运维 AI 告警系统|AI Alert System"
---

<objective>
统一根部用户入口文档、品牌文案和本地环境配置参考，使当前仓库对外描述与“无 AI 版本”的真实状态一致。

Purpose: 完成 DATA-01 中最直接影响使用者理解和启动配置的部分，避免 README、评审文档和 `.env` 继续误导为 AI 产品。
Output: 已更新的 `README.md`、`docs/CODE_REVIEW.md`、`.env`。
</objective>

<execution_context>
@C:/Users/doumaoxin/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/doumaoxin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-align-docs-and-verification/03-RESEARCH.md
@.planning/phases/01-remove-backend-ai-runtime/01-03-SUMMARY.md
@.planning/phases/02-remove-frontend-ai-surfaces/02-03-SUMMARY.md
@README.md
@docs/CODE_REVIEW.md
@.env
@frontend/index.html
@internal/handlers/config.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 清理 README 与面向用户的命名入口</name>
  <files>README.md, docs/CODE_REVIEW.md, frontend/index.html</files>
  <read_first>
    .planning/phases/03-align-docs-and-verification/03-RESEARCH.md
    README.md
    docs/CODE_REVIEW.md
    .planning/phases/02-remove-frontend-ai-surfaces/02-03-SUMMARY.md
  </read_first>
  <action>按 D-Documentation 目标执行当前态修订：把 `README.md` 标题、简介、项目结构、API 文档入口和启动说明改成非 AI 告警系统表述；删除对 `internal/ai/` 和 `.kiro/specs/ai-alert-system/design.md` 的当前产品导向引用；同步把 `docs/CODE_REVIEW.md` 中的项目名和“代码亮点”里仍把 AI 集成当成现有能力的描述改成符合现状的非 AI 运维告警表述；同时检查 `frontend/index.html` 的 `<title>`，若仍包含 `AI` 或旧产品名则改为 `游戏运维告警系统`。不要改写历史 phase 报告，不要创造新的 AI 替代能力描述。</action>
  <verify>
    <automated>rg -n '游戏运维 AI 告警系统|AI Alert System|internal/ai|\.kiro/specs/ai-alert-system/design\.md|AI 集成' README.md docs/CODE_REVIEW.md frontend/index.html</automated>
  </verify>
  <acceptance_criteria>`README.md`、`docs/CODE_REVIEW.md` 与 `frontend/index.html` 中不再出现把当前产品定义为 AI 系统的标题、目录或亮点描述；`rg` 对上述旧文案返回 0 条匹配。</acceptance_criteria>
  <done>仓库根目录的主要阅读入口和浏览器标题已经准确描述当前产品为非 AI 告警系统，且没有继续把已删除的 AI 模块当作现行结构或主文档入口。</done>
</task>

<task type="auto">
  <name>Task 2: 收口本地环境配置参考与测试文案中的 AI 残留</name>
  <files>.env, README.md, internal/handlers/config.go</files>
  <read_first>
    .env
    README.md
    .planning/phases/01-remove-backend-ai-runtime/01-VERIFICATION.md
    internal/config/config.go
    internal/handlers/config.go
  </read_first>
  <action>在不触碰无关用户本地配置值的前提下，最小化编辑 `.env`：删除 `OPENAI_API_KEY`、`OPENAI_API_BASE`、`AI_MODEL`、`AI_TIMEOUT` 四个 AI 专用变量，并把 AI 品牌化的数据库名/注释改成中性告警系统命名；同步调整 `README.md` 的环境变量说明，明确正常启动只需要数据库、Redis、服务端口和 JWT 相关配置。与此同时检查 `internal/handlers/config.go` 的测试通知标题与内容，若仍出现 `AI Alert System`、`游戏运维 AI 告警系统` 或其他 AI 产品文案，则改成 `游戏运维告警系统` 的中性测试消息。不要批量重写整个 `.env`，只处理 AI 专用键、品牌化命名和测试文案残留。</action>
  <verify>
    <automated>rg -n 'OPENAI_API_KEY|OPENAI_API_BASE|AI_MODEL|AI_TIMEOUT|ai_alert_system|游戏运维 AI 告警系统|AI Alert System' .env README.md internal/handlers/config.go</automated>
  </verify>
  <acceptance_criteria>`.env` 和 `README.md` 不再要求或示例化 AI 变量，AI 品牌化数据库名在这些文件中被清理或替换，且 `internal/handlers/config.go` 的测试通知文案不再包含 AI 产品名；`rg` 对上述模式返回 0 条匹配。</acceptance_criteria>
  <done>本地环境配置参考和系统测试文案已经与无 AI 运行时一致，用户按 README、`.env` 和测试通知理解项目时不会再误以为必须提供或仍在使用 AI 配置。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs→operator understanding | 文档与环境示例决定运维人员如何配置和认知当前系统 |
| local `.env`→runtime startup | 本地配置参考直接影响开发/验证时的启动参数 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | README.md | mitigate | 删除已失效 AI 模块、设计文档和配置说明，避免操作者按过时文档错误配置系统 |
| T-03-02 | I | .env | mitigate | 仅删除 AI 专用键并收口品牌化命名，不在 PLAN 中扩散或新增敏感值，避免继续暴露无用第三方密钥入口 |
| T-03-03 | R | docs/CODE_REVIEW.md | accept | 保留历史审查结论主体，仅修正文档头部和能力描述，接受该报告仍带有历史时间戳 |
</threat_model>

<verification>
`rg -n '游戏运维 AI 告警系统|AI Alert System|internal/ai|\.kiro/specs/ai-alert-system/design\.md|AI 集成|OPENAI_API_KEY|OPENAI_API_BASE|AI_MODEL|AI_TIMEOUT|ai_alert_system' README.md docs/CODE_REVIEW.md .env frontend/index.html internal/handlers/config.go`
</verification>

<success_criteria>
`README.md`、`docs/CODE_REVIEW.md`、`.env`、`frontend/index.html` 与 `internal/handlers/config.go` 中本计划负责的标题、测试文案、AI 配置键和 AI 品牌化命名均已与“无 AI 版本”的实际状态一致。
</success_criteria>

<output>
After completion, create `.planning/phases/03-align-docs-and-verification/03-01-SUMMARY.md`
</output>
